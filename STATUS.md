# Status — jellystructure + Ravilo

**This is the single source of truth for phase status.** Every phase across both products lives in
one of the two tables below, with exactly one status column. A spec's file location or body no
longer encodes status — only this file does.

- **Status values:** `✓ Done` = implemented in code · `⚠ Partial` = shipped but a real piece of the
  spec is genuinely unbuilt (the Focus cell's **⚠ GAP** says what) · `Planned` = spec written, not
  built. Other nuance (e.g. "on-device test pending") rides in the Focus cell.
- **Spec content** lives flat in [`specs/requirements/`](specs/requirements/) (admin) and
  [`specs/ravilo/requirements/`](specs/ravilo/requirements/) (Ravilo). Architecture is in
  [`specs/constitution.md`](specs/constitution.md) + [`specs/plan.md`](specs/plan.md) (admin) and
  the matching files under [`specs/ravilo/`](specs/ravilo/).
- **The "updated designs" sync overwrites everything under `specs/` AND `design/` AND `CLAUDE.md`,
  but never touches this `STATUS.md` or `scripts/`.** (CLAUDE.md is design-project-owned, so it gets
  re-reverted on each sync until the design export is updated — keep status here, not there.) After
  every sync run `scripts/check-phases.sh` — it flags any spec file with no row here (newly added →
  add a row) and any row whose spec file is gone (deleted by the sync → investigate). That is the
  "never forget a phase" guarantee.

## Current focus

**Admin phases 0–81 + Ravilo R01–R62 all ✓ Done.** No `⚠ Partial` phases remain. **16 phases Planned**
(specced 2026-06-26, not yet built):
- **Ravilo:** R63 (TV image-pipeline perf), R64 (rename "Channels & Collections" → "Channels"), R65
  (navigate-up reveals the row title + clears the app bar), R66 (shared `ChannelButtonSpec` + accurate
  admin preview), R67 (stop focus jumping to Home + stop needless genre reloads), R68 (strip ASS subtitle
  tags), R69 (compact player chrome + modern play/pause), R70 (real hero artwork in the config schematic
  preview), R71 (Merge-newly-added controls 1-vs-2 Newly-Added rows), R72 (detail-screen scroll breathing
  room + Play/Resume snaps to top), R73 (per-channel item-match count in the Channels list), R74
  ("Match ANY" must OR conditions in the count + Library).
- **Admin:** 82 (tag colour swatches render + tag-modal footer fits), 83 (live scan results respect the
  active Library filter), 84 (debounce the Library search), 85 (restore the Settings tabbed layout —
  sync-stripped CSS).

The 2026-06-25 done-ness audit found 7 falsely-Done phases and all 7 are now genuinely implemented
(70 brand mark, 72 TrackEditor language equivalence, 74 metadata write-through, 76 presence-matrix
toggle, R17 lazy hls.js/JASSUB, R18 Kids flag, R56 PlaybackInfo negotiation). Phases 41/42/46 stay
✓ Done — their staged-changes/command-preview UX was deliberately superseded by Phase 71.

> **Recurring sync regression:** each "updated designs" sync strips the Phase 75/76/79 cast CSS from
> `design/app/wf.css` and reverts `CLAUDE.md`. Both are restored in the repo but **must be fixed in
> the design-project source** to stop the loop (the cast tab renders unstyled without the CSS).

Non-blocking follow-ups (not bugs): Phase 78 scan-time people-cache pre-warm (the `Semaphore(8)`
already hard-caps the crash); R60 portrait polish via responsive `RaviloDimens` (the phone app
currently reflows with TV-tuned sizing, on-device test is the user's). Older open thread: the
`checkPortFree` TODO in `Main.kt`.

## Admin phases (jellystructure)

| Phase | Status | Focus | Spec |
|-------|--------|-------|------|
| 0 | ✓ Done | Scaffolding — Gradle KMP, Docker Compose, native binary + WASM bundle | — |
| 1 | ✓ Done | Config, DB, auth (Jellyfin sign-in, session cookie, admin gate), library mapping | — |
| 2 | ✓ Done | Jellyfin-driven discovery, ffprobe track data, TMDB match, Library + Media Detail UI | — |
| 3 | ✓ Done | NFO write (movie + tvshow), artwork download, language resolver, language settings UI | — |
| 4 | ✓ Done | Track editing (mkvpropedit/ffmpeg), triage queue, live WebSocket scan, Jellyfin refresh | — |
| 5 | ✓ Done | Folder watcher, Playwright E2E tests, fixture builder, series episode tab | — |
| 6 | ✓ Done | Per-episode TMDB metadata, episodedetails.nfo, episode stills, per-episode track editing | — |
| 7 | ✓ Done | Persistent scan state + resume (FR-S1) | [spec](specs/requirements/phase-07-persistent-scan-state.md) |
| 8 | ✓ Done | Three-way theme picker — Light / Dark / System (FR-T1) | [spec](specs/requirements/phase-08-three-way-theme-picker.md) |
| 9 | ✓ Done | Per-field dirty indicators + diff popup (FR-D2) | [spec](specs/requirements/phase-09-dirty-indicators-diff-popup.md) |
| 10 | ✓ Done | External links — Jellyfin + TMDB (FR-X1) | [spec](specs/requirements/phase-10-external-links.md) |
| 11 | ✓ Done | Language pickers — searchable dropdowns (FR-L1) | [spec](specs/requirements/phase-11-language-pickers.md) |
| 12 | ✓ Done | Simplify TV series language UI (FR-U1) | [spec](specs/requirements/phase-12-simplify-series-language-ui.md) |
| 13 | ✓ Done | Sync single media item (FR-S2) | [spec](specs/requirements/phase-13-sync-single-media-item.md) |
| 14 | ✓ Done | Persistence layer — SQLite/SQLDelight (FR-P1) | [spec](specs/requirements/phase-14-persistence-sqlite.md) |
| 15 | ✓ Done | Library path-match diagnostics + path-check (FR-B1) | [spec](specs/requirements/phase-15-library-path-matching.md) |
| 16 | ✓ Done | Multi-worker scanner — dynamic workers + thread pool (FR-W1) | [spec](specs/requirements/phase-16-multi-worker-scanner.md) |
| 17 | ✓ Done | Activity log backend — persistent log, filters, live runners (FR-A1) | [spec](specs/requirements/phase-17-activity-log-backend.md) |
| 18 | ✓ Done | Settings page cleanup — nav fix, section reorganisation (FR-C1) | [spec](specs/requirements/phase-18-settings-page-cleanup.md) |
| 19 | ✓ Done | Studios, Networks, Genres & Tags metadata page (FR-M1) | [spec](specs/requirements/phase-19-metadata-page.md) |
| 20 | ✓ Done | Library audio-track filter — find audio-description/commentary tracks (FR-LF1) | [spec](specs/requirements/phase-20-library-audio-track-filter.md) |
| 21 | ✓ Done | Flag multiple default audio tracks in Triage (FR-DA1) | [spec](specs/requirements/phase-21-multiple-default-audio-triage.md) |
| 22 | ✓ Done | Remove lockdata & detect Jellyfin field locks (FR-LK1) | [spec](specs/requirements/phase-22-remove-lockdata-detect-locks.md) |
| 23 | ✓ Done | Remove the Language page (FR-RL1) | [spec](specs/requirements/phase-23-remove-language-page.md) |
| 24 | ✓ Done | Manage the TMDB ID field on Media Detail (FR-TI1) | [spec](specs/requirements/phase-24-manage-tmdb-id.md) |
| 25 | ✓ Done | Fix Sync button → "Re-pull from Jellyfin…" (FR-RJ1) | [spec](specs/requirements/phase-25-repull-from-jellyfin.md) |
| 26 | ✓ Done | qBittorrent seeding guard — block mkvpropedit when file is actively seeded (FR-QB1) | [spec](specs/requirements/phase-26-qbittorrent-seeding-guard.md) |
| 27 | ✓ Done | Merge Triage into Media Detail + floating Triage dock (FR-MT1) | [spec](specs/requirements/phase-27-merge-triage-into-media-detail.md) |
| 28 | ✓ Done | URL-based tab & view-state navigation (FR-UN1) | [spec](specs/requirements/phase-28-url-based-tab-navigation.md) |
| 29 | ✓ Done | Multi-language library search — remember every title ever pulled (FR-ML1) | [spec](specs/requirements/phase-29-multi-language-library-search.md) |
| 30 | ✓ Done | Library multi-axis filters — studio/network/genre/tags dropdowns + meta-facets (FR-LMF1) | [spec](specs/requirements/phase-30-library-multi-axis-filters.md) |
| 31 | ✓ Done | Studio & network logo artwork — fetch/cache/serve + batch (FR-SNA1) | [spec](specs/requirements/phase-31-studio-network-artwork.md) |
| 32 | ✓ Done | In-app TMDB match picker — search & pick the right match (FR-TM1) | [spec](specs/requirements/phase-32-tmdb-match-picker.md) |
| 33 | ✓ Done | Jellyfin ⇄ NFO drift detection + re-assert (FR-DR1) | [spec](specs/requirements/phase-33-jellyfin-nfo-drift.md) |
| 34 | ✓ Done | Undo / revert from history (FR-UR1) | [spec](specs/requirements/phase-34-undo-revert-history.md) |
| 35 | ✓ Done | System health panel — real test-connections + tool/disk checks (FR-HC1) | [spec](specs/requirements/phase-35-system-health-panel.md) |
| 36 | ✓ Done | Operator controls — per-library scan/push, scheduled scans, notifications (FR-OC1) | [spec](specs/requirements/phase-36-operator-controls.md) |
| 37 | ✓ Done | Surface the qBittorrent seeding guard (FR-QS1) | [spec](specs/requirements/phase-37-surface-qbittorrent-guard.md) |
| 38 | ✓ Done | Command palette ⌘K + attention-queue keyboard nav (FR-KB1) | [spec](specs/requirements/phase-38-command-palette-keyboard-nav.md) |
| 39 | ✓ Done | Subtitle management — forced flag toggle on subtitle tracks, MKV only (FR-SUB1) | [spec](specs/requirements/phase-39-subtitle-management.md) |
| 40 | ✓ Done | Configure qBittorrent in Settings — guard opt-in; off ⇒ no cross-seed safety (FR-QC1) | [spec](specs/requirements/phase-40-configure-qbittorrent-settings.md) |
| 41 | ✓ Done | Merge track-order page into Media Detail "Tracks & order" tab — unified movie editor + warnings (FR-TO1) — _note: staged-changes/command-preview UX superseded by Phase 71 (write-through); page-merge + editor done._ | [spec](specs/requirements/phase-41-merge-track-order-into-detail.md) |
| 42 | ✓ Done | Per-episode track & order editor on Series detail — same editor in a modal (FR-TO2) — _note: per-episode staged-changes UX superseded by Phase 71; modal + editor done._ | [spec](specs/requirements/phase-42-series-episode-track-editor.md) |
| 43 | ✓ Done | Library page: design fidelity (Movies/TV switch) + infinite virtual scroll replacing pagination (FR-LV1) | [spec](specs/requirements/phase-43-library-fidelity-infinite-scroll.md) |
| 44 | ✓ Done | NFO raw viewer — read-only on-disk XML with a multi-file tree sidebar (FR-NR1) | [spec](specs/requirements/phase-44-nfo-raw-viewer-tree.md) |
| 45 | ✓ Done | Fix the track-editor language picker — unstyled `.langmenu` popup; unify on the shared styled picker (FR-LP1) | [spec](specs/requirements/phase-45-track-editor-language-picker-css.md) |
| 46 | ✓ Done | Track language writes must persist — 2↔3-letter code mapping, verify-after-write, truthful response + preview (FR-TL1) — _note: language write+persist+verify done; command-preview UI dropped with Phase 71's staged model._ | [spec](specs/requirements/phase-46-track-language-write-persist.md) |
| 47 | ✓ Done | Artwork manager on Movie & Series detail — asset rail + inline TMDB gallery, resolved-language fallback (no-language ≠ All), drag-drop/upload/URL, season posters + episode stills (FR-AM1) | [spec](specs/requirements/phase-47-artwork-manager.md) |
| 48 | ✓ Done | Artwork manager — fix the Textless / With-text filter: it only re-sorted (a no-op within a language bucket) and overlapped the language chips; unified both into one real single-select filter on TMDB `iso_639_1` — `All · Textless · With text · <langs>` (FR-AM2) | [spec](specs/requirements/phase-48-artwork-textless-filter.md) |
| 49 | ✓ Done | Full episode coverage for large series — the scan probed only a spread of 100 episodes (>100-ep series showed scattered gaps in Seasons & Episodes + Artwork, with no UI recovery); make the cap configurable (`scan_episode_cap`, default 0 = unlimited) + wire an on-demand uncapped "Re-scan all episodes" (FR-EP1) | [spec](specs/requirements/phase-49-full-episode-coverage.md) |
| 50 | ✓ Done | Fix Jellyfin item-refresh auth + single-item fetch — `refreshItem`'s `Authorization` header was malformed (missing `, Token=`) → 401 on every targeted refresh; `getItem` used the unreliable non-user-scoped `/Items/{id}` endpoint and the client had no response validator, so a non-2xx `text/plain` body threw `NoTransformationFoundException` (400). Routed all calls through one `jellyfinAuth` helper, added a 2xx-gated `bodyOrNull`, re-pointed `getItem` to the proven `/Items?Ids=` list shape, and returned the refresh outcome from `pushToJellyfin` (FR-JR1) | [spec](specs/requirements/phase-50-jellyfin-refresh-auth-fix.md) |
| 51 | ✓ Done | Tag population & merge lifecycle — item tags were never read from Jellyfin (`Tags` field unrequested, no DTO slot, scanner left `tags=[]`) and the Phase 19 §15 merge was unimplemented (full re-scan wiped even JS tags). Now: full scan = Jellyfin `Tags` + JS; re-pull from TMDB = TMDB **keywords** + JS; re-pull from Jellyfin = union; JS-defined tags always survive (FR-TG1) | [spec](specs/requirements/phase-51-tag-population-lifecycle.md) |
| 52 | ✓ Done | Tag UX — sync the media-detail tags section to `design/app/media.html` (own card, manage-tags link, dotted JS chips, outline dirty ring) + distinguish JS vs normal tags in the filter workbench & Library pickers (server-colored facet, dotted + grouped). Also corrected the stale "Tailwind" note: the app ships `wf.css`/`app.css` verbatim via `syncDesignAssets` (FR-TG2) | [spec](specs/requirements/phase-52-tag-ux-design-sync.md) |
| 53 | ✓ Done | Scanner data-quality fixes — from a post-reset full-sync review: full-scan `year` was null everywhere (now `searchYear = name ?? ProductionYear` for search/slug, stored year prefers TMDB); `slugify` made empty/colliding ids that silently dropped items (non-Latin → `""`, dup titles collide) → `itemId` falls back to `jf-<jellyfinId>` + `disambiguateIds` de-dupes collisions; widened the `SxxExx` season regex (`S2025E01`); scan now logs a skipped/unmatched-items summary; minor (zero-track-episode warn) (FR-SQ1) | [spec](specs/requirements/phase-53-scanner-data-quality.md) |
| 54 | ✓ Done | Configure Radarr & Sonarr in Settings — opt-in, read-only `[radarr]`/`[sonarr]` integrations (default off): read root folders → import as `[[libraries]]`, and best-effort `RescanMovie`/`RescanSeries` after JS writes. No acquisition/mutation; never blocks a write; api-key masked + `##KEEP##` like qBittorrent (FR-AR1) | [spec](specs/requirements/phase-54-configure-radarr-sonarr.md) |
| 55 | ✓ Done | Settings as URL-addressable tabs — convert the long-scroll Settings into 6 `?tab=` panels (Connections · Libraries [mapping+scanning] · Metadata · Download tools [Radarr/Sonarr+cross-seed] · Notifications · Advanced) via the Phase 28 `replaceState` Router; drop the scroll-spy; health-check failures aggregate to per-tab badges + switch-to-tab (FR-ST1) | [spec](specs/requirements/phase-55-settings-tabbed-navigation.md) |
| 56 | ✓ Done | Radarr/Sonarr acquisition pipeline + unified download-status state machine — request a not-in-library title (add+search) and track it through one shared `AcquisitionStatus` enum: `requested` (not yet at the download client) → `queued` (in the client queue) → `downloading` (% + `stalled`/`metadata` flags) → `importing` → `available` (+ `failed`); `*arr` queue is source of truth, qBittorrent enriches; `acquisition_changed` WS events; request+track only (FR-AQ1) | [spec](specs/requirements/phase-56-arr-acquisition-pipeline.md) |
| 57 | ✓ Done | Chart/Discover feed ingestion (vendor-abstracted; Netflix via Tudum first) — `ChartProvider` abstraction + `ChartEntry` model; country movies/TV (rank-only), global movies, non-English, all-time (views); weekly history for trend badges; TMDB-resolve + library-match at ingest; country feeds carry no views by construction (FR-CH1) | [spec](specs/requirements/phase-57-chart-discover-ingestion.md) |
| 58 | ✓ Done | Dark-theme comfort palette ("Soft Charcoal") — retune the **dark** surface/ink tokens only: lift the near-black floor (`--bg #0b0c12 → #16181f`), dim ink to ~88% (`--ink #eef0f7 → #dde1ec`) to kill glare, and raise card surfaces (`--card-bg`) so panels still read as elevated. Light theme, accent + status tokens, and all per-screen CSS untouched; retunes from one `:root` edit (FR-DK1) | [spec](specs/requirements/phase-58-dark-theme-comfort-palette.md) |
| 70 | ✓ Done | Brand mark "Quartet Play" + app logo wiring — replace the placeholder CSS glyph with a real SVG mark (2×2 structure grid whose open slot resolves into a play triangle) on the Aurora gradient; wired into the sidebar, mobile topbar and login, with a full asset set (icon sizes, favicon, mono, lockups, OG). No new color tokens; theme-independent (FR-BR1) | [spec](specs/requirements/phase-70-brand-mark-quartet-play.md) |
| 71 | ✓ Done | Detail-page **write-through editing** + **artwork lightbox** — drop the stage→apply model on Movie/Series detail: artwork candidates and track flag/order edits now write to disk immediately (toasts convey `mkvpropedit` ~40ms vs `ffmpeg -c copy` remux cost; **Save & sync to Jellyfin** pushes, else Jellyfin picks up on next scan). The "Staged changes" card is removed. Artwork gallery gains a hover ⤢ **lightbox** — large preview + metadata, ←/→ to browse, **Use this artwork** writes (FR-DE1) | [spec](specs/requirements/phase-71-detail-write-through-lightbox.md) |
| 72 | ✓ Done | Canonical language-code equivalence across all comparisons — fix the false **"Default ≠ resolved"** cascade (raw `eng` track tag compared to normalised `en` → mis-fires on nearly every tagged movie). One `LanguageResolver.sameLanguage()` routed through every language equality check (cascade, triage mismatch, majority/mix, filters); comparison/display-only (FR-LC1) | [spec](specs/requirements/phase-72-canonical-language-equivalence.md) |
| 73 | ✓ Done | **Write-through metadata sync to Jellyfin** — `PATCH …/metadata` saved to DB only, never writing the NFO or refreshing Jellyfin; fix launches `pushToJellyfin` after `store.updateOne`. **⚠ Trigger model revised by Phase 74** (design): the PATCH should persist to DB only, with the NFO write moved to the explicit Save action (FR-WM1) | [spec](specs/requirements/phase-73-write-through-metadata-sync.md) |
| 74 | ✓ Done | **Metadata write-through to the library DB; Save→NFO / Sync→Jellyfin** — title/year/plot/director/studio + tags write **directly to the DB** on edit (toasts), dropping the **"Save changes"** button, amber dirty borders and the diff popup (supersedes Phase 9). The top split button is reframed into **Save → NFO** (write our setup to the .nfo files), **Sync Jellyfin** (ask Jellyfin to re-read them), and a **Save & Sync** primary. Revises Phase 73's per-PATCH push: the PATCH persists to DB only (FR-WM2) | [spec](specs/requirements/phase-74-metadata-write-through.md) |
| 75 | ✓ Done | **Cast & crew — TMDB people, detail editor, NFO sync + person images** — fetch cast/crew from TMDB credits; new **Cast & crew** tab on Movie + Series detail (cast grid with drag-reorder + editable role, crew grouped by department, TMDB person-search add). Write-through (Phase 74): DB on edit, **Save → NFO** writes `<actor>`/`<director>`/`<writer>`. Person photos cached & served (Phase 31 pattern) and written as `<actor><thumb>` so Jellyfin pulls them — across movies, series & episode guest stars. New `Person` model; `cast[]`/`crew[]` on items (FR-CC1) | [spec](specs/requirements/phase-75-cast-crew.md) |
| 76 | ✓ Done | **Scope-aware season/episode cast** — Series-detail Cast & crew gains a **Series · Season · Episode** switcher. Main cast = TMDB `aggregate_credits` (inherited by every episode, → `tvshow.nfo`); episodes show inherited cast (read-only) + editable **guest stars** + crew (→ `episodedetails.nfo`). Season scope is a **presence matrix** (All-seasons numbered view; per-season E1…En tap-to-toggle). ⓘ help popup explains the TMDB→NFO→Jellyfin flow (FR-CC2) | [spec](specs/requirements/phase-76-scope-aware-season-episode-cast.md) |
| 78 | ✓ Done | **File-descriptor exhaustion crash fix** — Native CIO server's `select()` caps FDs at FD_SETSIZE=1024; a flood of cold `/api/people/{id}/image` requests opened unbounded outbound sockets + temp files → fatal selector throw. Fix: `Semaphore(8)` bounding `LogoDownloader.download`, O(1) cached `MediaStore.personProfilePath` (was a full-library deserialize per request), `loading="lazy"` on person imgs. Scan-time pre-warm deferred (FR-FD1) | [spec](specs/requirements/phase-78-fd-exhaustion-crash.md) |
| 79 | ✓ Done | **Cast tab: episode-count badge overlaid on the photo** — the `▸ N eps` / `⤓ inherited` badges moved inside the `.ph` photo div (anchored `bottom:6px`) so they overlay the image's lower-left instead of floating over the text body; `onerror` no longer wipes them (FR-CC3) | [spec](specs/requirements/phase-79-cast-badge-overlay.md) |
| 80 | ✓ Done | **Cast presence matrix reflects real TMDB per-season data** — added TMDB `/season/{n}/aggregate_credits`, populate real `Person.seasonEpisodeCounts` at scan time; the matrix shows true per-season counts (absent seasons blank), per-season view lists only cast in that season, guest stars stay episode-accurate; NFO episode inheritance follows season membership + per-episode overrides (FR-CC4) | [spec](specs/requirements/phase-80-cast-presence-matrix-real-data.md) |
| 81 | ✓ Done | **Artwork tab: selecting a new image updates which one is "on disk"** — picking a different poster/backdrop left the **ON DISK** ribbon stuck on the old image (the file was overwritten on disk, but `posterPath`/`backdropPath` were never updated, and the gallery flags on-disk by comparing the candidate's TMDB `file_path` to that field). `candidates/save` now writes the chosen TMDB path through to the item via `updateOne` (custom http URLs still overwrite the file but don't touch the field; clearlogo/banner stay file-existence based); the gallery already reloads, so the ribbon follows the selection (FR-AM3) | [spec](specs/requirements/phase-81-artwork-selection-write-through.md) |
| 82 | Planned | **Tag color swatches render + tag-modal footer fits** — on `/#/metadata?tab=tags` the colour picker *looks* absent and the edit-modal's Delete button floats outside the popup. Premise-corrected: the colour field + swatch picker + backend `color` are all already wired; the real bug is the `.swatch`/`.swatches`/`.swatch.on` (+ `.tag-card`/`.tag-dot-lg`) CSS lives only in the `metadata.html` mockup, never ported to shipped `wf.css` → swatches render zero-size. Fix: port the swatch CSS, align `PRESET_COLORS` to the design's Aurora palette (default `#7b6ef0`), and make the modal footer wrap/group (`[Delete] [View in library]` ‖ `[Cancel] [Save]`, drop `margin-right:auto`) so it fits (FR-TG3) | [spec](specs/requirements/phase-82-tag-color-swatches-and-modal-footer.md) |
| 83 | Planned | **Live scan results must respect the active Library filter** — during a scan, the `ItemScanned` WS handler (`Library.kt:418`) `appendItemToGrid`s **every** scanned item unconditionally, ignoring the active search/filter, so a filtered list gets polluted (the normal `/api/media` path filters server-side; the live insert bypasses it). Fix: gate the live insert on a `queryActive` flag — stream live only when no query is active, else show a "N new items — refresh" affordance that re-runs the server-filtered `loadMore(reset=true)` (keeps FE=BE; client-side matcher deferred — would need predicates moved to commonMain) (FR-SQ2) | [spec](specs/requirements/phase-83-scan-results-respect-active-filter.md) |
| 84 | Planned | **Debounce the Library search (URL + results)** — the search box fires `loadMore(reset=true)` on every keystroke (network fetch + "Loading…" grid teardown + 60-card rebuild each time), and the `libLoading` guard drops mid-flight keystrokes so URL/results can go stale. Not history spam / not re-render (already `replaceState`, no `hashchange`). Fix: debounce ~250ms like the ⌘K palette (capture latest `libSearch` immediately, cancel-previous + `delay` → `loadMore`), optionally skip the loading-flash on a search refresh — type freely, live results, URL ends at the final value (FR-LS1) | [spec](specs/requirements/phase-84-library-search-debounce.md) |
| 85 | Planned | **Restore the Settings tabbed layout (sync-stripped CSS)** — Phase 55's `?tab=` tabs stopped working; premise-corrected: the `Settings.kt` tab machinery is fully intact, but a later "updated designs" sync (`3650d61`) **deleted** the two load-bearing CSS rules `.set-section[data-tab]{display:none}` / `.set-section[data-tab].tab-show{display:block}` from `wf.css` (added by `7bc224f`), so all six panels fall back to `display:block` = long scroll. Fix: re-add the two rules to `design/app/wf.css` (no Kotlin change) **and** to the design-project source so the next sync doesn't strip them again — same recurring sync issue as the cast CSS (FR-ST2) | [spec](specs/requirements/phase-85-settings-tabs-restore-css.md) |

## Ravilo phases (Android TV · Web · phone)

| Phase | Status | Focus | Spec |
|-------|--------|-------|------|
| R01 | ✓ Done | `:shared` KMP module — DTOs + Ktor `TvApiClient` (once, for backend + both clients) | [spec](specs/ravilo/requirements/phase-R01-shared-module.md) |
| R02 | ✓ Done | Compose Multiplatform scaffolding — `:ravilo-ui` + `:ravilo-android` + `:ravilo-web`, one "hello focus" screen on both targets | [spec](specs/ravilo/requirements/phase-R02-compose-mp-scaffolding.md) |
| R03 | ✓ Done | Backend — `/api/tv/**` namespace + TV **device pairing** auth (code flow, non-admin users, device sessions) | [spec](specs/ravilo/requirements/phase-R03-device-pairing-auth.md) |
| R04 | ✓ Done | Backend — per-Jellyfin-user **RaviloConfig store** (server-owned, synced across devices) + `GET /api/tv/config` | [spec](specs/ravilo/requirements/phase-R04-per-user-config-store.md) |
| R05 | ✓ Done | Backend — **home feed** composition (hero, channels, rows incl. merged Continue+NextUp & Newly-Added) | [spec](specs/ravilo/requirements/phase-R05-home-feed-api.md) |
| R06 | ✓ Done | Backend — **browse + multi-language search** + channel-scoped feeds (reuse Phase 29/30) | [spec](specs/ravilo/requirements/phase-R06-browse-search-api.md) |
| R07 | ✓ Done | Backend — **detail** API (movie/series, seasons/episodes, watched-state/resume, cast, related) | [spec](specs/ravilo/requirements/phase-R07-detail-watched-state-api.md) |
| R08 | ✓ Done | Backend — **playback brokering + progress reporting** (StreamTicket, heartbeats, mark played/next-up) | [spec](specs/ravilo/requirements/phase-R08-playback-brokering-reporting.md) |
| R09 | ✓ Done | `:ravilo-ui` — **design system + focus engine** (Aurora/Midnight/Noir, components, shared D-pad/pointer model) + app bar | [spec](specs/ravilo/requirements/phase-R09-design-system-focus-engine.md) |
| R10 | ✓ Done | `:ravilo-ui` — **Home** (hero carousel, channel rail, content rows) wired to `GET /api/tv/home` | [spec](specs/ravilo/requirements/phase-R10-home-screen.md) |
| R11 | ✓ Done | `:ravilo-ui` — **Channel view + browse grids** (Movies/Series/My List, filters) | [spec](specs/ravilo/requirements/phase-R11-channel-and-browse-grids.md) |
| R12 | ✓ Done | `:ravilo-ui` — **Search** (on-screen keyboard + live results) | [spec](specs/ravilo/requirements/phase-R12-search-screen.md) |
| R13 | ✓ Done | `:ravilo-ui` — **Movie & Series detail** (season picker, episode rail, watched/up-next/resume) | [spec](specs/ravilo/requirements/phase-R13-detail-screens.md) |
| R14 | ✓ Done | **Player** — `RaviloPlayer` expect/actual (direct ExoPlayer/Media3 on Android, browser `<video>` on WASM); full player chrome; episode rail; next-up countdown; Audio & Subs picker | [spec](specs/ravilo/requirements/phase-R14-player.md) |
| R15 | ✓ Done | On-device **Settings** + first-run **pairing UX** (server URL setup, skin picker, playback prefs) | [spec](specs/ravilo/requirements/phase-R15-ondevice-settings-pairing.md) |
| R16 | ✓ Done | jellystructure web — **Ravilo config screen** backed by the R04 store (hero/channels/rows/merge/skin, per user) | [spec](specs/ravilo/requirements/phase-R16-jellystructure-config-screen.md) |
| R17 | ✓ Done | **Web target hardening** + packaging — `:ravilo-web` canvas a11y/input, both bundles served, E2E across TV + web | [spec](specs/ravilo/requirements/phase-R17-web-target-packaging.md) |
| R18 | ✓ Done | **Multi-user profiles & fast switching** — several Jellyfin users per TV, cached tokens, "Who's watching?" + avatar switcher | [spec](specs/ravilo/requirements/phase-R18-multi-user-profiles.md) |
| R19 | ✓ Done | **Interface localization** (en / da / fo), per Jellyfin user, set in Jellystructure | [spec](specs/ravilo/requirements/phase-R19-localization.md) |
| R20 | ✓ Done | **Performance & correctness overhaul** — focus-latch freeze fix, store coroutine hygiene, allocation hot-paths, CastCircle D-pad focus | [spec](specs/ravilo/requirements/phase-R20-performance-overhaul.md) |
| R21 | ✓ Done | **Back navigation + focus polish** — D-pad Back pop, spring animation on all focusables, halo focus glow | [spec](specs/ravilo/requirements/phase-R21-back-navigation-polish.md) |
| R22 | ✓ Done | **Android TV APK packaging** — TV banner, adaptive icon, splash screen, R8 release build + signing | [spec](specs/ravilo/requirements/phase-R22-apk-packaging.md) |
| R23 | ✓ Done | **Design system foundations** — bundled Sora + Space Grotesk fonts, corrected skin palettes (Noir warm-gold), new tokens, `RaviloDimens` | [spec](specs/ravilo/requirements/phase-R23-design-system-foundations.md) |
| R24 | ✓ Done | **Component upsizing & visual fidelity** — AppBar 92 dp, Hero 600 dp/72 sp, Tile full-size + gradient fallback, channel card, episode card, row headers, buttons | [spec](specs/ravilo/requirements/phase-R24-component-visual-fidelity.md) |
| R25 | ✓ Done | **Polish: loading skeletons, focus glow fix, smooth scroll** — shimmer skeletons, `focusGlow` shadow color, minimal-scroll `LazyRow`, season progress bar, browse chip contrast | [spec](specs/ravilo/requirements/phase-R25-polish-loading-states.md) |
| R26 | ✓ Done | **Config DTO unification** — drop parallel `Admin*` DTOs; `wasmJsMain` uses `:shared` `RaviloConfig` directly; backend validates IDs and clamps values | [spec](specs/ravilo/requirements/phase-R26-config-dto-unification.md) |
| R27 | ✓ Done | **Layout model extensions** — `HeroConfig.enabled/order`, `heroHeightPct`, `autoAdvanceSeconds`; feed filters by enabled, sorts by order, passes layout params | [spec](specs/ravilo/requirements/phase-R27-layout-model-extensions.md) |
| R28 | ✓ Done | **Config editor fidelity** — `/ravilo` rebuilt: drag-reorder, show/hide toggles, typed filters, system-row protection, hero height slider, tile-shape picker, live schematic preview | [spec](specs/ravilo/requirements/phase-R28-config-editor-fidelity.md) |
| R29 | ✓ Done | **TV control-plane contract review fixes** — stop/settings request-type bugs, server `ignoreUnknownKeys`, shared `ViewerSettingsRequest`, `viewerSkinOverride`/`effectiveSkin`, `session_id` removal, FocusGrid empty-row guard, R26 clamp/UUID reconcile, `:ravilo-player` deferred doc sync | [spec](specs/ravilo/requirements/phase-R29-tv-contract-review-fixes.md) |
| R30 | ✓ Done | **Native focus traversal** — replace the manual `FocusEngine` (per-item `requestFocus`) with Compose focus search + `focusRestorer`; fixes held-key lag/stuck on lazy rows & grids; `FocusEngine.kt` deleted | [spec](specs/ravilo/requirements/phase-R30-native-focus-traversal.md) |
| R31 | ✓ Done | **`:ravilo-player` FFmpeg decoder** — jellyfin's `media3-ffmpeg-decoder` in an Android-only, GPL-contained module + renderers factory for DTS/TrueHD/AC3 (Media3 1.8.0); source-vendor fork skipped as redundant; on-device codec test pending | [spec](specs/ravilo/requirements/phase-R31-player-engine-fork.md) |
| R32 | ✓ Done | **Unified filter workbench + hero builder + Library round-trip** — one shared condition-stack builder for Content rows, Channels & the Library; audio-track + hero-item as universal facets; Save-filter-as-Channel/Row + "Feature in Ravilo" on Movie/Series detail | [spec](specs/ravilo/requirements/phase-R32-unified-filter-workbench.md) |
| R33 | ✓ Done | **Live config push** — per-user WebSocket (`/api/tv/events`); a config write (`RaviloConfigService.save`) pushes `config_changed` to that user's connected TVs, which silently re-pull the feed/config so channel/hero-height/tile-shape/row/skin edits appear in ~1s with no reload | [spec](specs/ravilo/requirements/phase-R33-live-config-push.md) |
| R34 | ✓ Done | **TV detail layout + content size** — full-bleed viewport hero on Movie/Series detail (opens at top, no entry-scroll that strands the screen) + TV-tuned detail sizing; per-user `uiDensity` (Compact/Cozy/Comfortable) scales the grid tiles, edited in the config screen, applied live via R33 | [spec](specs/ravilo/requirements/phase-R34-detail-layout-content-size.md) |
| R35 | ✓ Done | **Home-screen TV sizing + hero framing** — Home hero carousel + content-row headers brought down to the R34 detail scale; shared `RaviloButton` chrome shrunk globally (44dp/15sp/10dp); hero backdrop crop re-framed to `center 26%` (`heroBackdropAlignment`) across all three heroes. Fixed-tuned; config-driven values (hero height, auto-advance, tile shape, `uiDensity`) untouched. Supersedes R24's home Hero numbers | [spec](specs/ravilo/requirements/phase-R35-home-screen-tv-sizing.md) |
| R36 | ✓ Done | **Channel-button editor** — channel look (Logo/Text + brand fill) configured **only** in the editor popup; Logo = a real uploaded/pickable **image asset** (`logoUrl`), Text = a label input; **brand fill** gains a custom **solid/gradient** builder beside the presets (`brandColor` = CSS color or `linear-gradient`); every editable row gets a discoverable **pencil edit** affordance. Revises R32 §C2 | [spec](specs/ravilo/requirements/phase-R36-channel-button-editor.md) |
| R37 | ✓ Done | **Brand-mark centering + asset-pack regeneration** — the jellyfish art sat small/high in its `viewBox`, so every launcher/store raster and in-app inline copy inherited an off-centre glyph. Recentre the **master** `ravilo-mark.svg` (paths unchanged, `viewBox` reframed) + regenerate the whole `assets/**` pack from it; fix the inline marks (app bar, config header, Library menu, create-flows) and flex-align the config-header mark. Follow-up to R22 | [spec](specs/ravilo/requirements/phase-R37-brand-mark-centering.md) |
| R38 | ✓ Done | **Launcher-icon optical centering** — the jellyfish reads low in the TV Apps overview (top-heavy dome + thin tentacles → geometric centre below optical centre); nudge the mark up a few % when compositing and regenerate the icon pack. Master SVG + inline marks unchanged. Follow-up to R37 | [spec](specs/ravilo/requirements/phase-R38-launcher-icon-optical-centering.md) |
| R39 | ✓ Done | **Channel logo fills the button** — an uploaded channel logo covers the whole button (cover / no padding), clipped by the rounded corners, consistent on TV + admin preview/row + mockup. Follow-up to R36 | [spec](specs/ravilo/requirements/phase-R39-channel-logo-fill.md) |
| R40 | ✓ Done | **Instant back navigation** — Back re-shows a loading skeleton because `AnimatedContent` disposes the screen + its store; retain store state/cache so re-entry is instant with silent background refresh (Home/Channel/Browse/Detail/Search) | [spec](specs/ravilo/requirements/phase-R40-instant-back-navigation.md) |
| R41 | ✓ Done | **TV subtitle tracks (always-zero bug)** — source subtitles from Jellyfin `PlaybackInfo`, fix the malformed (duplicated-id) subtitle URL, and make subtitle selection actually work (`TrackSelectionOverride` / external config); picker lists embedded + external | [spec](specs/ravilo/requirements/phase-R41-tv-subtitle-tracks.md) |
| R42 | ✓ Done | **Fix focus-animation viewport jump** — stop bring-into-view re-centring already-visible tiles, reserve scale-overflow space (`trackPadV`), keep the scale draw-only; smooth focus with no screen jump | [spec](specs/ravilo/requirements/phase-R42-focus-animation-viewport-jump.md) |
| R43 | ✓ Done | **Focus-navigation smoothness** — after R42 the jump was gone but moving focus still felt laggy: the focus shadow/border read animated values as modifier params (recomposition every frame) and the spring was soft. Run the whole focus animation in the draw phase (scale + shadow in `graphicsLayer{}`, ring in `drawWithCache`) → no per-frame recomposition, plus a snappier `StiffnessMedium` spring. Follow-up to R42 | [spec](specs/ravilo/requirements/phase-R43-focus-navigation-smoothness.md) |
| R44 | ✓ Done | **Media transport keys** — the remote's physical Play/Pause/Stop/FF/Rew/Next/Prev buttons were inert (only on-screen transport worked). `dpadFocusable` gained a `MediaKey`/`onMediaKey` handler that maps + consumes `Key.Media*` (+ `Spacebar`); PlayerScreen routes them to `togglePlay`/`skip`/`advanceNext`/restart/exit independent of focus + chrome state; Android binds a Media3 `MediaSession` to the ExoPlayer (so the OS + external controllers deliver transport keys, no double-toggle since Compose consumes the key); web wires `navigator.mediaSession` play/pause/seek/stop | [spec](specs/ravilo/requirements/phase-R44-media-transport-keys.md) |
| R45 | ✓ Done | **Entry scroll + focus restore** — screens opened scrolled to a mid-page button and never re-framed the hero, and content rows lost their `trackPadH` left inset after scrolling in and back. The detail actions row + Home hero now snap the scroll to the top via `onFocusChanged` whenever they hold focus (entry + return-up), so the full hero re-frames; the row `BringIntoViewSpec` reveals a start-clipped tile at the left inset (`leading - insetPx`) so item 0 keeps its padding — fully-visible tiles still never move (R42/R43 intact) | [spec](specs/ravilo/requirements/phase-R45-entry-scroll-focus-restore.md) |
| R46 | ✓ Done | **Human-readable track labels** — the Audio/Subtitles picker showed bare codes ("DAN") instead of Jellyfin's rich `DisplayTitle` (e.g. "Synstolkning") because the `StreamTicket` carried no audio metadata and the player labelled from container `Format.label`/language only. Added an `AudioTrack` list to the ticket sourced from Jellyfin `MediaStreams`, `RaviloPlayer.load` takes it, the Android getter prefers the server label (mapped by audio-stream order) then a humanized language (`languageName()`), and the web actual surfaces the labels. Follow-up to R41 | [spec](specs/ravilo/requirements/phase-R46-track-label-metadata.md) |
| R47 | ✓ Done | **Detail focusables: draw-only focus scale** — R42's viewport-jump fix (focusable at fixed size, scale draw-only) was only applied to Tile/ChannelCard, so navigating Movie/Series detail still jumped: `EpisodeCard`, `SeasonPicker` pills and `RaviloButton` applied `.scale(scale)` (and the button's lift) as an **ancestor** of `.dpadFocusable`, so the focused bounds grew with the spring and the scroll chased them. Moved scale + glow + lift into an inner `graphicsLayer` with the focusable on a fixed-size outer (Tile/ChannelCard pattern); `CastCircle` already scaled a descendant (left as-is). Fixes detail rails + the Home hero buttons. Follow-up to R42/R43 | [spec](specs/ravilo/requirements/phase-R47-detail-focusable-draw-only-scale.md) |
| R48 | ✓ Done | **Discover / Top 10 API** (`/api/tv/discover`) — extend per-user `RaviloConfig` with a `discover` block (enabled/source/region/ordered lists); compose Phase 57 charts + Phase 56 acquisition status; `GET /api/tv/discover`, `discover/item/...`, `POST discover/request`; server-decided `discoverAvailable` gating; live `acquisition_changed` over the R33 events socket (FR-RD1) | [spec](specs/ravilo/requirements/phase-R48-discover-api.md) |
| R49 | ✓ Done | **Ravilo TV: Top 10 tab + dedicated detail + request/status** — gated app-bar tab; ranked rows (rank numeral + country weeks-on-chart / global views + trend); rich status indicators (`requested`/`queued`/`downloading`%+`stalled`/`importing`/`available`) that update live; a **separate** DiscoverDetailScreen (no playback/seasons) with a status-driven Request/Watch-Now button (trailers out of scope for now) (FR-RD2) | [spec](specs/ravilo/requirements/phase-R49-tv-discover-screen.md) |
| R50 | ✓ Done | **Ravilo config editor: per-user Top 10 lists** — a Top 10 section on the `/ravilo` config screen (R28 chrome): master enable, source + country selects, drag-reorder/toggle list selection from `GET /api/discover/lists`, Radarr-gating link to Settings → Download tools; writes `RaviloConfig.discover`, live-pushed via R33 (FR-RD3) | [spec](specs/ravilo/requirements/phase-R50-config-editor-discover.md) |
| R51 | ✓ Done | **Global-default config + optional per-user override** — replace the per-user-only default (R16): a single **Global layout** every user gets, plus an optional **custom layout** that **fully replaces** it for one user (binary, no piecemeal inheritance). Config screen gains a Global/user **scope switcher** with a create/remove-override flow; store keyed global + full per-user records (FR-RV-G1) | [spec](specs/ravilo/requirements/phase-R51-global-config-user-override.md) |
| R52 | ✓ Done | **Per-page hero carousels + hero naming** — Home keeps its hero; **each channel/collection page** can have its **own optional hero** (opt-in, one per page) built with the shared R32 hero-item builder (items + height + auto-advance); Ravilo TV renders a channel hero. UI says **hero carousel / hero item** everywhere ("featured" removed; facet = In hero / Not in hero). **Hero height clamp now 40–100%** (was R27's 30–100). `HeroConfig` becomes per-placement (FR-RV-H1) | [spec](specs/ravilo/requirements/phase-R52-per-page-hero-carousels.md) |
| R53 | ✓ Done | **Channel editor as a dedicated page + channel-button padding** — promote the Channel/Collection editor from a popup (R36) to a full **in-app page** (sidebar intact, "‹ Back to layout"), so the hero-item picker layers as a normal popup; rows + Library filters stay popups. Channel button gains per-display **padding** (T/R/B/L 0–40px, separate logo/text), live-previewed, collapsed behind an icon that auto-opens when set (FR-RV-C1) | [spec](specs/ravilo/requirements/phase-R53-channel-editor-page-padding.md) |
| R54 | ✓ Done | **Rows config simplification: workbench-only custom rows** — system rows (Continue, Newly Added) fixed at top with enable/disable only; all other rows are workbench-filter rows (no kind picker); `+ Add row` opens workbench directly; default config trimmed to 3 system rows (genre presets removed); legacy GENRE rows migrate to CUSTOM kind on first edit | [spec](specs/ravilo/requirements/phase-R54-rows-config-simplification.md) |
| R55 | ✓ Done | **Subtitle rendering surface + embedded text subs** — subtitles never *drew* (bare `TextureView`, no `SubtitleView`/`onCues`) and only external sidecar subs were offered; wire a Media3 `SubtitleView` fed by `onCues` and drop the `isExternal` filter so embedded SRT/ASS/SSA sideload as Jellyfin VTT. Android-only render fix; web already uses `<track>`. Follow-up to R41/R46 (FR-RV-S1) | [spec](specs/ravilo/requirements/phase-R55-subtitle-rendering.md) |
| R56 | ✓ Done | **Image-based subtitle parity (PGS/VobSub/DVDSub)** — render image subs like the official Jellyfin client: native in-container where Media3 can, server **burn-in transcode** where it can't, via real Jellyfin `PlaybackInfo`/`DeviceProfile` negotiation (replaces the R14 direct-play `TODO`); subtitle selection re-requests a transcoded stream when burn-in is needed. Follow-up to R55 (FR-RV-S2) | [spec](specs/ravilo/requirements/phase-R56-image-subtitles.md) |
| R57 | ✓ Done | **Config: Pair-a-TV action + sticky action navbar** — a **Pair a TV** button in the config top bar (beside Save, not under the scope switcher) opens a popup to pick the Jellyfin user + enter the TV's 6-char code (admin side of R03/R15 pairing; device-scoped, orthogonal to R51). The top action bar (Save · Pair · Open Ravilo) is **sticky** so Save stays reachable on the long page (FR-RV-P1) | [spec](specs/ravilo/requirements/phase-R57-pair-tv-sticky-navbar.md) |
| R58 | ✓ Done | **Single global hero height + auto-advance** — drop the **per-channel** hero height **and** auto-advance controls (R52); the **Home hero** settings (height 40–100%, auto-advance) are the one source applied to **every** hero, Home and channel pages alike. Channel Page-hero keeps only its items; `heroHeightPct` + `autoAdvanceSeconds` leave the per-channel `HeroConfig` (FR-RV-H2) | [spec](specs/ravilo/requirements/phase-R58-single-global-hero-height.md) |
| R59 | ✓ Done | **Per-channel content rows** — channel editor gains a **Same as Home / Custom** switch (default inherit): a channel can override the Home content rows with its **own ordered set**, built with the shared row workbench (popup over the channel page). System rows still appear unless removed; `ChannelConfig` gains `rows {mode, items}` reusing `RowConfig` (FR-RV-R1) | [spec](specs/ravilo/requirements/phase-R59-per-channel-content-rows.md) |
| R60 | ✓ Done | **Android phone (mobile) target** — ship Ravilo on a Pixel 9 phone (portrait, touch) from the **same shared Compose codebase**; the only TV lock-in is the thin `:ravilo-android` entry (manifest + Activity), so a new thin `:ravilo-phone` module (or `tv`/`phone` flavor) is all that's needed — no shared-UI fork. Design = **portrait/touch reflow** of the existing system (top tabs, swipe hero, scroll rows, slide-in detail, ranked Top 10, native keyboard); no bottom-nav for v1; responsive `Dimens` over new mockups. TV stays primary (FR-RV-M1) | [spec](specs/ravilo/requirements/phase-R60-android-phone-target.md) |
| R61 | ✓ Done | **Home rows: reduce system defaults to Continue + Newly Added** — replace the three system rows (Continue, Movies — Newly Added, Series — Newly Added) with two (Continue + Newly Added, all media). `DEFAULT_ROWS` (backend) + `SYSTEM_ROW_DEFAULTS` (editor) trimmed; existing configs gain ✕ delete buttons on the formerly-system split rows via `normalizedRows()` auto-insert of `newly-all` (FR-RV-RD1) | [spec](specs/ravilo/requirements/phase-R61-home-rows-reduce-defaults.md) |
| R62 | ✓ Done | **Brand recolor: Jellyfin palette + "D · Lit mark" asset regen** — recolored launcher icons (adaptive bg/fg/monochrome + all density `ic_launcher`/`ic_launcher_round`), splash logo and leanback banner copied from the regenerated design asset pack into **both** `ravilo-android` and `ravilo-phone` res; splash `windowSplashScreenBackground` → `#000B25` in both `themes.xml`. Mark shape unchanged; in-app Aurora accent untouched; both modules `assembleDebug` clean (FR-RV-B1) | [spec](specs/ravilo/requirements/phase-R62-brand-recolor-jellyfin-palette.md) |
| R63 | Planned | **TV performance: right-sized images + tuned image cache + viewport prefetch** — the bottleneck is the image pipeline, not focus/scroll. Backend serves **full-res** images (no `&fillHeight`/`&maxWidth`/`&quality` on any image URL though `PlaybackService` already does it for video); `RemoteImage`/Coil is unconfigured (no placeholder/crossfade, no mem/disk cache, **no SingletonImageLoader on web**); hero re-decodes a full-res backdrop every 7s. Fix: size image URLs per kind at the backend, configure Coil on every target, add placeholder+crossfade (reuse the Tile hash-gradient), right-size the hero. `LazyLayoutCacheWindow` prefetch deferred (needs CMP 1.9). Focus/Tile draw-phase work (R42/43/47) untouched (FR-RV-PF1) | [spec](specs/ravilo/requirements/phase-R63-tv-performance-image-pipeline.md) |
| R64 | Planned | **Rename "Channels & Collections" → "Channels"** — label-only standardization across the TV app + admin + design mockups. There is no `Collection` model (display-only), so it's a safe string rename, no migration: `section.channels` i18n (en/da/fo), the admin section header (`RaviloConfig.kt:896`), and the `design/` mockups + `ravilo-i18n.js`. Model identifiers (`ChannelConfig`, `channels`, `data-kind="channel"`, ids/CSS) stay unchanged (FR-RV-RN1) | [spec](specs/ravilo/requirements/phase-R64-rename-channels-only.md) |
| R65 | Planned | **Navigate-up: reveal the row title + clear the app bar** — D-pad up to the top content row scrolls the focused tile to `y=0`, hiding the row title ("Action"/"Comedy") and colliding with the 60dp app-bar overlay, because `rememberEdgeBringIntoViewSpec` reveals top-clipped targets at offset 0 with **no top inset** (Home can't use top `contentPadding` — the hero sits under the bar). Fix: add a `topInsetDp` (~94dp = bar + title + headroom) to the vertical spec, tokenize `appBarHeight` in `Dimens`, apply to Home + Channel screens (Discover already compensates via contentPadding) (FR-RV-UP1) | [spec](specs/ravilo/requirements/phase-R65-navigate-up-focus-inset.md) |
| R66 | Planned | **Shared channel-button spec + accurate admin preview** — the button is defined twice (Compose `ChannelCard.kt` vs three rough admin approximations) and the admin preview disagrees on size/radius/logo-fit/watermark/gradient-direction/padding. `:shared` is on both consumers' classpath, so a pure-data `ChannelButtonSpec` constants object becomes the single source: TV reads it as `.dp`/`.sp`, admin reads it as CSS px (same aspect ratio + radius ratio, `object-fit:contain` not cover, forced-diagonal gradient, watermark+sheen, applied padding). Rendering can't be shared (Compose≠DOM) but the definition can (FR-RV-CB1) | [spec](specs/ravilo/requirements/phase-R66-shared-channel-button-spec-preview.md) |
| R67 | Planned | **Stop focus jumping to "Home" + stop needless genre reloads** — two bugs: (1) entering Movies/Series snaps focus to Home because `BrowseScreen` requests `navBarFR` which `AppBar` hardwires to nav item 0, ignoring the computed `activeNav`; (2) picking a genre runs `filterByGenre → load()` which goes through `BrowseState.Loading` (un-mounts the focused chip → focus falls back to Home) and refetches `getFacets()` (genres never change per kind). Plus a latent bug: `browse()` has no `genre` param so the filter is a server-side no-op. Fix: entry-focus the active tab, filter genres in place (no Loading, cached facets), and actually send the genre (FR-RV-NF1) | [spec](specs/ravilo/requirements/phase-R67-nav-focus-and-genre-reload.md) |
| R68 | Planned | **Strip ASS/SSA override tags from subtitles** — selected subs show raw `{\an1}`/`{\pos}`/`\N`/`\h` text. Cause: text subs come as Jellyfin VTT (`Stream.vtt`) and Jellyfin's ASS→VTT extractor leaves `{\…}` blocks as literal cue text; Ravilo renders cues verbatim (Android `onCues` passthrough, web native `<track>`), with **zero** cue cleaning (the R17 JASSUB path is unreachable — backend never emits a `.ass` URL). Fix: a shared `cleanCueText` (regex `\{\\[^}]*\}` strip + `\N`/`\h` normalize) applied in Android `onCues` (`cue.buildUpon().setText`) and the web cue path (FR-RV-S3) | [spec](specs/ravilo/requirements/phase-R68-subtitle-ass-tag-stripping.md) |
| R69 | Planned | **Player chrome: more compact + modern play/pause** — the woken transport is too big (all sizes are hardcoded literals in `PlayerScreen.kt`): control `Column` `vertical=36dp`, seek `Canvas height(48dp)` for a 12dp bar, button heights 60/54·46·44, `420dp` bottom scrim. And the "yellow" play/pause = a white disc that's the **default-focused** control wearing NOIR's gold focus ring/glow, plus `▶`/`⏸` Unicode glyphs rendering as colored emoji. Fix: shrink the literals (optionally tokenize), use vector icons, restyle play/pause neutral/accent-consistent across all skins (FR-RV-PC1) | [spec](specs/ravilo/requirements/phase-R69-player-chrome-compact-and-playpause.md) |
| R70 | Planned | **Real hero artwork in the config schematic preview** — the R28 schematic (`RaviloConfig.kt renderPreview`) draws the hero as a raw `itemId` over a flat gradient, even though `HeroConfig` already stores `displayBackdrop` (TMDB path) + `displayTitle` and the other **three** hero preview surfaces already render the real backdrop. Fix: point the schematic's hero block at `image.tmdb.org/t/p/w300${displayBackdrop}` with the title overlay (fallback to the per-item gradient). No backend change (FR-RV-CP1) | [spec](specs/ravilo/requirements/phase-R70-config-hero-schematic-real-preview.md) |
| R71 | Planned | **"Merge newly added" controls ONE vs TWO Newly-Added rows** — the switch is correctly wired to `mergeNewlyAdded` but behaves like enable/disable. Root cause: R61 made the default a single `newly-all` (`mediaKind=null`) row, but `HomeFeedService.buildRows` merge logic was written to collapse two stored typed rows — so with one row there's nothing to split and both flag states yield one row (flag only greys the editor row). Fix (recommended): keep the single system row but split it at feed-build — `false` → emit "Newly Added Movies" + "Newly Added Series", `true` → one combined; update the editor preview/greying. Not a DB error; no migration (FR-RV-MA1) | [spec](specs/ravilo/requirements/phase-R71-merge-newly-added-one-vs-two-rows.md) |
| R72 | Planned | **Detail-screen scroll breathing room + Play/Resume snaps to top** — on Movie/Series detail, focusables scroll flush to the screen edge (the detail screens install the **bare** parameterless `EdgeBringIntoViewSpec` — no peek/inset, unlike Home's `peekDp=80`), and the R45 Play/Resume `animateScrollTo(0)` loses a `MutatorMutex` race with bring-into-view (returning up reveals Play flush at y=0, not the full hero). Fix: give the detail spec a small top inset + bottom peek for all focusables (reuse R65's `topInsetDp`), and make the actions-row snap authoritative (UserInput mutate priority) so focusing Play reframes the full backdrop (FR-RV-DS1) | [spec](specs/ravilo/requirements/phase-R72-detail-scroll-padding-and-play-snap-top.md) |
| R73 | Planned | **Per-channel item-match count in the Channels list** — `/#/ravilo` Channels shows only a *condition-count* badge (`"2 condition(s) · ALL"`), not how many items match. The R32 workbench live count already does this (`Workbench.kt wbRefreshPreview` → `MediaApi.list(...).total`, where `MediaStore.list` sets `total` = full filtered count). Fix: extract a reusable `countMatching` helper and render a per-channel async count badge in `renderChannels` (using `wbCondsFrom`/`legacyToConds` + the channel's match). No backend change; ANY counts show `≈` until R74 makes them exact (FR-RV-CC2) | [spec](specs/ravilo/requirements/phase-R73-channel-match-counts.md) |
| R74 | Planned | **"Match ANY" must OR conditions (count + Library)** — premise-corrected: ANY already ORs on the **TV feed** (`ConditionEvaluator.matches` used by `HomeFeedService` channels/CUSTOM rows), but it's dropped in the workbench **live count** (`wbRefreshPreview` flattens to per-facet `MediaApi.list`, never sends `wbMatch`) and the **Library** (hardcoded `initialMatch="ALL"`, `onApply` drops it) because `/api/media`→`MediaStore.list` has no `match`/`conditions` param and AND's facets. Fix: route the count + Library through the same `ConditionEvaluator` — add `conditions`+`match` to `MediaStore.list`/`/api/media`/`MediaApi.list`, send them from the workbench + Library; bonus makes `is_none_of`/`not_contains` exact (FR-RV-MM1) | [spec](specs/ravilo/requirements/phase-R74-workbench-match-any-or.md) |
