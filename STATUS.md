# Status — jellystructure + Ravilo

**This is the single source of truth for phase status.** Every phase across both products lives in
one of the two tables below, with exactly one status column. A spec's file location or body no
longer encodes status — only this file does.

- **Status values:** `✓ Done` = implemented in code · `⚠ Partial` = shipped but a real piece of the
  spec is genuinely unbuilt (the Focus cell's **⚠ GAP** says what) · `Planned` = spec written, not
  built. Other nuance (e.g. "on-device test pending") rides in the Focus cell.
- **Spec content** lives flat in [`specs/requirements/`](specs/requirements/) (admin) and
  [`specs/ravilo/requirements/`](specs/ravilo/requirements/) (Ravilo). Architecture is in
  [`specs/constitution.md`](specs/constitution.md) + [`specs/plan.md`](specs/plan.md) (admin) and
  the matching files under [`specs/ravilo/`](specs/ravilo/).
- **The "updated designs" sync overwrites everything under `specs/` AND `design/` AND `CLAUDE.md`,
  but never touches this `STATUS.md` or `scripts/`.** (CLAUDE.md is design-project-owned, so it gets
  re-reverted on each sync until the design export is updated — keep status here, not there.) After
  every sync run `scripts/check-phases.sh` — it flags any spec file with no row here (newly added →
  add a row) and any row whose spec file is gone (deleted by the sync → investigate). That is the
  "never forget a phase" guarantee.
- **Research reports live in [`specs/research-reports/`](specs/research-reports/)** (dated deep-dives
  behind a decision/future phase; see its README). ⚠ Because they sit under `specs/`, the "updated
  designs" sync can overwrite or delete them — until the design export is taught to leave
  `specs/research-reports/` alone, treat these as **code-owned**: keep the source of truth in this
  repo and re-add anything the sync strips (same recurring issue as the cast / Settings-tab CSS).

## Current focus

**Admin phases 0–137 ✓ Done** (100–104 landed 2026-06-29; 105–118 landed 2026-07-02; 119–129 landed 2026-07-03; **135–137 landed 2026-07-04** — bounded worker pools for every non-scan_files pipeline step + a step-aware WS protocol (`PipelinePlan`/`StepStarted`/`StepProgress`/`StepFinished`) + structured trigger/scope/type run descriptors, with the Activity page rendering step chips, an active-phase highlight, and a run-descriptor badge, one commit per phase — see individual rows for detail on each). Ravilo **R01–R171 ✓ Done — every planned admin + Ravilo phase is now built** (R142–R149 landed 2026-06-29/30; R152–R159 landed 2026-07-02, one commit per phase); **R156, R158, R165** re-verified/landed 2026-07-03 (R156/R158 were already implemented 2026-07-02 but this table lagged; R165 landed fresh). **105, 132, 133** landed 2026-07-03 (105's table row had lagged despite being in the "105–118" range above); **R150** was already implemented 2026-06-30 (commit `0f80c36`, not tagged in its message) — table reconciled 2026-07-03; **R151, 130, R163, 131, R164, R162, R161, R160** implemented 2026-07-03 (R162 fixes a confirmed-live orphaning bug, see its row; R160 is the last of the 16 planned phases from this session). **New 2026-07-03 (evening): Phase 134 ✓ Done** — a second real FD incident (user-visible on a TV) root-caused live to a systemic kotlinx-io file-read leak, fixed, plus a mid-implementation scale-up to a genuine 100-scan-worker + 50-TV target (`ProcessGate`/`OutboundHttp` gate sizes, not just config ceilings); see its row. **2026-07-04: R166–R169 ✓ Done** — the four dev-authored Ravilo fixes (episode-level Upcoming availability + localized titles; not-held-item art + rich detail; Radarr minimum-availability for "Missing"; web-player video-behind-chrome, implemented via the FR-R169-3 DOM-overlay fallback — see that spec's implementation note) all implemented and committed individually, alongside the STATUS/specs recovery from a design-sync clobber (see the note above) and the four Seerr specs' dev-review enrichment. **Phase 135 ✓ Done** the same session (see its row). **Phase 136 ✓ Done** the same session too — the Seerr connection + full chart-subsystem retirement (see its row); `/tv/discover*` stubbed to unavailable in the interim. **Phase 137 ✓ Done** likewise — the per-user Request/Seerr-feeds builder replacing per-user Top 10 (see its row). **R170 ✓ Done** the same session — the TV AppBar profile-hub nav + Discover merge (see its row). **R171 ✓ Done** likewise, closing out the Seerr cluster and this session's full backlog — the TV Request tab is now genuinely Seerr-backed end to end (see its row for the scope notes: no persisted acquisition store, status derived live per fetch). Next unassigned numbers: 138/R172 (see the note above) — **zero `Planned` rows remain**. **2026-07-03 session:** 119–127 spec'd from code-traced root causes (six product/bug fixes) plus a real local `qodana.recommended` run (125 enables it locally, Community linter no cloud; 126 suppresses/mechanically-fixes 349 findings; 127 triages the 121 judgment-heavy ones, keeping 7+3+3 with documented reasons, and commits `qodana-baseline.sarif.json` so future runs flag only regressions) — all implemented and committed the same day. **2026-07-02 implementation session complete:** Phase 106–118 + R149–R159 were implemented and committed individually.

> **2026-07-13 (implementation): 150 + R182 ✓ Done, including cross-episode fingerprinting.** Skip
> Intro / Skip Credits, built end to end — Phase 150's layered segment detection (chapter-title match →
> ffmpeg credits heuristic → TMDB stinger merge → cross-episode Chromaprint fingerprinting once `fpcalc`
> was installed on the backend host) as a new opt-in `detect_segments` pipeline step, the admin segment
> scrubber, two new Triage rows, and the DTO trip to the player; R182's Skip Intro pill + stinger-aware
> credits card, configured jellystructure-side via new `skipIntro`/`skipCredits`/`skipSecs` behaviour
> fields (mirroring the existing `autoplayNext`). **Next numbers: 151 / R183.**

> **2026-07-12 (design sync): R180 spec'd + 149/R179 rows backfilled.** The in-player **Audio & Subtitles**
> picker redesign — flag-forward flat list, jargon-free badges (no codec/delivery/latency cues), keep every
> track, selected-track flag shown in each tab, D-pad scroll-follow — is authored as **R180** (`Planned`;
> design-complete in `design/ravilo/`, not yet coded). Also added the previously-missing STATUS rows for
> **149** (multi-episode files) and **R179** (multi-episode combined card) — both design-complete +
> dev-reviewed 2026-07-12, awaiting code. **Next numbers: 150 / R181.**

> **2026-07-10 (evening): R178 spec'd** — a live-reported player UX bug (D-pad Select re-triggering a
> hidden control's action, e.g. reopening Audio & Subs, instead of toggling play/pause, because the
> hand-rolled `focus: PlFocus` state is never reset when the chrome auto-hides). Spec only this round
> (`Planned`) — not yet implemented. **Next numbers: 146 / R179.**

> **🧹 2026-07-10 spec cleanup, round 2.** 69 more phase spec files **deleted** on the same "keep
> newest 15 per track" rule — **admin 90–130** (41 files) and **Ravilo R127/R130/R131/R133–R143/
> R148–R161** (28 files, non-contiguous — numbers already skipped by round 1). Only **admin 131–145**
> and **Ravilo R162–R176** keep their spec files now; all are ✓ Done. Their STATUS.md rows were also
> **trimmed** to a 1–2 sentence summary (from long implementation-narrative paragraphs) — full detail
> for these 69 rows lives in `git log -- STATUS.md` alongside the deleted specs themselves
> (`git log -- specs/requirements/phase-NN-*.md` / `specs/ravilo/requirements/phase-RNN-*.md`); rows
> for the still-live 131–145/R162–176 specs are untouched. One dangling in-spec reference (R175 →
> R161) was reworded to drop the now-dead file path. **Numbering policy unchanged, updated for this
> round: next admin spec is 146, next Ravilo is R177.**

> **🧹 2026-06-30 spec cleanup (round 1).** The 158 oldest phase spec files were **deleted** to declutter
> the active tree — **admin 7–89** and **Ravilo R01–R87**. The deleted specs' **content lives in git
> history** (`git log -- specs/requirements/phase-NN-*.md`). Their status rows are retained below with
> `git history` in the Spec column (so this table stays a complete overview). **Numbering policy:** spec
> numbers are assigned monotonically and **never reused**, even for a deleted spec — at the time of this
> note the next admin spec was 140, the next Ravilo R175 (110–118 + R155–R159 assigned 2026-07-02;
> 119–129 assigned 2026-07-03; 130–138 + R160–R165 assigned 2026-07-03/04; 139 + R166–R172 assigned
> 2026-07-04) — see the round-2 note above for the current numbers.

> **2026-07-03 design-side sync.** The design project had drafted three Ravilo specs against stale
> numbers (R155/R156/R157) that the dev team had since reassigned to different, already-shipped work
> (remote-commands / web-overlay-warning / web-player-overhaul). Renumbered to the next free slots and
> re-added here: **R160** (Upcoming calendar), **R161** (In-app Settings: theme/language/unpair),
> **R162** (Behaviour preferences as a field-level per-user overlay). All three are **design-only**
> (mockups built, nothing implemented) — flagged `Planned` and not yet reviewed by the dev team. **Next
> Ravilo number is now R163.**
>
> **2026-07-03 (later)** — also claimed design-side for the **TMDB trailer** feature: **Phase 130** (ingest
> the TMDB videos section + trailer card on the admin detail page + TV DTO field) and **R163** (Ravilo
> Play-Trailer button + fullscreen YouTube/Vimeo embed player). Both **design-only**, `Planned`. Also
> **Phase 131** (IMDb ratings via imdbapi.dev — ingest/store/sync + admin display + DTO) and **R164**
> (Ravilo IMDb rating chip on the detail page), likewise design-only. *(At the time this reserved 130–131
> + R160–R164; 132–133 + R165 were then added the same day — see the note below. Current next numbers:
> 134 / R166.)*
>
> **✓ 2026-07-03 (dev side) — all seven reviewed.** Each spec got a dated **"Dev-review addenda"**
> section reconciling it with backend reality (see the per-row notes below). Headlines: R160's stale
> `R155` title fixed; R161's device-local/localStorage model replaced by the **existing server-owned
> viewer-settings lane** (much of R161 already ships — `SettingsScreen.kt`, `viewerSkinOverride`,
> `POST /api/tv/unpair`); R162 confirmed as a **live shipped bug fix** (`applyViewerSettings` forks the
> whole layout on first viewer write) with a writer-tagged overlay replacing the device-override
> exposure; 130's nonexistent "thumbnail proxy" replaced by ingest-time oEmbed; 131's `imdbId` already
> exists and its new client is bound to the Phase 129 shared pool. Implementation order note: **R162
> before R161**. The sync also re-stripped `design/app/wf.css` (Phases 98/106/107/108/109/124 + `.cv-select`,
> 137 lines) and `design/app/ravilo-builders.css` (R154, 19 lines) — both restored, again; and deleted the
> long-stale `specs/tasks.md` (kept deleted; `specs/README.md` reference dropped).
>
> **2026-07-03 (dev side, later) — three new dev-authored specs from thorough code investigation** (not
> design-tool drafts): **Phase 132** (remove RapidAPI / streaming-availability Discover provider end to
> end — keep Netflix + JustWatch), **Phase 133** (keep a manually-chosen poster/backdrop across TMDB
> sync — a real reported bug, root-caused to `posterPath` being reset with no manual-marker), and
> **R165** (remove the Home hero scroll-parallax). All `Planned`, investigated, ready to implement.
> **Next admin number is now 134; next Ravilo is R166.**

> **✓ 2026-06-29 phase-number collision — RESOLVED.** The "updated designs" pull added canonical specs for
> **R142 (Watched state everywhere)** and **R143 (Per-channel system rows)** that clashed with the numbers an
> earlier commit had used. Resolved by implementing the canonical features and renumbering:
> - **R142 = Watched state everywhere** — now built to spec (tile ✓/progress badges everywhere + mark-played
>   write-through on movie + series detail + player ≥90% finish). Commit `48e8f2d`.
> - **R143 = Per-channel system rows** — extended the interim per-channel Newly Added work up to the full
>   canonical spec (Continue + Newly Added each with show / All-titles·This-channel scope / merge). Commit `a2de8eb`.
> - **R144 = Multi-provider Discover + Search navbar padding fix** — the feature that earlier commit `a111e0f`
>   mislabeled "R142". Renumbered here (git history keeps the old label; this table is canonical).

> **2026-06-26 correction:** the previous "15 phases Planned (R63–R74 + admin 82–85)" line was **stale** — all 15 are implemented in code and the tables mark them ✓ Done (verified: R64 `section.channels` i18n, R65 `topInsetDp`, R68 `cleanCueText`, R73 `countMatching`, 82 `PRESET_COLORS`, 83 `isQueryActive`, 84 search debounce, 85 `data-tab` machinery). The design-project's CLAUDE.md still lists them as "Planned" because **design-side status lags code** (the recurring sync issue). Phases 82 & 85 needed only their `wf.css` rules, which the 2026-06-26 `df5b6ca` "updated designs" sync stripped again (along with 76/87/R75) and which are **now restored** in `design/app/wf.css`.

> **2026-06-26 sync (`df5b6ca`) housekeeping:** that sync **renamed all 12 R63–R74 spec files** (e.g. `phase-R63-tv-performance-image-pipeline.md` → `phase-R63-tv-image-pipeline-performance.md`); the 12 spec links above have been **repointed** to the new filenames (status unchanged — Done). It also added the new **phase 91** spec + `settings.html` mockup. Pre-existing **dead rows** (spec files for R76 + admin 82–86, stripped by the earlier `35e2cdc` sync) have been **restored from git history** — `check-phases.sh` is clean (168 rows).
- **Backend performance (88–90 + R86):** ALL DONE 2026-06-26. Root cause of slow Library/search/sofa
  was every read re-decoding the whole 18.7 MB / 307-item library. **88** decoded-library cache
  (write-invalidated `allItemsCache`, `libraryVersion` counter). **89** strip cast/crew/tracks/
  titlesByLang from grid responses (~61KB → ~1KB/item mean); memoize `trackFacets`/`metaFacets`/
  `nfoCoveredCount` against `libraryVersion` (drops 307 `stat()` calls per `/stats` load).
  **90** `synchronous=NORMAL` + `maxReaderConnections=4`; `HttpTimeout` on all 8 Curl clients;
  `ArtworkDownloader.Semaphore(8)` (FD ceiling). **R86** 5-min home-feed cache (atomic, no row-pop);
  O(1) token-validation cache + debounced `updateLastSeen`; `ItemFacets` precomputed per item;
  `resolveByJellyfinId()` O(1) for detail. Report:
  `specs/research-reports/backend-performance-investigation.md`.

The 2026-06-25 done-ness audit found 7 falsely-Done phases and all 7 are now genuinely implemented
(70 brand mark, 72 TrackEditor language equivalence, 74 metadata write-through, 76 presence-matrix
toggle, R17 lazy hls.js/JASSUB, R18 Kids flag, R56 PlaybackInfo negotiation). Phases 41/42/46 stay
✓ Done — their staged-changes/command-preview UX was deliberately superseded by Phase 71.

> **Recurring sync regression:** each "updated designs" sync strips the Phase 75/76/79 cast CSS from
> `design/app/wf.css` and reverts `CLAUDE.md`. Both are restored in the repo but **must be fixed in
> the design-project source** to stop the loop (the cast tab renders unstyled without the CSS).

Non-blocking follow-ups (not bugs): Phase 78 scan-time people-cache pre-warm (the `Semaphore(8)`
already hard-caps the crash); R60 portrait polish via responsive `RaviloDimens` (the phone app
currently reflows with TV-tuned sizing, on-device test is the user's). Older open thread: the
`checkPortFree` TODO in `Main.kt`.

> **2026-07-03 (dev side, evening) — six new dev-authored specs from thorough code investigation**
> (four Ravilo Upcoming/player fixes + one admin observability phase; each spec was root-caused against
> the live code with file:line anchors and a parallel-agent investigation, not a design-tool draft):
> **R166** (Upcoming: episode-level "Already available" + localized library titles), **R167** (Upcoming:
> openable metadata detail + poster art for not-held items — picks up R160 addendum #3, client-loads-CDN
> like Discover so no image-proxy/FD surface), **R168** (Upcoming: honor Radarr "Minimum Availability"
> for the "Missing" section), **R169** (Ravilo web player: keep the video visible while the controls are
> shown — z-index-swap-against-opaque-canvas root cause; R157's `opaque=false` premise is outdated), and
> **Phase 135** (Pipeline observability: per-step progress, real worker counts & run descriptors — only
> `scan_files` is observable today, the later steps go dark). All six **Planned**, nothing implemented.
> **Next numbers: 136 / R170.**
>
> **2026-07-04 — design sync clobbered STATUS + specs; recovered.** The "updated designs" sync
> (`885f0b3`) this time **overwrote `STATUS.md` with the design project's stale mirror** (it is documented
> to never touch this file): it reverted ~14 phases (105/113/130/131 + R150/R151/R156/R158/R160–R165) from
> ✓ Done back to `Planned`, **deleted the rows + spec files** for the still-unbuilt dev-authored specs
> (**135, R166–R169**), **re-deleted** the trimmed-but-Done spec files (132–134, R165), and **re-stripped**
> the Dev-review addenda from 130/131/R160–R163 (plus the `tasks.md` README reference the dev team had
> dropped). All of that was **restored from `885f0b3^` (= ebaca77)**. The sync's **legitimate** additions
> were kept: the `design/**` mockup updates and **four genuinely-new design-side Seerr specs — 136, 137,
> R170, R171** (rows added below). **Guard:** the design export must be taught to leave `STATUS.md` alone
> (same class of bug as the recurring cast-CSS / research-report strips). **Next numbers: 138 / R172.**
>
> **2026-07-04 — Seerr pivot (four new design specs, `Planned`, not yet dev-reviewed/built).** A coherent
> cluster that **retires the third-party chart Discover/Top-10 stack** (RapidAPI / movieofthenight + Netflix·
> Tudum / JustWatch; supersedes Phases 57 & 107 and the shipped R48–R50 / R154) in favour of **Jellyseerr /
> Overseerr**: **Phase 136** (admin Seerr connection + rip out the chart subsystem), **Phase 137** (per-user
> Ravilo-config **Request** feeds builder, replacing per-user Top 10), **R170** (TV nav declutter → profile-hub
> avatar menu + unified **Discover** = Coming Soon / Request), **R171** (Seerr-powered **Request** tab: feeds ·
> request lifecycle · Seerr-scoped search). **Dev-reviewed the same day** — all four specs gained code-anchored
> **backend/app design addenda** (Seerr API surface + locked status enums; `SeerrConfig`/`SeerrClient`/test-connection
> mirroring `ArrConfig`; the exact chart-subsystem removal surface; the per-user `SeerrFeed` model replacing
> `DiscoverConfig.lists`; the Compose nav/avatar-menu/Discover-segment rework; and the **Seerr→`AcquisitionStatus`
> mapping** that lets R171 reuse the existing request lifecycle re-sourced from Seerr `mediaInfo`). Key decision
> flagged for the dev team: requests route **through Seerr** (Seerr owns approval), retiring the direct-to-*arr
> request path for TV while Radarr/Sonarr stay for R160 Upcoming. Still `Planned` (nothing built).

> **2026-07-05 — Phase 140 Workbench query blocks: ✓ Done (dev-reviewed, implemented, tested same day).**
> The shared R32 filter Workbench (Library filter · Channels · Home rows · per-channel rows) moved from
> a flat ALL/ANY condition stack to **blocks**: a recursive `QueryNode` tree (`ConditionGroup(join, not,
> children)` of conditions/groups, sealed union with a `kind` JSON discriminator, `Condition` retrofitted
> as its leaf) with per-block **NOT**, nestable **sub-blocks** (depth ≤ 3, enforced both in the editor and
> server-side in `RaviloConfigService.validate()`), clickable join pills (root + per-block), and per-block
> live count badges via one `batch-count` call. A per-channel row's editor shows the owning channel's
> query as a **locked read-only first block** — replacing R127's "Scoped to channel filter" banner and
> fixing a real bug along the way (`wbConds + wbBaseConds` concatenation only composed correctly under
> ALL; the tree's `channel AND row` composition is correct under ANY too). `ConditionEvaluator` is now
> recursive (one evaluator — the flat `(match, conditions)` overload is a thin wrapper that migrates then
> recurses) and fixes a second bug: an empty-values condition used to evaluate `false` and veto its AND
> block; it's now skipped (neutral), matching "a fresh block never zeroes the results." Legacy flat
> filters migrate losslessly on read (ALL → one OR-block per condition; ANY → single OR-block, verified
> in tests to evaluate identically to the old flat evaluator, not just structurally). New **additive**
> `query` field on `ChannelConfig`/`RowConfig` (installed Ravilo TV APKs skip it via `ignoreUnknownKeys` —
> the TV never evaluates filters itself). `/api/media` gains `query=`; `batch-count`/`facets` request
> bodies gain the tree; `conditions=`/`match=` still accepted everywhere and migrated. The R59 catch-all/
> coverage-gap handoff now composes the channel's blocks verbatim **AND** a `content_row is_none_of
> <rows>` block (was: catch-all rows had `conditions = emptyList()` — matched the whole library, not the
> channel) and hands the whole gap query to the Library via `query=` (replacing the old lossy per-facet-
> params + `coverage=` pair). Library's existing simple per-facet quick-filters (studios/genres/tags/etc)
> are unchanged and stay the fast path; opening the full workbench adds a `libQuery` tree on top, shown as
> one more active-filter chip. Mobile pass on the workbench chrome (≤760px bottom-sheet modal, matches
> panel stacked below the query, coarse-pointer tap targets — ported from the design's own `@media` rules
> into the workbench's existing lazy-injected stylesheet, not `app.css`; no floating value-picker popovers
> exist in this implementation to clamp). **First native unit tests in the repo** (`src/linuxX64Test`,
> 14 tests: join mixing, NOT, neutral-empty incl. NOT-on-empty, depth-3 nesting, both legacy migrations
> verified against the old evaluator's output, empty-values-skipped, `content_row` embedded-row migration,
> `pruned()`) — needed a `build.gradle.kts` linker fix (`binaries.getTest("DEBUG")` didn't inherit
> `executable{}`'s sqlite3 linkerOpts). No Ravilo TV app change (server-pushed rows, as designed).
> Verified via real `compileKotlinLinuxX64` + admin `compileKotlinWasmJs` + `:ravilo-ui`/`:ravilo-web`
> + `linuxX64Test` (all 14 passing).
>
> **2026-07-05 — R173 ✓ Done, same-day bug fix.** A viewer reported an HDR10+ movie played very dark on
> Ravilo but fine in Jellyfin's own client; root-caused live against the real Jellyfin server (no
> `CodecProfiles`/`VideoRangeType` constraint in the `DeviceProfile` sent to `PlaybackInfo`, so HDR
> content always direct-played regardless of what the device could show) and fixed same session — see
> its row for the full investigation + verification detail.
>
> **2026-07-07 — 3 admin + 1 Ravilo spec added (`Planned`, design-only, not built).** Phase **141**
> (Ravilo proxied username/password login — retire code pairing), **142** (restricted-user catalog
> filtering by Jellyfin library access), **143** (Users & Devices admin overview + web-session
> tracking), and Ravilo **R175** (the login screen) — one auth→filtering→overview arc, root-caused
> against the live system (all 24 devices bound to the admin user; the stale-token 401/403 log flood;
> restricted users seeing the whole catalog). See each row for detail. (Note: the earlier
> "R174" next-number was already consumed by grid-columns; login screen is R175.) **Next numbers:
> 144 / R176.**
>
> **2026-07-08 — Phase 144 specced from a research report, then BUILT (`✓ Done`).** Cover-art-
> muxed-as-video detection/triage/repair — the library-health management proposed in
> `specs/research-reports/ravilo-cover-art-video-track-2026-07-08.md` after the *Server Farm* TURG
> playback failure (whose client-side fix already shipped, Ravilo commit `482a336`, hardware-only
> video). Detection + `cover_as_video` triage type + "Fix cover track" repair (episode-aware remove
> job) implemented + verified (compile all + `linuxX64Test`). See its row. **Next numbers: 145 / R176.**
>
> **2026-07-08 — Phase 145 spec added (`Planned`, spec-first, implementing next).** Event-driven full
> pipeline: run the *configured* `[[scan.pipeline]]` per triggered item (Radarr/Sonarr/Jellyfin) instead
> of today's hardcoded event flow that omits IMDb + ignores config; adds a targeted `executePipeline`
> mode, rewires `RealtimeIngestService`, and fixes the *arr `getItemByPath` unreliability. **Built same
> day** (`✓ Done`) — shared `PipelineStepOps`, config-driven `runConfiguredSteps`, burst debounce,
> reliable `findRecentItemByPath`. **Next numbers: 146 / R176.**

> **2026-07-09 — the auth arc (141/142/143 + R175) implemented, closing out the design-tool backlog.**
> Each spec first got a dated "Dev-review addenda" reconciling it with live code + the live DB (24
> devices all bound to admin `jogvan`, confirmed) before implementation; the one substantive design
> gap found — Jellyfin's per-DeviceId token pruning would break multi-user on a shared TV under a
> naive per-*device* identity — is fixed by making the Jellyfin identity per-**(device, user)**. **141 +
> R175 ✓ Done** (proxied login + LoginScreen, see their rows). **142 ✓ Done** likewise (library-access
> filtering, see its row). **143 ✓ Done** — the admin overview, revoke, sign-out-everywhere, **and**
> the "Recently watched" history section (initially deferred, then finished the same session — see its
> row) are all built and verified. **The full 141–143 + R175 auth arc is now complete.**

## Admin phases (jellystructure)

| Phase | Status | Focus | Spec |
|-------|--------|-------|------|
| 0 | ✓ Done | Scaffolding — Gradle KMP, Docker Compose, native binary + WASM bundle | — |
| 1 | ✓ Done | Config, DB, auth (Jellyfin sign-in, session cookie, admin gate), library mapping | — |
| 2 | ✓ Done | Jellyfin-driven discovery, ffprobe track data, TMDB match, Library + Media Detail UI | — |
| 3 | ✓ Done | NFO write (movie + tvshow), artwork download, language resolver, language settings UI | — |
| 4 | ✓ Done | Track editing (mkvpropedit/ffmpeg), triage queue, live WebSocket scan, Jellyfin refresh | — |
| 5 | ✓ Done | Folder watcher, Playwright E2E tests, fixture builder, series episode tab | — |
| 6 | ✓ Done | Per-episode TMDB metadata, episodedetails.nfo, episode stills, per-episode track editing | — |
| 7 | ✓ Done | Persistent scan state + resume (FR-S1) | git history |
| 8 | ✓ Done | Three-way theme picker — Light / Dark / System (FR-T1) | git history |
| 9 | ✓ Done | Per-field dirty indicators + diff popup (FR-D2) | git history |
| 10 | ✓ Done | External links — Jellyfin + TMDB (FR-X1) | git history |
| 11 | ✓ Done | Language pickers — searchable dropdowns (FR-L1) | git history |
| 12 | ✓ Done | Simplify TV series language UI (FR-U1) | git history |
| 13 | ✓ Done | Sync single media item (FR-S2) | git history |
| 14 | ✓ Done | Persistence layer — SQLite/SQLDelight (FR-P1) | git history |
| 15 | ✓ Done | Library path-match diagnostics + path-check (FR-B1) | git history |
| 16 | ✓ Done | Multi-worker scanner — dynamic workers + thread pool (FR-W1) | git history |
| 17 | ✓ Done | Activity log backend — persistent log, filters, live runners (FR-A1) | git history |
| 18 | ✓ Done | Settings page cleanup — nav fix, section reorganisation (FR-C1) | git history |
| 19 | ✓ Done | Studios, Networks, Genres & Tags metadata page (FR-M1) | git history |
| 20 | ✓ Done | Library audio-track filter — find audio-description/commentary tracks (FR-LF1) | git history |
| 21 | ✓ Done | Flag multiple default audio tracks in Triage (FR-DA1) | git history |
| 22 | ✓ Done | Remove lockdata & detect Jellyfin field locks (FR-LK1) | git history |
| 23 | ✓ Done | Remove the Language page (FR-RL1) | git history |
| 24 | ✓ Done | Manage the TMDB ID field on Media Detail (FR-TI1) | git history |
| 25 | ✓ Done | Fix Sync button → "Re-pull from Jellyfin…" (FR-RJ1) | git history |
| 26 | ✓ Done | qBittorrent seeding guard — block mkvpropedit when file is actively seeded (FR-QB1) | git history |
| 27 | ✓ Done | Merge Triage into Media Detail + floating Triage dock (FR-MT1) | git history |
| 28 | ✓ Done | URL-based tab & view-state navigation (FR-UN1) | git history |
| 29 | ✓ Done | Multi-language library search — remember every title ever pulled (FR-ML1) | git history |
| 30 | ✓ Done | Library multi-axis filters — studio/network/genre/tags dropdowns + meta-facets (FR-LMF1) | git history |
| 31 | ✓ Done | Studio & network logo artwork — fetch/cache/serve + batch (FR-SNA1) | git history |
| 32 | ✓ Done | In-app TMDB match picker — search & pick the right match (FR-TM1) | git history |
| 33 | ✓ Done | Jellyfin ⇄ NFO drift detection + re-assert (FR-DR1) | git history |
| 34 | ✓ Done | Undo / revert from history (FR-UR1) | git history |
| 35 | ✓ Done | System health panel — real test-connections + tool/disk checks (FR-HC1) | git history |
| 36 | ✓ Done | Operator controls — per-library scan/push, scheduled scans, notifications (FR-OC1) | git history |
| 37 | ✓ Done | Surface the qBittorrent seeding guard (FR-QS1) | git history |
| 38 | ✓ Done | Command palette ⌘K + attention-queue keyboard nav (FR-KB1) | git history |
| 39 | ✓ Done | Subtitle management — forced flag toggle on subtitle tracks, MKV only (FR-SUB1) | git history |
| 40 | ✓ Done | Configure qBittorrent in Settings — guard opt-in; off ⇒ no cross-seed safety (FR-QC1) | git history |
| 41 | ✓ Done | Merge track-order page into Media Detail "Tracks & order" tab — unified movie editor + warnings (FR-TO1) — _note: staged-changes/command-preview UX superseded by Phase 71 (write-through); page-merge + editor done._ | git history |
| 42 | ✓ Done | Per-episode track & order editor on Series detail — same editor in a modal (FR-TO2) — _note: per-episode staged-changes UX superseded by Phase 71; modal + editor done._ | git history |
| 43 | ✓ Done | Library page: design fidelity (Movies/TV switch) + infinite virtual scroll replacing pagination (FR-LV1) | git history |
| 44 | ✓ Done | NFO raw viewer — read-only on-disk XML with a multi-file tree sidebar (FR-NR1) | git history |
| 45 | ✓ Done | Fix the track-editor language picker — unstyled `.langmenu` popup; unify on the shared styled picker (FR-LP1) | git history |
| 46 | ✓ Done | Track language writes must persist — 2↔3-letter code mapping, verify-after-write, truthful response + preview (FR-TL1) — _note: language write+persist+verify done; command-preview UI dropped with Phase 71's staged model._ | git history |
| 47 | ✓ Done | Artwork manager on Movie & Series detail — asset rail + inline TMDB gallery, resolved-language fallback (no-language ≠ All), drag-drop/upload/URL, season posters + episode stills (FR-AM1) | git history |
| 48 | ✓ Done | Artwork manager — fix the Textless / With-text filter: it only re-sorted (a no-op within a language bucket) and overlapped the language chips; unified both into one real single-select filter on TMDB `iso_639_1` — `All · Textless · With text · <langs>` (FR-AM2) | git history |
| 49 | ✓ Done | Full episode coverage for large series — the scan probed only a spread of 100 episodes (>100-ep series showed scattered gaps in Seasons & Episodes + Artwork, with no UI recovery); make the cap configurable (`scan_episode_cap`, default 0 = unlimited) + wire an on-demand uncapped "Re-scan all episodes" (FR-EP1) | git history |
| 50 | ✓ Done | Fix Jellyfin item-refresh auth + single-item fetch — `refreshItem`'s `Authorization` header was malformed (missing `, Token=`) → 401 on every targeted refresh; `getItem` used the unreliable non-user-scoped `/Items/{id}` endpoint and the client had no response validator, so a non-2xx `text/plain` body threw `NoTransformationFoundException` (400). Routed all calls through one `jellyfinAuth` helper, added a 2xx-gated `bodyOrNull`, re-pointed `getItem` to the proven `/Items?Ids=` list shape, and returned the refresh outcome from `pushToJellyfin` (FR-JR1) | git history |
| 51 | ✓ Done | Tag population & merge lifecycle — item tags were never read from Jellyfin (`Tags` field unrequested, no DTO slot, scanner left `tags=[]`) and the Phase 19 §15 merge was unimplemented (full re-scan wiped even JS tags). Now: full scan = Jellyfin `Tags` + JS; re-pull from TMDB = TMDB **keywords** + JS; re-pull from Jellyfin = union; JS-defined tags always survive (FR-TG1) | git history |
| 52 | ✓ Done | Tag UX — sync the media-detail tags section to `design/app/media.html` (own card, manage-tags link, dotted JS chips, outline dirty ring) + distinguish JS vs normal tags in the filter workbench & Library pickers (server-colored facet, dotted + grouped). Also corrected the stale "Tailwind" note: the app ships `wf.css`/`app.css` verbatim via `syncDesignAssets` (FR-TG2) | git history |
| 53 | ✓ Done | Scanner data-quality fixes — from a post-reset full-sync review: full-scan `year` was null everywhere (now `searchYear = name ?? ProductionYear` for search/slug, stored year prefers TMDB); `slugify` made empty/colliding ids that silently dropped items (non-Latin → `""`, dup titles collide) → `itemId` falls back to `jf-<jellyfinId>` + `disambiguateIds` de-dupes collisions; widened the `SxxExx` season regex (`S2025E01`); scan now logs a skipped/unmatched-items summary; minor (zero-track-episode warn) (FR-SQ1) | git history |
| 54 | ✓ Done | Configure Radarr & Sonarr in Settings — opt-in, read-only `[radarr]`/`[sonarr]` integrations (default off): read root folders → import as `[[libraries]]`, and best-effort `RescanMovie`/`RescanSeries` after JS writes. No acquisition/mutation; never blocks a write; api-key masked + `##KEEP##` like qBittorrent (FR-AR1) | git history |
| 55 | ✓ Done | Settings as URL-addressable tabs — convert the long-scroll Settings into 6 `?tab=` panels (Connections · Libraries [mapping+scanning] · Metadata · Download tools [Radarr/Sonarr+cross-seed] · Notifications · Advanced) via the Phase 28 `replaceState` Router; drop the scroll-spy; health-check failures aggregate to per-tab badges + switch-to-tab (FR-ST1) | git history |
| 56 | ✓ Done | Radarr/Sonarr acquisition pipeline + unified download-status state machine — request a not-in-library title (add+search) and track it through one shared `AcquisitionStatus` enum: `requested` (not yet at the download client) → `queued` (in the client queue) → `downloading` (% + `stalled`/`metadata` flags) → `importing` → `available` (+ `failed`); `*arr` queue is source of truth, qBittorrent enriches; `acquisition_changed` WS events; request+track only (FR-AQ1) | git history |
| 57 | ✓ Done | Chart/Discover feed ingestion (vendor-abstracted; Netflix via Tudum first) — `ChartProvider` abstraction + `ChartEntry` model; country movies/TV (rank-only), global movies, non-English, all-time (views); weekly history for trend badges; TMDB-resolve + library-match at ingest; country feeds carry no views by construction (FR-CH1) | git history |
| 58 | ✓ Done | Dark-theme comfort palette ("Soft Charcoal") — retune the **dark** surface/ink tokens only: lift the near-black floor (`--bg #0b0c12 → #16181f`), dim ink to ~88% (`--ink #eef0f7 → #dde1ec`) to kill glare, and raise card surfaces (`--card-bg`) so panels still read as elevated. Light theme, accent + status tokens, and all per-screen CSS untouched; retunes from one `:root` edit (FR-DK1) | git history |
| 70 | ✓ Done | Brand mark "Quartet Play" + app logo wiring — replace the placeholder CSS glyph with a real SVG mark (2×2 structure grid whose open slot resolves into a play triangle) on the Aurora gradient; wired into the sidebar, mobile topbar and login, with a full asset set (icon sizes, favicon, mono, lockups, OG). No new color tokens; theme-independent (FR-BR1) | git history |
| 71 | ✓ Done | Detail-page **write-through editing** + **artwork lightbox** — drop the stage→apply model on Movie/Series detail: artwork candidates and track flag/order edits now write to disk immediately (toasts convey `mkvpropedit` ~40ms vs `ffmpeg -c copy` remux cost; **Save & sync to Jellyfin** pushes, else Jellyfin picks up on next scan). The "Staged changes" card is removed. Artwork gallery gains a hover ⤢ **lightbox** — large preview + metadata, ←/→ to browse, **Use this artwork** writes (FR-DE1) | git history |
| 72 | ✓ Done | Canonical language-code equivalence across all comparisons — fix the false **"Default ≠ resolved"** cascade (raw `eng` track tag compared to normalised `en` → mis-fires on nearly every tagged movie). One `LanguageResolver.sameLanguage()` routed through every language equality check (cascade, triage mismatch, majority/mix, filters); comparison/display-only (FR-LC1) | git history |
| 73 | ✓ Done | **Write-through metadata sync to Jellyfin** — `PATCH …/metadata` saved to DB only, never writing the NFO or refreshing Jellyfin; fix launches `pushToJellyfin` after `store.updateOne`. **⚠ Trigger model revised by Phase 74** (design): the PATCH should persist to DB only, with the NFO write moved to the explicit Save action (FR-WM1) | git history |
| 74 | ✓ Done | **Metadata write-through to the library DB; Save→NFO / Sync→Jellyfin** — title/year/plot/director/studio + tags write **directly to the DB** on edit (toasts), dropping the **"Save changes"** button, amber dirty borders and the diff popup (supersedes Phase 9). The top split button is reframed into **Save → NFO** (write our setup to the .nfo files), **Sync Jellyfin** (ask Jellyfin to re-read them), and a **Save & Sync** primary. Revises Phase 73's per-PATCH push: the PATCH persists to DB only (FR-WM2) | git history |
| 75 | ✓ Done | **Cast & crew — TMDB people, detail editor, NFO sync + person images** — fetch cast/crew from TMDB credits; new **Cast & crew** tab on Movie + Series detail (cast grid with drag-reorder + editable role, crew grouped by department, TMDB person-search add). Write-through (Phase 74): DB on edit, **Save → NFO** writes `<actor>`/`<director>`/`<writer>`. Person photos cached & served (Phase 31 pattern) and written as `<actor><thumb>` so Jellyfin pulls them — across movies, series & episode guest stars. New `Person` model; `cast[]`/`crew[]` on items (FR-CC1) | git history |
| 76 | ✓ Done | **Scope-aware season/episode cast** — Series-detail Cast & crew gains a **Series · Season · Episode** switcher. Main cast = TMDB `aggregate_credits` (inherited by every episode, → `tvshow.nfo`); episodes show inherited cast (read-only) + editable **guest stars** + crew (→ `episodedetails.nfo`). Season scope is a **presence matrix** (All-seasons numbered view; per-season E1…En tap-to-toggle). ⓘ help popup explains the TMDB→NFO→Jellyfin flow (FR-CC2) | git history |
| 78 | ✓ Done | **File-descriptor exhaustion crash fix** — Native CIO server's `select()` caps FDs at FD_SETSIZE=1024; a flood of cold `/api/people/{id}/image` requests opened unbounded outbound sockets + temp files → fatal selector throw. Fix: `Semaphore(8)` bounding `LogoDownloader.download`, O(1) cached `MediaStore.personProfilePath` (was a full-library deserialize per request), `loading="lazy"` on person imgs. Scan-time pre-warm deferred (FR-FD1) | git history |
| 79 | ✓ Done | **Cast tab: episode-count badge overlaid on the photo** — the `▸ N eps` / `⤓ inherited` badges moved inside the `.ph` photo div (anchored `bottom:6px`) so they overlay the image's lower-left instead of floating over the text body; `onerror` no longer wipes them (FR-CC3) | git history |
| 80 | ✓ Done | **Cast presence matrix reflects real TMDB per-season data** — added TMDB `/season/{n}/aggregate_credits`, populate real `Person.seasonEpisodeCounts` at scan time; the matrix shows true per-season counts (absent seasons blank), per-season view lists only cast in that season, guest stars stay episode-accurate; NFO episode inheritance follows season membership + per-episode overrides (FR-CC4) | git history |
| 81 | ✓ Done | **Artwork tab: selecting a new image updates which one is "on disk"** — picking a different poster/backdrop left the **ON DISK** ribbon stuck on the old image (the file was overwritten on disk, but `posterPath`/`backdropPath` were never updated, and the gallery flags on-disk by comparing the candidate's TMDB `file_path` to that field). `candidates/save` now writes the chosen TMDB path through to the item via `updateOne` (custom http URLs still overwrite the file but don't touch the field; clearlogo/banner stay file-existence based); the gallery already reloads, so the ribbon follows the selection (FR-AM3) | git history |
| 82 | ✓ Done | **Tag color swatches render + tag-modal footer fits** — on `/#/metadata?tab=tags` the colour picker *looks* absent and the edit-modal's Delete button floats outside the popup. Premise-corrected: the colour field + swatch picker + backend `color` are all already wired; the real bug is the `.swatch`/`.swatches`/`.swatch.on` (+ `.tag-card`/`.tag-dot-lg`) CSS lives only in the `metadata.html` mockup, never ported to shipped `wf.css` → swatches render zero-size. Fix: port the swatch CSS, align `PRESET_COLORS` to the design's Aurora palette (default `#7b6ef0`), and make the modal footer wrap/group (`[Delete] [View in library]` ‖ `[Cancel] [Save]`, drop `margin-right:auto`) so it fits (FR-TG3) | git history |
| 83 | ✓ Done | **Live scan results must respect the active Library filter** — during a scan, the `ItemScanned` WS handler (`Library.kt:418`) `appendItemToGrid`s **every** scanned item unconditionally, ignoring the active search/filter, so a filtered list gets polluted (the normal `/api/media` path filters server-side; the live insert bypasses it). Fix: gate the live insert on a `queryActive` flag — stream live only when no query is active, else show a "N new items — refresh" affordance that re-runs the server-filtered `loadMore(reset=true)` (keeps FE=BE; client-side matcher deferred — would need predicates moved to commonMain) (FR-SQ2) | git history |
| 84 | ✓ Done | **Debounce the Library search (URL + results)** — the search box fires `loadMore(reset=true)` on every keystroke (network fetch + "Loading…" grid teardown + 60-card rebuild each time), and the `libLoading` guard drops mid-flight keystrokes so URL/results can go stale. Not history spam / not re-render (already `replaceState`, no `hashchange`). Fix: debounce ~250ms like the ⌘K palette (capture latest `libSearch` immediately, cancel-previous + `delay` → `loadMore`), optionally skip the loading-flash on a search refresh — type freely, live results, URL ends at the final value (FR-LS1) | git history |
| 87 | ✓ Done | **Media detail: audio-track language flags in the pagebar** — show which languages a title can be heard in via a compact country-flag strip in the media-detail pagebar (right of TMDB badge). ISO-639-1 → ISO-3166-1 `LANG_CC` map, `audioFlagsHtml()` renders up to 5 flags + `+N` overflow pill using lipis/flag-icons SVGs (served via `syncDesignAssets`, linked via `flags.css`). Live-updating: `TrackEditor.kt:renderList()` calls `refreshPagebarAudioFlags()` when audio tab changes. Admin-only (not Ravilo) (FR-AF1) | git history |
| 88 | ✓ Done | **Decoded-library cache (read-path keystone)** — `allItemsCache: List<MediaItem>?` in `MediaStore`, nulled in `upsertItem()` (same pattern as `peopleIndexCache`/`jellyfinIdIndex`). `allItems()` builds once then returns the cached list; `list()` switched from `db.mediaQueries.listFiltered()` to filtering the in-memory list. `libraryVersion: Long` counter incremented on every write — used by R86 as a cache-invalidation key for derived caches. Eliminates 18.7 MB JSON deserialization on every admin + Ravilo read (FR-PF1) | git history |
| 89 | ✓ Done | **Library & search read performance** — grid list response strips `cast`, `crew`, `tracks`, `titlesByLang`, `episodes` (was shipping ~61 KB/item mean, now ~1 KB); `trackFacets()`, `metaFacets()`, and `nfoCoveredCount()` memoised against `libraryVersion` so second calls within a write epoch are O(1) — drops the 307 `SystemFileSystem.exists()` stats per `/stats` load and rebuilding facet maps on every Library page. SQL pushdown + FTS5 search deferred (FR-PF2) | git history |
| 90 | ✓ Done | **Transport & SQLite engine hardening** — HTTP timeouts installed on every outbound client + SQLite tuning + a download-concurrency cap, for FD safety. | git history |
| 99 | ✓ Done | **Seeding snapshot cache: one short-lived qBittorrent snapshot, shared everywhere** — One TTL-cached, stale-while-revalidate snapshot now backs every seeding read instead of redundant live qBittorrent calls. | git history |
| 98 | ✓ Done | **Tracker registry: name a tracker, map its announce-host mirrors** — Lets an operator name each qBittorrent tracker and its mirror hosts so cross-seed counts are correct, plus a new "seeded on" Library filter. | git history |
| 97 | ✓ Done | **Seeding & cross-seed surface on detail pages** — A Seeding tab + pagebar pill showing per-torrent tracker, scope, and cross-seed depth, with per-episode edit-locking while actively seeding. | git history |
| 96 | ✓ Done | **Bulk track re-order across a series** — A 3-step wizard (set up → review → apply) reorders audio/subtitle tracks across a whole series, with a per-episode remux queue and live WS progress. | git history |
| 95 | ✓ Done | **Non-destructive scanner: vanished items become a triage issue, never a deletion** — Fixed a real data-loss incident (a filtered scheduled scan wiped the catalog 323→6) — the scanner now only adds/updates; a vanished item is flagged for manual triage instead of deleted. | git history |
| 94 | ✓ Done | **Genre provenance: user genre edits survive TMDB re-sync** — A derived added/removed provenance model so a manually-added or removed genre is no longer overwritten by the next TMDB sync. | git history |
| 93 | ✓ Done | **Scan / refresh subsystem overhaul (admin clarity)** — Fixed a dead folder watcher, a schedule that silently ignored its configured time, added on-demand pipeline runs, per-run Activity filtering, and clearer scan vocabulary. | git history |
| 92 | ✓ Done | **"Missing artwork" means no poster on disk (filter + triage)** — Collapsed two overlapping signals into one real on-disk-poster-exists check for the filter, triage, and pill. | git history |
| 91 | ✓ Done | **Scheduled scan pipeline (composable building blocks) + release-age freshness policy** — An operator-composed chain of scan/pull/artwork/NFO/sync blocks, plus an age-tiered re-check cadence for unchanged titles. | git history |
| 86 | ✓ Done | **Ravilo Config page: performance overhaul** — page degrades at scale: `resolveHeroDisplayHints()` fetches whole library O(n/100) sequential calls; N+1 per-channel count badges each do a full SQLite scan; workbench `render()` replaces full modal DOM + re-wires 50+ listeners on every keystroke; hero-height slider fires `renderPreview()` per px. Fix: (1) `/api/metadata/all-facets` single endpoint; (2) per-item hero hint resolution; (3) `POST /api/media/batch-count` batch count endpoint; (4) event delegation + targeted DOM in workbench builder; (5) scope config client cache; (6) incremental preview updates (FR-RC-PF1) | git history |
| 85 | ✓ Done | **Restore the Settings tabbed layout (sync-stripped CSS)** — Phase 55's `?tab=` tabs stopped working; premise-corrected: the `Settings.kt` tab machinery is fully intact, but a later "updated designs" sync (`3650d61`) **deleted** the two load-bearing CSS rules `.set-section[data-tab]{display:none}` / `.set-section[data-tab].tab-show{display:block}` from `wf.css` (added by `7bc224f`), so all six panels fall back to `display:block` = long scroll. Fix: re-add the two rules to `design/app/wf.css` (no Kotlin change) **and** to the design-project source so the next sync doesn't strip them again — same recurring sync issue as the cast CSS (FR-ST2) | git history |
| 100 | ✓ Done | **Artwork: confirm logo on-disk + make banner work** — Fixed the clearlogo gallery not badging its on-disk pick, and added upload/URL-only banner support (TMDB has no banner category). | git history |
| 101 | ✓ Done | **Library search simplification + workbench-apply race fix** — Removed redundant inline facet controls (superseded by the workbench) and fixed a pagination race that caused zero results right after applying a filter. | git history |
| 102 | ✓ Done | **Clickable tag → open Library filtered by that tag** — Tag chips on the Metadata page now deep-link into a pre-filtered Library view, reusing the existing URL-driven filter. | git history |
| 103 | ✓ Done | **Sort the Trackers tab by seeded-torrent count** — One-line backend sort so the most-seeded tracker shows first. | git history |
| 104 | ✓ Done | **Detail tab order parity with the design files** — Reordered the Movie/Series detail tabs to match the design mockups (Seeding moved up, Artwork before Cast). | git history |
| 105 | ✓ Done | **Library filter row: match the design layout + drop "Save filter as…"** — Moved search/kind-toggle into the pagebar and removed the save-filter round trip, now superseded by the Ravilo config editor. | git history |
| 106 | ✓ Done | **Age ratings: region-cascade config + TMDB ingest + detail display + workbench facet** — Ingests TMDB certifications and resolves them live against an operator-configurable region cascade, shown on the detail page, NFO, Ravilo, and a new workbench facet. | git history |
| 107 | ✓ Done | **Global Discover / Top 10 sources config in Settings** — Added the missing region multi-select and refresh-cadence controls, plus RapidAPI-key gating for the paid streaming-availability providers. | git history |
| 108 | ✓ Done | **Correct "Newly Added" sort (by JS created-at) + surface all timestamps** — New JS-owned createdAt/updatedAt (not Jellyfin's file-mtime-derived DateCreated) drives recency sort everywhere, with a new Timestamps card on the detail page. | git history |
| 109 | ✓ Done | **Serialized media-worker job queue for heavy edits (ffmpeg remux) + Activity ▸ Jobs page** — Remux operations now run through a single-concurrency background queue with live progress instead of blocking inline. | git history |
| 110 | ✓ Done | **Jellyfin session bridge: one named session per Ravilo TV** — Each TV now gets its own named Jellyfin device/session instead of sharing one global identity, fixing device-list clutter and a likely 401-storm cause; also carries dashboard messages and remote commands. | git history |
| 111 | ✓ Done | **Jellystructure API keys + Ravilo remote-control API** — Hashed, scoped API keys back a Home-Assistant-style remote play/pause/stop/home control surface and an admin device list. | git history |
| 112 | ✓ Done | **Remove the "Banner" artwork type** — TMDB has no banner category so the slot could never be filled — removed end-to-end. | git history |
| 113 | ✓ Done | **Scan-pipeline freshness cadences: real dropdowns + a Daily option** — Replaced undiscoverable cycle-buttons with real dropdowns and added a missing Daily cadence. | git history |
| 114 | ✓ Done | **Realtime ingest: new media reaches Ravilo in minutes, not scan-cycles** — An *arr webhook nudges Jellyfin and triggers a poll-and-scan, so new media lands in Ravilo within minutes instead of waiting for the next scheduled scan. | git history |
| 115 | ✓ Done | **NFO ⇄ Jellyfin sync correctness: make the drift banner rare and truthful** — A real state machine (NFO stale / Jellyfin behind / genuine drift) replaces a crude field compare, so the drift banner and its CTA are reliably correct. | git history |
| 116 | ✓ Done | **Determinate scan progress: a real bar with a real total** — Scans now precompute a real item total and emit per-item progress instead of an indeterminate spinner. | git history |
| 117 | ✓ Done | **Dashboard: full triage-type breakdown + per-issue Library filters** — An always-visible per-issue-type breakdown card on the Dashboard, each row deep-linking into a pre-filtered Library view. | git history |
| 118 | ✓ Done | **Crash resilience: never die silently + keep FDs under the select() ceiling** — Added a crash marker + recovery webhook, and FD-budget engineering (one shared outbound client, cache headers, process gating, a watchdog with shed/drain/restart tiers) to hold off the unfixable FD_SETSIZE crash. | git history |
| 119 | ✓ Done | **Age ratings: the region cascade is authoritative — no out-of-cascade fallback** — Removed a silent alphabetical-fallback bug that could show a rating from the wrong country when the configured cascade matched nothing. | git history |
| 120 | ✓ Done | **Track editor: "Keep this one" resolves multi-default audio for ANY track** — Fixed a diff bug that only checked the first default track, so clicking "keep this one" on any other track silently did nothing. | git history |
| 121 | ✓ Done | **Triage: "missing episode overview" → "missing episode still image"** — Reframed the triage signal to the real problem — no still image on disk — now that the screen-grabber (R131) guarantees one. | git history |
| 122 | ✓ Done | **Triage: detect (and prevent) duplicate library entries sharing one Jellyfin id** — Fixed a dedup bug that let stale duplicate rows persist across scans, plus a new triage type to catch any that already exist. | git history |
| 123 | ✓ Done | **Media-job progress: accurate ETA & time-remaining for remux jobs** — Wires ffmpeg's own speed readout into a real ETA instead of a placeholder. | git history |
| 124 | ✓ Done | **Metadata page fixes: tag "View in library" deep-link + Trackers CSS** — Fixed a double-hash navigation bug on the tag deep-link and ported missing Trackers-tab CSS into the shipped stylesheet. | git history |
| 125 | ✓ Done | **Enable Qodana locally (Community linter, no cloud)** — Switched to the Community linter (the committed config required a paid cloud token) plus a script working around two real local-run gotchas. | git history |
| 126 | ✓ Done | **Qodana remediation, part 1: profile tuning + mechanical cleanup** — Scoped out false-positive rule categories and fixed roughly 228 mechanical findings across 64 files. | git history |
| 127 | ✓ Done | **Qodana remediation, part 2: correctness review + baseline** — Resolved the judgment-heavy findings (dead code, duplication, redundant suspend/nullable) with per-item verification, plus a committed baseline so future runs only surface new regressions. | git history |
| 129 | ✓ Done | **Bound FD growth at the source: census-driven in-code queue + last-resort restart** — Consolidated outbound HTTP clients onto one shared pool, added a global inbound load-shed above a threshold, and a watchdog that drains + restarts as a last resort. | git history |
| 128 | ✓ Done | **Zero-audio-track diagnosis & repair tools in "Tracks & order"** — A corrupt/truncated file with no audio tracks used to render blank and mislabel itself English; added real diagnosis plus re-probe/re-acquire actions. | git history |
| 130 | ✓ Done | **TMDB trailer ingest + trailer on the detail page (YouTube/Vimeo)** — Pulls one official trailer per title from TMDB at scan time, shown on the admin Overview tab and carried to the Ravilo detail DTO for R163. | git history |
| 131 | ✓ Done | **IMDb ratings: ingest from imdbapi.dev, store & sync, show on the detail page** — for titles with an **IMDb id** (`MediaItem.imdbId`, already existed), fetches `{aggregateRating, voteCount}` from **imdbapi.dev** via a new `ImdbClient` (`imdb/ImdbClient.kt`, rides the Phase 129 shared `OutboundHttp.client` + `withPermit`, no new HTTP client), stores `MediaItem.imdbRating = ImdbRating(aggregateRating, voteCount, syncedAt)` (additive/nullable, no migration), refreshed by a new **`sync_imdb_ratings`** `PipelineStep` kind in `executePipeline` (`Main.kt` — no second scheduler; 250ms per-title throttle on top of the permit gate since imdbapi.dev's batch endpoint wasn't verified) — **never queried ad-hoc**; a failed/absent lookup leaves the previous value intact. Added to the Settings pipeline builder's block palette (`PIPE_BLOCKS`/`PIPE_PALETTE`/`PIPE_SHORT` in `Settings.kt`). Admin detail shows a **pagebar IMDb pill** (`buildImdbPillHtml`, ★ rating · abbreviated votes → imdb.com, hidden with no rating) + an Overview **IMDb rating** card (`buildImdbCard` — score/10 · votes · id link · synced-ago · Re-sync; empty-state when an imdbId exists but nothing's synced yet, absent entirely with no imdbId). New `POST /api/media/{id}/imdb-rating/sync` manual per-title route, `MediaHistory`-recorded. `MovieDetail`/`SeriesDetail.imdbRating: TvImdbRating?` added to the TV DTO (catalog-only, `DetailService.tvImdbRating()`) for **R164**; `MediaCard` deliberately skipped per the spec's dev-review (payload bloat — detail DTOs only). `.imdb-*` CSS added to `wf.css` (the runtime app ships that file, not the static mockups). | [spec](specs/requirements/phase-131-imdb-ratings-ingest.md) |
| 132 | ✓ Done | **Remove RapidAPI (streaming-availability Discover provider) end to end (FR-RAPI1)** — we've moved off RapidAPI; removed the one provider that used it, `StreamingAvailabilityProvider` (the **Max / Disney+ / Amazon Prime / Apple TV+** Top-10 charts), plus its config key, Settings UI, and registry wiring, so RapidAPI is neither configurable nor mentioned anywhere (code + design mockups). **Netflix (Tudum) and JustWatch (Viaplay/Paramount+/SkyShowtime) are independent and stay.** Handled both investigated traps: `weekKey()` relocated from the deleted file to `ChartProvider.kt` (still used by `JustWatchProvider.kt`) before deletion; and `ConfigStore.kt` now parses with `Toml(inputConfig = TomlInputConfig(ignoreUnknownNames = true))` (landed **before** deleting `ApiKeys.streamingAvailabilityKey`, so an existing config with the stale key loads its real values instead of silently reverting to defaults). Also: scrubbed the live key from `config/config.toml` (gitignored, local-only), added `ChartStore.deleteListsWithPrefix()` + a one-time boot-time cleanup in `Main.kt` for the orphan `max-*/disney-*/prime-*/apple-*` chart rows (prefix-based rather than enumerating per-region ids, since regions are operator-configured), and stripped the Settings-UI card + `updateDiscoverProviderLocks()` gating (`Settings.kt`) + the `movieofthenight` mock (`settings.html`, `ravilo-config.html`) including `crunchyroll` (design-only, never registered — nothing real lost). | [spec](specs/requirements/phase-132-remove-rapidapi.md) |
| 133 | ✓ Done | **Keep a manually-chosen poster/backdrop across TMDB sync (FR-ART1)** — bug: a manually picked/uploaded poster reverted on the next TMDB sync. **Root-caused: dual source of truth** — on-disk `poster.jpg` (what Ravilo renders; the `download_artwork` step is skip-if-present so it's NOT clobbered) vs `MediaItem.posterPath` (what every admin surface rendered via the TMDB CDN), which was **unconditionally reset** to the TMDB default on every pull, with no preservation and no marker distinguishing a user-chosen poster. Fixed: additive `MediaItem.lockedArtwork: List<String>` ("poster"/"backdrop") set by both manual routes (`POST .../artwork/upload`, `POST .../artwork/candidates/save`), preserved at the single `MediaStore.addOrUpdate` choke point via new `preserveLockedArtwork` (mirroring `preserveJsTags`) — covers scan, sync, and re-pull uniformly for both `posterPath` and `backdropPath`. Also fixed the **latent second bug**: an uploaded poster now sets `posterPath`/`backdropPath` to the existing public `/tv/image/{id}/poster|backdrop` on-disk-serve route (already used by Ravilo) instead of nothing, so it's visible in the admin immediately; the 4 admin poster-render call sites (`MediaDetail.kt`, `Library.kt`, `Workbench.kt`, `RaviloConfig.kt`) route through a new `posterSrc()` helper that uses the on-disk URL as-is instead of prefixing the TMDB CDN. | [spec](specs/requirements/phase-133-keep-manual-poster.md) |
| 134 | ✓ Done | **FD leak elimination: close every file read + prove the 50-TV/100-worker-scan budget (FR-OPS2)** — second real FD incident, this time **user-visible on a TV** (`FD count critical: 851`, then the Phase-129 shed 503 `"server under FD pressure"` rendered raw on Ravilo). **Root cause proven live** (`/proc/<pid>/fd` census on the still-running broken process: 922 FDs, **~800 = leaked plain-file read handles** — hundreds of episode-still `.src` sidecars at 2–3× each; sockets: just 7): kotlinx-io's `SystemFileSystem.source()` is a raw `fopen()` with **no finalizer**, and the pervasive `source(p).buffered().readString()/readByteArray()` idiom never closes — one permanent FD per call. Fixed all ~16 read sites across 10 files (worst: `ArtworkDownloader.readStillSrc` per-episode × every scan pass — the incident; `RaviloArtworkService.readFresh` 1–2 per image-proxy request **including cache hits**; `Server.serveFrontendFile` per static asset) via a new `FileIo` (`.use{}`-scoped read/write helpers), + hardened non-exception-safe write sinks, + fixed 1 of 2 strays (`QBittorrentClient`'s private Curl pool → shared `OutboundHttp.client`; the other, an ungated `stat` popen, was investigated and descoped — its call chain is already strictly sequential, no concurrency to gate). New `scripts/check-fd-hygiene.sh` regression grep (0 findings). **`/api/tv/events` capped at 128 distinct devices** (reconnects exempt; `TRY_AGAIN_LATER`) — the one genuine pre-existing scaling gap, though not what caused this incident. Census JSON added to the >850 critical log line (the incident's only visible line lacked it). **Mid-implementation scale-up** (explicit ask: 100 concurrent scan workers alongside 50 TVs, "a super powerful machine"): `scan_workers`/`scan_threads` config ceiling 32→100 (backend `Main.kt` **and** the admin frontend's mirrored `max`/`coerceIn`, or the backend change is invisible); `ProcessGate` 4→16 (kept conservative — real forked processes, not just FDs); `OutboundHttp` in-flight 24→64, idle pool ≈15→≈40 (the gate 100 workers actually queue behind for TMDB/artwork/Jellyfin calls). Post-fix budget: ~290 committed at the full 50-TV + 100-worker target, ~610–734 headroom — architecture is now sized for the stated goal; the leak was the *incident's* whole story, the gate scale-up is what makes "100 workers" real throughput rather than a config number. Verified via actual `compileKotlinLinuxX64` + `compileKotlinWasmJs` (not just review — a lesson from earlier the same day). | [spec](specs/requirements/phase-134-fd-leak-elimination.md) |
| 135 | ✓ Done | **Pipeline observability: per-step progress, real worker counts & run descriptors (FR-OPS3)** — only `scan_files` is observable (worker pool + "Now processing / Overall N/M / Workers N/M" via `JobEvent`); every later step (`pull_tmdb → download_artwork → sync_imdb_ratings → rescan_arr → write_nfo → sync_jellyfin`) is a sequential `for`-loop that logs a start+summary line only, so the Activity page freezes on the last scan item and shows a false **"Workers: 0/32"** for the rest of the run, with no phase indicator. Two halves: **processing** — each step's per-item work runs through a bounded worker pool (reusing `scan_files`' pattern, honoring each step's real ceiling: `OutboundHttp`/`ProcessGate`/imdb-throttle/Phase-134 FD-hygiene) so it's faster *and* `activeWorkers`/`targetWorkers` reflect the active step; **observability** — a step-aware progress protocol (`JobEvent`/`ScanStatusResponse` gain a step/phase field + the ordered step plan) drives an Activity page that renders all steps as phase chips, highlights the active phase, and keeps Overall/Now-processing/Workers live in *every* phase; log lines render their already-persisted-but-never-surfaced `step` tag (`RunContext` tagging exists — broadcast it on `LogLine` + add a `?step=` filter). Plus structured **run descriptors** — `trigger` (manual/scheduled/startup) × `scope` (library scan vs full pipeline) × `type` (normal vs `full`/freshness-ignoring) — badged in the header + run picker, replacing the conflated `"scan"|"manual"|"scheduled"` vocabulary (the `?full=true` flag is currently never surfaced client-side though the backend knows it at the call site). | [spec](specs/requirements/phase-135-pipeline-observability.md) |
| 136 | ✓ Done | **Jellyseerr/Overseerr connection + retire the third-party chart Discover/Top-10 subsystem (FR-SEERR1)** — added a **Seerr** card under **Settings ▸ Download tools** (enable · URL · masked API key · Test connection; `config.toml` `[seerr]`; `SeerrClient.ping` under the shared `OutboundHttp` gate) as the sole browse/request source, and **removed the entire RapidAPI / movieofthenight streaming-availability + Netflix·Tudum / JustWatch chart stack** end to end: the `chart/` backend package (5 files), `ChartRoutes.kt`, `DiscoverFeedConfig`/`[discover]` config, the scheduled ingest loop, the DB tables (migration `15.sqm`), and the Settings "Discover" tab/UI. **Supersedes/retires Phases 57 & 107** and the shipped **R48–R50 / R154** chart Top-10. `/tv/discover*` routes are stubbed to `available=false` in the interim (Phase 137/R171 re-point them at Seerr — `shared/.../tv/Chart.kt` deletion is deferred to R171, since the admin editor's list picker and the TV Discover screen still consume it until then). Seerr card is connection-only (per-user rows → Phase 137); approved titles fulfil via existing Radarr/Sonarr. | [spec](specs/requirements/phase-136-seerr-connection-retire-charts.md) |
| 137 | ✓ Done | **Ravilo config ▸ Request — Seerr discover-feeds row builder (replaces per-user Top 10)** — renamed the config editor's **Top 10** section to **Request**: a show-the-tab toggle, an **allow-this-user-to-request** permission toggle, and a draggable list of **Seerr discover feeds** (per-row show/hide + remove; order/visibility persist in the user's Ravilo layout) with an **＋ Add row** popover exposing the full endpoint catalogue — Movies (Discover / genre / orig-language / studio / Upcoming) · TV (Discover / genre / orig-language / network / Upcoming) · Mixed (Trending); parameterised feeds prompt for their value (validated server-side too — a shared `SeerrDiscoverEndpoint.needsParam` keeps the editor and `RaviloConfigService.validate()` from drifting apart). `RaviloConfig.discover` (`DiscoverConfig`) keeps its field name but its shape is now `enabled`/`canRequest`/`feeds: List<SeerrFeed>` — the old chart-list `lists`/`source`/`sources`/`region` are gone. Drives the TV **Request** tab (R171); needs the Phase-136 connection; availability/status resolved live from Seerr at render (not stored). Backend: `SeerrFeed`/`SeerrFeedKind`/`SeerrDiscoverEndpoint` in `:shared`; the dead chart-list-picker client calls (`RaviloApi.getDiscoverLists`/`getDiscoverCoverage`, hitting Phase 136's now-deleted routes) are removed. | [spec](specs/requirements/phase-137-ravilo-config-request-builder.md) |
| 138 | ✓ Done | **Dashboard: hide desktop-only panels on mobile (FR-DB1)** — below the existing 980px breakpoint, "Recently processed" and "Quick actions" are hidden and the "Needs your attention" queue takes the full width; desktop unaffected (`dash-sidecol` class on the column in `Dashboard.kt` + `.dash-sidecol{display:none}` in `app.css`). **Landed once already** (`4c63f14`) and was **silently reverted the very next commit** by a wholesale "updated designs" sync (`885f0b3`) that has no knowledge of this repo-only CSS rule — re-applied here, plus a new regression fence: `scripts/check-mobile-css.sh` (registered in `specs/requirements/README.md`'s post-sync checklist alongside `check-phases.sh`/`check-fd-hygiene.sh`) so the *next* sync that strips it is caught immediately instead of silently, rather than trusting the fix to survive on its own. | [spec](specs/requirements/phase-138-dashboard-mobile-friendly.md) |
| 139 | ✓ Done | **Request-language steering: intents, *arr auto-provisioning, change-later (FR-RL1)** — backend half of "request a title in **Original** vs **Nordic/Danish**". A request now carries a **language intent**; jellystructure maps it to a Radarr/Sonarr **quality profile + tags** so the grab lands the right release (Nordic/Danish, usually incl. English, in practice from NordicVault). Verified: the pipeline carried no language before this (Seerr got only mediaType/mediaId/seasons), Seerr override rules can't express a *desired dub* (they match original language + are skipped for admin callers) but Seerr `POST /request` accepts explicit `profileId`+`tags`, and Radarr doesn't parse "NORDiC" as a language ⇒ match on **release title** regex (`upsertReleaseTitleCustomFormat`/`upsertScoredQualityProfile` in `ArrClient.kt`, verified against live Radarr/Sonarr API shapes). Decisions: **auto-provision** the CF + cloned profiles into both *arrs via a new `RequestLanguageService` (idempotent create-or-update, preview-first — no hand-editing Radarr); **strict** matching (wait for a Nordic release) **plus** a viewer **change-later** to switch a still-waiting request's language (re-profiles + re-searches the *arr item); **configurable** intent list (seeded live with Original + Dansk/Nordic using the real "HD-1080p" profile); requests still go **through Seerr** with jellystructure talking to *arr directly for provisioning + change-later. New: `[[request_language]]` config (+ wasmJs-mirrored DTOs), `RequestLanguageIntent`/`RequestLanguageOption` DTOs, `request_intent` SQLDelight store (v16→v17 migration; flag/waiting/in-progress), `POST …/request/{kind}/{tmdb}/language`, `GET/POST /config/request-language/{preview,provision}`, Settings ▸ Download tools ▸ Request languages card. Verified via real `compileKotlinLinuxX64` + admin `compileKotlinWasmJs`. TV half = R172 (also built). **Remaining before it's live end-to-end:** an admin must click "Set up profiles in Radarr/Sonarr" in Settings once (deliberately not run by the agent — the whole point of routing this through the real backend endpoint, per the owner, is that a human triggers it, not a one-off script). | [spec](specs/requirements/phase-139-request-language-steering.md) |
| 140 | ✓ Done | **Workbench query blocks: nestable AND/OR groups + locked channel scope (FR-WB1)** — the shared R32 filter Workbench (Library filter · Channels · Home rows · per-channel rows) goes from a flat condition stack under one ALL/ANY toggle to **blocks**: a recursive query tree (`ConditionGroup{join, not, children}` of conditions/groups — a sealed `QueryNode` union with a `kind` JSON discriminator, `Condition` retrofitted as the leaf — root = AND/OR between blocks) that finally expresses `(Tag nordic-noir OR dansk-tv OR Genre Thriller) AND Network Kringvarp`. Per-block **NOT** (exclude everything the block matches), nestable **sub-blocks** (depth ≤ 3, enforced in the editor and server-side in `RaviloConfigService.validate()`, new sub-block defaults to the opposite join), clickable join pills, per-block live count badges (one `batch-count` call per refresh), parenthesised text summaries (`groupSummary`). In a per-channel row's editor the channel's whole query renders as a **locked read-only first block** (🔒 name + count + locked AND pill — replaces R127's scope banner; R127 count-narrowing preserved and now also feeds per-block badges; fixed a real bug — the old banner's `wbConds + wbBaseConds` concatenation only composed correctly under ALL, over-matching under ANY). One evaluator: `ConditionEvaluator` is recursive (the flat `(match, conditions)` overload is a thin wrapper that migrates then recurses); fixed a second bug in the process — an empty-values condition used to evaluate `false` and veto its AND block, now correctly skipped. Flat legacy filters **migrate on read** (ALL → one OR-block per condition; ANY → single OR-block, verified in tests to evaluate identically to the old flat evaluator) in config, per-user layouts, and `/api/media` (new `query=` param, `conditions=`/`match=` still accepted and migrated; `batch-count`/`facets` bodies gain the tree too). Library's simple per-facet quick-filters are unchanged (still the fast path); the full workbench's tree rides an additive `libQuery`, shown as one more active-filter chip. The R59 catch-all/coverage-gap handoff now composes the channel's blocks verbatim **AND** a `content_row is_none_of <rows>` block (was: catch-all rows had `conditions = emptyList()`, matching the whole library instead of the channel) via `query=`. New **additive** `query` field on `ChannelConfig`/`RowConfig` — installed Ravilo TV APKs skip it (`ignoreUnknownKeys`); no Ravilo TV app change (server-pushed rows). Mobile pass on the workbench chrome: ≤760px bottom-sheet modal, matches panel stacked below, coarse-pointer tap targets (ported into the workbench's own lazy-injected stylesheet). **First native unit tests in the repo** (`src/linuxX64Test`, 14 passing — join mixing, NOT, neutral-empty, depth-3, both legacy migrations, empty-values-skipped, `content_row` migration, `pruned()`); needed a `build.gradle.kts` linker fix for the native test binary. Verified via real `compileKotlinLinuxX64` + admin `compileKotlinWasmJs` + `:ravilo-ui`/`:ravilo-web` + `linuxX64Test`. | [spec](specs/requirements/phase-140-workbench-query-blocks.md) |
| 141 | ✓ Done | **Ravilo proxied username/password login — retire code pairing (FR-AUTH1)** — replaces the code+poll+admin-approve TV pairing with a direct username/password login on the Ravilo client, proxied through a new `POST /api/tv/login` to Jellyfin `AuthenticateByName`. Dev-review correction: the Jellyfin identity is **per-`(device,user)`** (`ravilo-<deviceId>-<jellyfinUserId>`, via `JellyfinDeviceIdentity.forDevice`), not per physical device — a plain `ravilo-<deviceId>` would let Jellyfin's per-DeviceId token pruning silently invalidate one profile's token the moment a second profile signs in on the same shared TV (R175 multi-user). Fixes the confirmed "all 24 devices are the admin user `jogvan`" bug and the resulting stale-admin-token 401 (`PlaybackService.tvToken`) + 403 WS (`JellyfinSessionBridge`) log flood. `RaviloDeviceService.loginDevice` upserts the `ravilo_device` row directly (skips `ravilo_pairing`, left dormant); password proxied server-side, never stored/logged. Hardens `JellyfinSessionBridge` to route through `tvToken`'s negative-cache/server-token fallback and log "dropped" once per transition. Removes `/tv/pair/{start,poll,approve}` + the admin Pair-a-TV modal (kept `/tv/pair/unpair`). Foundation for 142/143; client half = R175. | [spec](specs/requirements/phase-141-ravilo-proxied-login.md) |
| 142 | ✓ Done | **Restricted-user catalog filtering by Jellyfin library access (FR-AUTH2)** — Ravilo now learns each user's Jellyfin library access and filters every device-facing read path so a restricted user only ever sees/plays content their account is allowed (filtering, **never** blocking — restricted users stay first-class). `JellyfinPolicy` gains `EnableAllFolders`/`EnabledFolders`, resolved for free from the `AuthenticateByName` response Phase 141 already parses (no extra Jellyfin call). `MediaItem.libraryId` captured at scan (`Scanner.scanItem`'s already-resolved `lib.jellyfinId`) and self-healed on every sync/rescan; a one-time startup backfill (`MediaStore.backfillLibraryIds`, matching the stored **local** path — the sync-method direction, not `scanItem`'s Jellyfin-path direction) covers pre-142 rows. `ravilo_device` gains a normalized, comma-joined `allowed_libraries` column (migration `17.sqm`). `MediaStore.visibleTo`/`liveItems(allowed)` gate `HomeFeedService` (home + channel rows, cache key extended with the allowed-set hash), `BrowseService` (browse/search **and** the previously-unfiltered `facets`), and `DetailService` (movie/series detail 404s on a blocked item; `relatedByGenre` filtered). GUID-normalized (dashes stripped, lowercased) on both sides. Live users needing this: charlotte (5 folders), danjal (2), Test Stream (3). **Follow-up (2026-07-09, found live):** library-only filtering left a real gap — a Jellyfin user with `EnableAllFolders=true` but tag-based restriction (e.g. Olivar, `AllowedTags=[børne-tv]`) passed the library check trivially and saw the **entire** catalog in Ravilo, since nothing enforced `AllowedTags`/`BlockedTags`. Fixed: `DeviceData` gains `allowedTags`/`blockedTags` (lowercased), persisted via two new `ravilo_device` columns (migration `19.sqm`) and resolved at login from the same Policy response (no extra call); `MediaItem.passesTagPolicy` + a combined `MediaItem.visibleTo(device)` (library AND tags) now gate every read path above via a new `MediaStore.liveItems(device)` overload; the home-feed cache key folds in the tag sets too. `VisibleToTest` extended with the regression case. `MaxParentalRating` enforcement remains **not** built (Jellyfin's numeric score has no established mapping to jellystructure's TMDB-certification strings in this codebase) — display-only, as before. Verified: `compileKotlinLinuxX64`, `linuxX64Test` (incl. tag-policy tests), admin `compileKotlinWasmJs`, `:ravilo-ui`/`:ravilo-web:compileKotlinWasmJs`, `:ravilo-android`/`:ravilo-phone:compileReleaseKotlin`. | [spec](specs/requirements/phase-142-restricted-user-library-filtering.md) |
| 143 | ✓ Done | **Users & Devices admin overview + web-session tracking (FR-AUTH3)** — one admin surface grouping every Jellyfin user → all their Ravilo devices + admin web sessions (created / last-used / connected), with per-item revoke + "sign out everywhere". `session` gains `created_at`/`last_used_at` (migration `18.sqm`; `SessionService.validate` bumps it debounced once/min, mirroring `RaviloDeviceService`'s inline pattern). `RaviloDeviceService.allDevices()` + `deleteAllForUser`, `SessionService.list()`/`revokeAllForUser` back a new cookie-gated `GET /tv/admin/overview` (one call: devices + sessions + a free policy access-line, since `GET /Users` already returns each user's full `Policy` inline — no extra Jellyfin round-trip) + `DELETE /tv/admin/sessions/{id}` + `POST /tv/admin/users/{userId}/signout-all`. Dev-review correction: session **ids are never the raw token** (that IS the `js_session` cookie value — leaking it to another admin viewing the page would let them hijack that session) — a 12-hex-char prefix is used instead, resolved back to the full token server-side. New "Users & devices" Settings tab (Phase-55 tabs) mirrors the API-keys card; linked from the Ravilo config editor's pagebar (replacing the retired Pair-a-TV button). `JellyfinPolicy` gains `AllowedTags`/`BlockedTags` (display-only; `MaxParentalRating` already existed). **"Now watching"** is sourced from the existing local `PlaybackService.activePlayback` map (no new Jellyfin call). **"Recently watched"** (the design addendum's remaining piece) is built too: `JellyfinUserData` gains `LastPlayedDate`; a new `JellyfinClient.getRecentlyPlayed` (`Filters=IsPlayed&SortBy=DatePlayed`, admin token) backs a lazy, per-user-collapsed `GET /tv/admin/users/{userId}/history?offset=`, with `groupHistoryEntries` collapsing consecutive same-series-same-season episodes into one row (`Havets Hjarta · S01 · E01–E08 · ✓ 8 episodes`), paginated behind a "Show more" button — never fanned out across all users on the base overview load. **Follow-up same session:** in-progress ("stopped at N%") items are folded in too, via `getResumeItems` (`Filters=IsResumable`) reused with the admin token, merged + re-sorted with the finished groups on the first page only (no pagination needed for these — there are only ever a handful at once). A static footer ("Full history lives in Jellyfin") closes out the addendum's last small detail. The shared ISO-8601 parser used for this was extracted out of `Scanner` into `dev.jellystructure.util.isoToEpochSeconds` so both call sites share one implementation. A dead CSS rule-set for the retired Pair-a-TV modal (`.pair-back`/`.pair-modal`/`.code-inputs`) was also removed from `design/app/wf.css`, and a `VisibleToTest` unit test was added for Phase 142's fail-closed filter predicate + GUID normalization (the spec's own acceptance criteria called for one). Completes the 141/142/143 + R175 auth arc in full, no known gaps. Verified: `compileKotlinLinuxX64`, `linuxX64Test` (incl. the new `VisibleToTest`), admin `compileKotlinWasmJs`, `:ravilo-ui`/`:ravilo-web:compileKotlinWasmJs`, `:ravilo-android`/`:ravilo-phone:compileReleaseKotlin`. | [spec](specs/requirements/phase-143-users-devices-overview.md) |
| 144 | ✓ Done | **Cover-art-muxed-as-video: detect, triage & repair (FR-TK2)** — some releases (verified: the *Server Farm* TURG rip, all S01 episodes) mux a `cover.png` as a **second, non-attached-picture video stream**; the malformed file broke Ravilo playback until the client fix ([research report](specs/research-reports/ravilo-cover-art-video-track-2026-07-08.md); Ravilo commit `482a336`, hardware-only video). This adds library-health management in jellystructure: a scan-time O(1) `TriageDetection.coverAsVideoCount` (a VIDEO track with an image codec — png/mjpeg/bmp/gif — beside a real video track), a new `cover_as_video` Triage type (Dashboard breakdown + `Library?filter=` + Triage-dock subline, mirroring `zero_audio`/`multi_default`), and a one-click **Fix cover track** repair that drops the junk stream via an `ffmpeg -map … -c copy` remux — reusing the write-through track-edit infra (`TrackRoutes` + `FfmpegRunner` + `SeedingGuard` + background job + re-probe). Client already handles these files (no Ravilo change); this cleans the library for any client. Same model as Phase 128 (zero-audio) / Phase 120 (multi-default). **Built** 2026-07-08: `TriageDetection.coverAsVideoCount`/`coverVideoSpecifier`, `cover_as_video` triage type + `Library?filter=` + Shell subline, `runRemove` made episode-aware + new episode `DELETE …/episodes/{ep}/tracks/{spec}` route (movie DELETE already existed), and a "Fix cover track" banner on movie/series detail (series = one remux per affected episode). Verified via `compileKotlinLinuxX64` + `compileKotlinWasmJs` + `linuxX64Test` (4 new detection tests). | [spec](specs/requirements/phase-144-cover-art-video-track-health.md) |
| 145 | ✓ Done | **Event-driven full pipeline — run the *configured* pipeline per triggered item (FR-ING1)** — when a file lands via Radarr/Sonarr or Jellyfin, run the **exact configured `[[scan.pipeline]]`** (Scan→TMDB→Artwork→IMDb→*arr→NFO→Jellyfin) instantly, scoped to that item — instead of today's hardcoded event flow (`RealtimeIngestService`) that omits **IMDb ratings** and ignores the configured steps. Adds a **targeted mode to `executePipeline`** (`seedItems` → skip whole-library scan, run every configured step over the seed, close with `/Library/Refresh`) with its own `ScanTracker`/jobId so it never collides with the scheduled scan; rewires `RealtimeIngestService` into a thin dispatcher (resolve → scan → targeted pipeline) with per-series burst debounce; and **fixes the *arr trigger reliability** by dropping the unreliable `getItemByPath` (this Jellyfin ignores `?Path=`) in favour of the nudge + `LibraryChanged` listener (correct ids). Also makes a newly-added series correct in Ravilo immediately (closing library refresh recomputes the child rollup — complements `8a89507`). Builds on Phase 114 (webhooks/listener) + 91 (pipeline) + 135 (per-step pools). **Built** 2026-07-08: extracted `PipelineStepOps` (per-item cores) used by BOTH `executePipeline` (delegated, no drift) and `RealtimeIngestService.runConfiguredSteps` (iterates `cfg.scan.pipeline`, skips scan_files/pull_tmdb) — the realtime path never touches the DB-backed `ScanTracker`, so it coexists with a scheduled scan; per-target 8s burst debounce; `write_nfo` op gained `includeEpisodes` (realtime writes episode NFOs like the old `pushToJellyfin`, scheduled unchanged); reliable `findRecentItemByPath` (client-side path match) replaces the `getItemByPath` `?Path=` lookup, with the LibraryChanged listener's enqueue coalesced by the debounce. Verified via `compileKotlinLinuxX64` (live import test pending a backend rebuild). | [spec](specs/requirements/phase-145-event-driven-full-pipeline.md) |
| 146 | ✓ Done | **Dashboard "Needs your attention" breakdown redesign (FR-DB2)** — `Dashboard.kt`'s attention card reworked into a two-column `.attn-breakdown`/`.abk` grid (ported into the shipped `wf.css`, one column below 640px), each cell a fully clickable `label + one-line description + severity badge` linking to `library.html?filter=<key>`. Resolved the dev-review's open questions: kept the 10th type `missing_artwork` (grouped next to `missing_still`) rather than silently dropping it; authored a net-new bad/warn severity map (`ATTENTION_ROW_ORDER`, since nothing in `TriageTypeCount` carried one) in the dev-review's fixed order; resolved the three-way label contradiction by keeping the backend `TriageTypeCount.label/description` as the one canonical source (already feeds the Triage dock too) rather than adopting the mockup's differently-worded copy. Removed the redundant per-item sample table (`loadAttentionQueue`/`#attention-list`) entirely — the floating Triage dock + Library `?filter=` are now the only per-item entry points; zero-count cells are dimmed but (unlike before) stay clickable. Added the footer "Browse all flagged items in Library →" link and fixed the header's own "Browse all →" button, which had been navigating to unfiltered `/library` instead of `?filter=attention`. Quick actions already had all 6 (§D8 — nothing to do). Presentation-only, no backend change. Verified via admin `compileKotlinWasmJs`; not live-browser-verified this session (no dev-server restart, per standing preference). | [spec](specs/requirements/phase-146-dashboard-breakdown-redesign.md) |
| 147 | ✓ Done | **Live TV configuration in jellystructure (FR-LTV1)** — surfaces Jellyfin's Live TV into Ravilo. `JellyfinClient` gained `getLiveTvInfo`/`getLiveTvChannels`/`getLiveTvPrograms`/`openLiveStream`/`closeLiveStream` (all `OutboundHttp.withPermit`-gated); `JellyfinMediaSourceInfo` extended with the Live TV lifecycle fields (`Protocol`/`Path`/`IsInfiniteStream`/`RequiresOpening`/`RequiresClosing`/`OpenToken`/`LiveStreamId`). Storage is a `JsTagStore`-pattern JSON file (`LiveTvStore`, `data/livetv.json`) — a `LiveTvChannelOverride` per Jellyfin channel id (`shown`/`number`/`order`/`logoOverrideUrl`/`category`/`unavailable`/`isNew`) plus global `LiveTvSettings` (`enabled`/`epgCadenceMinutes`); no SQLDelight migration needed for something this small. `LiveTvService` does the sync/diff (new channels → hidden + badged, gone channels → flagged unavailable + one-click removable, a reappearing channel un-flags automatically), the merged admin lineup view, a cadence-cached guide fetch (dropped the spec's custom-XMLTV option per the dev-review — Jellyfin stays the one guide source), a channel-logo proxy/cache (Jellyfin has no on-disk artwork for Live TV — R133 doesn't cover it), and `tune`/`stopTune` — a **sibling** to `PlaybackService.startPlayback`, not a branch of it, with its own stop-watchdog map wired alongside `PlaybackService`'s in `Main.kt`/`Server.kt` (Live TV's `RequiresOpening`/`RequiresClosing` open/close lifecycle + infinite-duration/no-resume stream has nothing in common with VOD's finite-item resume flow). New `LiveTvRoutes.kt`: admin CRUD under `/tv/admin/livetv/*` (cookie-gated), TV-facing `/tv/livetv/channels`/`/tv/livetv/guide`/`/tv/livetv/channels/{id}/tune`/`/tv/livetv/stop`/`/tv/livetv/heartbeat` (device-token gated, ready for R177), and a public `/tv/livetv/logo/{id}` (added to `AuthPlugin`'s `OPEN_API_PATHS` — logos aren't sensitive and an `<img>` can't carry a device token). `RaviloConfig` gained a `liveTvHome: LiveTvHomePlacement?` field (On-now-row toggle+position, Live-TV-collection toggle) — the Layout tab's new "Live TV on Home" section (`RaviloConfig.kt`) only renders once Live TV is enabled on the Live TV page itself, per the spec's placement/connection split. A real admin page (`LiveTv.kt`, `LiveTvApi.kt`) replaces Phase 148's placeholder: connection badge, channels-discovered/shown/last-synced summary, guide-cadence picker, the new/unavailable lineup-change banner (Show new / Remove missing bulk actions), and the full channel lineup table (▲▼ reorder, show/hide toggle, free-text category, EPG-presence badge) — ported from `design/app/livetv.html` onto real endpoints, dropping its custom-XMLTV-URL option (dev-review addendum F). Verified via `compileKotlinLinuxX64`, `linuxX64Test`, admin `compileKotlinWasmJs`, `:ravilo-ui`/`:ravilo-web:compileKotlinWasmJs`, `:ravilo-android`/`:ravilo-phone:compileDebugKotlin`; not live-browser-verified this session (no dev-server restart, per standing preference) and no tuner/live-stream on-device test (this Jellyfin's M3U/HLS-provider Live TV lineup wasn't exercised end-to-end against a real tune). | [spec](specs/requirements/phase-147-livetv-admin-config.md) |
| 148 | ✓ Done | **Admin "Ravilo" nav section: split editor, move pages, collapsible groups, Collections rename (FR-RV-NAV1)** — the sidebar's `Shell.kt` "Apps" group renamed to **Ravilo** with five items (Layout · Live TV · Requests · Users & devices · Preferences); a genuinely-live `updateActiveNav` bug fixed in the same pass (hrefs render `#`-prefixed but were compared against a bare route — nothing had ever gotten the `.active` class), replaced with query-aware `hrefMatchesRoute` (path must match; an href carrying `tab=` also requires that param to match) fed by `Router.current()` (path+query) instead of the old path-only call. Collapsible sidebar groups (chevron + `localStorage 'js-nav-collapsed:<group>'`, ported from the already-target-state `design/app/app-shell.js` into Kotlin, since that JS file never ships) apply to both Setup and Ravilo. `RaviloConfig.kt`'s `renderSections()` (Hero · Collections · Content rows · Request · Behaviour · **Portrait screen** — the spec's unmentioned 6th section) is now gated by `?tab=layout|requests|preferences` via a `.rav-tab-section[data-rtab].tab-show` CSS toggle; Portrait screen assigned to Layout; the in-page section-nav only renders for Layout (Requests/Preferences are single-section tabs); the layout-lock card is suppressed on Preferences (always-editable per §B1); a bare `/ravilo` canonicalizes (replace-nav) to `?tab=layout`. Fixed a real regression caught before commit: since the three tabs are now separate sidebar links to one shared page, every tab click was re-triggering `renderRaviloConfig`'s full load (refetch users/config, reset scope to Global) — added a `raviloLoaded` guard so only the first visit loads; later tab switches reuse in-memory state, matching the spec's "one shared editor" intent. **Users & devices** extracted verbatim out of `Settings.kt`'s tab rail (`sect-users`/`SECTION_TAB`/`SETTINGS_TABS`/`loadUsersCard`+`refreshUsersList`+`loadUserHistory`) into a new standalone `RaviloUsers.kt` page/route (`/ravilo-users`); the stale `#/settings?tab=users` pagebar link fixed to point at it. Added a `/livetv` placeholder route/page (`LiveTv.kt`) ahead of Phase 147. **Collections rename** (display strings only, ids/keys unchanged): ~20 strings across `RaviloConfig.kt` (section header, "+ Add collection", dialog titles, fallback names, help text) + 2 in `Workbench.kt` (the locked-block eyebrow + preview-chip fallback) + the `ravilo-ui` i18n (`section.channels`/`browse.empty_channel`, en/da/fo) + the `design/app/ravilo-config.html` and `design/ravilo/ravilo-i18n.js` mockups (left already-correct Live TV "channel" wording alone). Verified via `compileKotlinWasmJs` (admin) and `:ravilo-ui:compileKotlinWasmJs`; not live-browser-verified this session (no dev-server restart, per standing preference). | [spec](specs/requirements/phase-148-ravilo-admin-nav-restructure.md) |
| 149 | ✓ Done | **Multi-episode files: one file, several episodes (FR-MEF1)** — `parseSeasonEpisodes` (`Scanner.kt`) handles `SxxEyyEzz…`, `SxxEyy-Ezz` (range expansion), and `1x01x02x03`; `Episode` gains `partIndex`/`partCount`/`chapterStartMs`/`chapterEndMs`/`hasChapters` (`FfprobeRunner.chapters` via `-show_chapters`). Admin **Seasons & episodes** renders one combined row per file (`MediaDetail.kt` `buildMultiEpisodeGroupRow`); **Artwork** picks stills per episode (`ArtworkDownloader.episodeStillPath` folds the episode number into the filename, fixing the addendum §4 disk-path collision). NFO writes one grouped file per addendum §5 (`NfoWriter.writeEpisodeGroup`/`writeEpisodeNfos`). All six dev-review addenda items landed. | [spec](specs/requirements/phase-149-multi-episode-files.md) |
| 150 | ✓ Done | **Intro & credits segment detection + admin management (FR-SEG1)** — layered detection (chapter-title match via `FfprobeRunner.chapters().tags.title` → ffmpeg `blackdetect`/`silencedetect` credits heuristic over a bounded end-of-file window → TMDB `during`/`aftercreditsstinger` keyword merge, gated by a new "Trust TMDB stinger tags" toggle) as a `detect_segments` `PipelineStep` (off by default; admin-configurable extra chapter keywords + cross-episode-fingerprinting toggle), writing a nullable `SegmentMarkers`/`Stinger` JSON-blob field on `Episode`/`MediaItem` (no migration) — never overwritten once `manuallyConfirmed`. Admin **Intro & credits** scrubber (illustrative timeline bar + exact m:ss edit fields + source/confidence badge + Re-scan/Lock) on both the movie Tracks tab and every episode row, write-through via new `/api/media/{id}/segments` + `/episodes/{filename}/segments` routes (`episodeNumber` query param disambiguates a multi-episode-file group). Two new Triage rows ("Low-confidence segments", "No intro/credits detected" — both silent until an admin actually enables the step, so an unconfigured library never floods with false positives), a `has_segments` indexed column (`21.sqm`), and the DTO trip to Ravilo (`Episode.segments` → `buildEpisodeContext` → `PlayerScreen`) for R182 to consume. **Cross-episode audio fingerprinting (FR-SEG1-4)** — `fpcalc` installed on the backend host — `FfmpegRunner.computeFingerprint` (Chromaprint, frame timing derived empirically) + a new `FingerprintService` on-disk cache (`dataDir/fingerprints/`, never the JSON blob) + `SegmentDetection.findIntroMatch` (sliding-offset Hamming-distance correlation, one reference episode per season rather than full pairwise), gated by the `detect_fingerprint` toggle. | [spec](specs/requirements/phase-150-intro-credits-segment-detection.md) |
| 151 | ✓ Done | **Manually-selected images are never overwritten automatically (FR-ART2)** — reported again after Phase 133: a hand-picked poster still reverted on the next scan/re-pull. Root cause: the Phase-133 guard was applied at `MediaStore.addOrUpdate` **only**, but the two routes an operator actually clicks — `POST /{id}/sync` and `POST /{id}/repull-jellyfin` — plus `pushToJellyfin` all persist a *freshly scanned* item (whose `posterPath`/`backdropPath` `Scanner.rescanMetadata`/`syncMovie`/`syncSeriesEpisodes`/`rescanFromJellyfin` had just reset to TMDB's default) through `updateOne`, which had no guard; the scheduled scan was fine, so the bug looked intermittent. Fix: `preserveLockedArtwork` extracted to `media/ArtworkLock.kt` (+ `ArtworkAsset` keys) and applied at **both** write choke points, `updateOne` gaining `respectArtworkLock: Boolean = true` (only the explicit pick/upload routes pass `false`, so a deliberate re-pick still wins); `addOrUpdate` now keys the guard off `old` (`stale ?: existing`) so a slug rename no longer drops the lock. Extended the guarantee to the assets that had **no** lock at all via a new `<image>.manual` marker sidecar (R131 `.src` precedent — travels with the media, survives a DB reset) covering `poster.jpg`/`fanart.jpg`/`clearlogo.png`/`seasonNN-poster.jpg`/`*-thumb.jpg`: `fetchEpisodeStill` no longer upgrades a marked screen-grab to TMDB's still, the picker's **Generate from frame** now marks its frame (it was silently replaced on the next run), `isArtworkIncomplete` stops counting a locked screen-grab as incomplete, `fetch`'s legacy misplaced-artwork cleanup no longer deletes a marked `<library-root>/poster.jpg`, and `fetch`'s automatic season-poster gap-fill passes `manual = false`. Lock made observable via `ArtworkStatus.posterManual`/`fanartManual`/`logoManual`, `EpisodeStillStatus.manual` and the seasons DTO's `posterManual` (all additive/defaulted; the admin client runs `ignoreUnknownKeys`). Verified via `linuxX64Test` (72/72, incl. 7 new `ArtworkLockTest` cases). | [spec](specs/requirements/phase-151-manual-artwork-lock.md) |
| 161 | ✓ Done | **Stop double-delivering embedded text subtitles on direct play (FR-SUB1)** — correction to R55: every embedded SRT/ASS/SSA subtitle was sideloaded as an extracted VTT unconditionally, correct when the player had no in-container text-track rendering at all (R55's original state) but wrong once a client's `MatroskaExtractor` also parses that exact embedded stream natively on a direct-played file — every text subtitle reached the player twice. Reported live via R195's new picker as two identical "English" subtitle rows for "It's Always Rainy in Pittsburgh" S03E03; verified live against Jellyfin and the filesystem that the source genuinely has one subtitle per language (ruling out a mixed embedded+external file) and that this title direct-plays. Already documented and deferred in R183's "Known adjacent issue" (also names a 4m37s Jellyfin ffmpeg VTT-extraction stall on a 26GB title from the same cause). New `ClientCapabilities.supportsEmbeddedTextSubs` (conservative `false` default) + `supportsEmbeddedTextSubtitles()` seam (Android `true`, web `false`); `PlaybackService.startPlayback()` reordered so `needsTranscode` resolves before `buildSubtracks()`, which now sideloads a text subtitle only when transcoding OR the client hasn't confirmed in-container support — `RaviloPlayerAndroid.load()` already skips null-URL subs, no client render-path change needed. `restream()`'s burn-in path is unaffected (always transcodes, so always sideloads, as before). Verified via `compileKotlinLinuxX64`, `linuxX64Test`, and all 5 Ravilo compile targets. Not yet live-retested (deploy is user-initiated). | [spec](specs/requirements/phase-161-embedded-subtitle-double-delivery.md) |
| 160 | ✓ Done | **Scanner falls back to Jellyfin's own numbering when the filename can't be parsed (FR-SCAN3)** — reported live: "La Curva" (bare `SEE`-numbered filenames, no `SxxEyy`/Kodi marker) showed every episode as S0E0 in Ravilo. `parseSeasonEpisodes` genuinely can't parse this convention, and unlike the Phase-152 Jellyfin-id join, jellystructure's season/episode *numbering* never fell back to the correct numbers Jellyfin's own scanner already assigned to the same files (confirmed live: Jellyfin had all 82 episodes numbered correctly). New pure `resolveSeasonEpisode(filenameParsed, jfSeason, jfEpisode)` — only used when the filename parse found nothing, never overrides a successful parse — wired into both `scanSeries` and `syncSeriesEpisodes`, reusing the `jfByPath`/`jfByPathSync` lookups already built for Phase 152/153. Verified via `linuxX64Test` (full suite, incl. 4 new `resolveSeasonEpisode` cases); not live-rescan-verified (requires triggering a rescan against the live library, left for the user). | [spec](specs/requirements/phase-160-scanner-jellyfin-numbering-fallback.md) |
| 157 | Planned | **Bazarr subtitle integration (FR-BZ1)** — optional Bazarr connection so subtitles never need Bazarr's own UI. Principle: **Jellystructure persists no subtitle state** — reads Bazarr live over the API and issues commands; Bazarr keeps owning providers, scoring and language profiles (mirrored read-only). §A Settings → Download tools **Bazarr** card + optional `[bazarr]` TOML (`enabled`/`url`/`api_key`/`auto_search_on_add`), path-matched to the *arr roots. §B a **Subtitles** page (`design/app/subtitles.html`) — **not** in the left nav; reached from a dashboard summary card — with a live queue/tasks list, a **wanted list built for scale** (5,000+ items: total + debounced search + kind/language facets + paged loading, no flat render; **Search all wanted (N)** confirms first), a download/sync/upgrade/remove history feed, provider health, and read-only language profiles. §C per-title Bazarr sections on the movie **Tracks & subtitles** tab (renamed from "Tracks & order") and the series **Seasons & episodes** tab (season-scoped, per-episode have/wanted + actions). §D actions all proxied to Bazarr: manual search/download, auto-search, sync-to-audio, upgrade, delete, full scan. Design-complete 2026-08-07; **not yet dev-reviewed** — open backend question is the exact Bazarr command API + how a JS item resolves to a Bazarr `radarrId`/`sonarrId` (confirm against a live Bazarr before build, as done for Seerr in R190). Design: `design/app/{settings,subtitles,index,media,series}.html` + `app-shell.js`. Spec `specs/requirements/phase-157-bazarr-subtitles.md`. |

## Ravilo phases (Android TV · Web · phone)

| Phase | Status | Focus | Spec |
|-------|--------|-------|------|
| R01 | ✓ Done | `:shared` KMP module — DTOs + Ktor `TvApiClient` (once, for backend + both clients) | git history |
| R02 | ✓ Done | Compose Multiplatform scaffolding — `:ravilo-ui` + `:ravilo-android` + `:ravilo-web`, one "hello focus" screen on both targets | git history |
| R03 | ✓ Done | Backend — `/api/tv/**` namespace + TV **device pairing** auth (code flow, non-admin users, device sessions) | git history |
| R04 | ✓ Done | Backend — per-Jellyfin-user **RaviloConfig store** (server-owned, synced across devices) + `GET /api/tv/config` | git history |
| R05 | ✓ Done | Backend — **home feed** composition (hero, channels, rows incl. merged Continue+NextUp & Newly-Added) | git history |
| R06 | ✓ Done | Backend — **browse + multi-language search** + channel-scoped feeds (reuse Phase 29/30) | git history |
| R07 | ✓ Done | Backend — **detail** API (movie/series, seasons/episodes, watched-state/resume, cast, related) | git history |
| R08 | ✓ Done | Backend — **playback brokering + progress reporting** (StreamTicket, heartbeats, mark played/next-up) | git history |
| R09 | ✓ Done | `:ravilo-ui` — **design system + focus engine** (Aurora/Midnight/Noir, components, shared D-pad/pointer model) + app bar | git history |
| R10 | ✓ Done | `:ravilo-ui` — **Home** (hero carousel, channel rail, content rows) wired to `GET /api/tv/home` | git history |
| R11 | ✓ Done | `:ravilo-ui` — **Channel view + browse grids** (Movies/Series/My List, filters) | git history |
| R12 | ✓ Done | `:ravilo-ui` — **Search** (on-screen keyboard + live results) | git history |
| R13 | ✓ Done | `:ravilo-ui` — **Movie & Series detail** (season picker, episode rail, watched/up-next/resume) | git history |
| R14 | ✓ Done | **Player** — `RaviloPlayer` expect/actual (direct ExoPlayer/Media3 on Android, browser `<video>` on WASM); full player chrome; episode rail; next-up countdown; Audio & Subs picker | git history |
| R15 | ✓ Done | On-device **Settings** + first-run **pairing UX** (server URL setup, skin picker, playback prefs) | git history |
| R16 | ✓ Done | jellystructure web — **Ravilo config screen** backed by the R04 store (hero/channels/rows/merge/skin, per user) | git history |
| R17 | ✓ Done | **Web target hardening** + packaging — `:ravilo-web` canvas a11y/input, both bundles served, E2E across TV + web | git history |
| R18 | ✓ Done | **Multi-user profiles & fast switching** — several Jellyfin users per TV, cached tokens, "Who's watching?" + avatar switcher | git history |
| R19 | ✓ Done | **Interface localization** (en / da / fo), per Jellyfin user, set in Jellystructure | git history |
| R20 | ✓ Done | **Performance & correctness overhaul** — focus-latch freeze fix, store coroutine hygiene, allocation hot-paths, CastCircle D-pad focus | git history |
| R21 | ✓ Done | **Back navigation + focus polish** — D-pad Back pop, spring animation on all focusables, halo focus glow | git history |
| R22 | ✓ Done | **Android TV APK packaging** — TV banner, adaptive icon, splash screen, R8 release build + signing | git history |
| R23 | ✓ Done | **Design system foundations** — bundled Sora + Space Grotesk fonts, corrected skin palettes (Noir warm-gold), new tokens, `RaviloDimens` | git history |
| R24 | ✓ Done | **Component upsizing & visual fidelity** — AppBar 92 dp, Hero 600 dp/72 sp, Tile full-size + gradient fallback, channel card, episode card, row headers, buttons | git history |
| R25 | ✓ Done | **Polish: loading skeletons, focus glow fix, smooth scroll** — shimmer skeletons, `focusGlow` shadow color, minimal-scroll `LazyRow`, season progress bar, browse chip contrast | git history |
| R26 | ✓ Done | **Config DTO unification** — drop parallel `Admin*` DTOs; `wasmJsMain` uses `:shared` `RaviloConfig` directly; backend validates IDs and clamps values | git history |
| R27 | ✓ Done | **Layout model extensions** — `HeroConfig.enabled/order`, `heroHeightPct`, `autoAdvanceSeconds`; feed filters by enabled, sorts by order, passes layout params | git history |
| R28 | ✓ Done | **Config editor fidelity** — `/ravilo` rebuilt: drag-reorder, show/hide toggles, typed filters, system-row protection, hero height slider, tile-shape picker, live schematic preview | git history |
| R29 | ✓ Done | **TV control-plane contract review fixes** — stop/settings request-type bugs, server `ignoreUnknownKeys`, shared `ViewerSettingsRequest`, `viewerSkinOverride`/`effectiveSkin`, `session_id` removal, FocusGrid empty-row guard, R26 clamp/UUID reconcile, `:ravilo-player` deferred doc sync | git history |
| R30 | ✓ Done | **Native focus traversal** — replace the manual `FocusEngine` (per-item `requestFocus`) with Compose focus search + `focusRestorer`; fixes held-key lag/stuck on lazy rows & grids; `FocusEngine.kt` deleted | git history |
| R31 | ✓ Done | **`:ravilo-player` FFmpeg decoder** — jellyfin's `media3-ffmpeg-decoder` in an Android-only, GPL-contained module + renderers factory for DTS/TrueHD/AC3 (Media3 1.8.0); source-vendor fork skipped as redundant; on-device codec test pending | git history |
| R32 | ✓ Done | **Unified filter workbench + hero builder + Library round-trip** — one shared condition-stack builder for Content rows, Channels & the Library; audio-track + hero-item as universal facets; Save-filter-as-Channel/Row + "Feature in Ravilo" on Movie/Series detail | git history |
| R33 | ✓ Done | **Live config push** — per-user WebSocket (`/api/tv/events`); a config write (`RaviloConfigService.save`) pushes `config_changed` to that user's connected TVs, which silently re-pull the feed/config so channel/hero-height/tile-shape/row/skin edits appear in ~1s with no reload | git history |
| R34 | ✓ Done | **TV detail layout + content size** — full-bleed viewport hero on Movie/Series detail (opens at top, no entry-scroll that strands the screen) + TV-tuned detail sizing; per-user `uiDensity` (Compact/Cozy/Comfortable) scales the grid tiles, edited in the config screen, applied live via R33 | git history |
| R35 | ✓ Done | **Home-screen TV sizing + hero framing** — Home hero carousel + content-row headers brought down to the R34 detail scale; shared `RaviloButton` chrome shrunk globally (44dp/15sp/10dp); hero backdrop crop re-framed to `center 26%` (`heroBackdropAlignment`) across all three heroes. Fixed-tuned; config-driven values (hero height, auto-advance, tile shape, `uiDensity`) untouched. Supersedes R24's home Hero numbers | git history |
| R36 | ✓ Done | **Channel-button editor** — channel look (Logo/Text + brand fill) configured **only** in the editor popup; Logo = a real uploaded/pickable **image asset** (`logoUrl`), Text = a label input; **brand fill** gains a custom **solid/gradient** builder beside the presets (`brandColor` = CSS color or `linear-gradient`); every editable row gets a discoverable **pencil edit** affordance. Revises R32 §C2 | git history |
| R37 | ✓ Done | **Brand-mark centering + asset-pack regeneration** — the jellyfish art sat small/high in its `viewBox`, so every launcher/store raster and in-app inline copy inherited an off-centre glyph. Recentre the **master** `ravilo-mark.svg` (paths unchanged, `viewBox` reframed) + regenerate the whole `assets/**` pack from it; fix the inline marks (app bar, config header, Library menu, create-flows) and flex-align the config-header mark. Follow-up to R22 | git history |
| R38 | ✓ Done | **Launcher-icon optical centering** — the jellyfish reads low in the TV Apps overview (top-heavy dome + thin tentacles → geometric centre below optical centre); nudge the mark up a few % when compositing and regenerate the icon pack. Master SVG + inline marks unchanged. Follow-up to R37 | git history |
| R39 | ✓ Done | **Channel logo fills the button** — an uploaded channel logo covers the whole button (cover / no padding), clipped by the rounded corners, consistent on TV + admin preview/row + mockup. Follow-up to R36 | git history |
| R40 | ✓ Done | **Instant back navigation** — Back re-shows a loading skeleton because `AnimatedContent` disposes the screen + its store; retain store state/cache so re-entry is instant with silent background refresh (Home/Channel/Browse/Detail/Search) | git history |
| R41 | ✓ Done | **TV subtitle tracks (always-zero bug)** — source subtitles from Jellyfin `PlaybackInfo`, fix the malformed (duplicated-id) subtitle URL, and make subtitle selection actually work (`TrackSelectionOverride` / external config); picker lists embedded + external | git history |
| R42 | ✓ Done | **Fix focus-animation viewport jump** — stop bring-into-view re-centring already-visible tiles, reserve scale-overflow space (`trackPadV`), keep the scale draw-only; smooth focus with no screen jump | git history |
| R43 | ✓ Done | **Focus-navigation smoothness** — after R42 the jump was gone but moving focus still felt laggy: the focus shadow/border read animated values as modifier params (recomposition every frame) and the spring was soft. Run the whole focus animation in the draw phase (scale + shadow in `graphicsLayer{}`, ring in `drawWithCache`) → no per-frame recomposition, plus a snappier `StiffnessMedium` spring. Follow-up to R42 | git history |
| R44 | ✓ Done | **Media transport keys** — the remote's physical Play/Pause/Stop/FF/Rew/Next/Prev buttons were inert (only on-screen transport worked). `dpadFocusable` gained a `MediaKey`/`onMediaKey` handler that maps + consumes `Key.Media*` (+ `Spacebar`); PlayerScreen routes them to `togglePlay`/`skip`/`advanceNext`/restart/exit independent of focus + chrome state; Android binds a Media3 `MediaSession` to the ExoPlayer (so the OS + external controllers deliver transport keys, no double-toggle since Compose consumes the key); web wires `navigator.mediaSession` play/pause/seek/stop | git history |
| R45 | ✓ Done | **Entry scroll + focus restore** — screens opened scrolled to a mid-page button and never re-framed the hero, and content rows lost their `trackPadH` left inset after scrolling in and back. The detail actions row + Home hero now snap the scroll to the top via `onFocusChanged` whenever they hold focus (entry + return-up), so the full hero re-frames; the row `BringIntoViewSpec` reveals a start-clipped tile at the left inset (`leading - insetPx`) so item 0 keeps its padding — fully-visible tiles still never move (R42/R43 intact) | git history |
| R46 | ✓ Done | **Human-readable track labels** — the Audio/Subtitles picker showed bare codes ("DAN") instead of Jellyfin's rich `DisplayTitle` (e.g. "Synstolkning") because the `StreamTicket` carried no audio metadata and the player labelled from container `Format.label`/language only. Added an `AudioTrack` list to the ticket sourced from Jellyfin `MediaStreams`, `RaviloPlayer.load` takes it, the Android getter prefers the server label (mapped by audio-stream order) then a humanized language (`languageName()`), and the web actual surfaces the labels. Follow-up to R41 | git history |
| R47 | ✓ Done | **Detail focusables: draw-only focus scale** — R42's viewport-jump fix (focusable at fixed size, scale draw-only) was only applied to Tile/ChannelCard, so navigating Movie/Series detail still jumped: `EpisodeCard`, `SeasonPicker` pills and `RaviloButton` applied `.scale(scale)` (and the button's lift) as an **ancestor** of `.dpadFocusable`, so the focused bounds grew with the spring and the scroll chased them. Moved scale + glow + lift into an inner `graphicsLayer` with the focusable on a fixed-size outer (Tile/ChannelCard pattern); `CastCircle` already scaled a descendant (left as-is). Fixes detail rails + the Home hero buttons. Follow-up to R42/R43 | git history |
| R48 | ✓ Done | **Discover / Top 10 API** (`/api/tv/discover`) — extend per-user `RaviloConfig` with a `discover` block (enabled/source/region/ordered lists); compose Phase 57 charts + Phase 56 acquisition status; `GET /api/tv/discover`, `discover/item/...`, `POST discover/request`; server-decided `discoverAvailable` gating; live `acquisition_changed` over the R33 events socket (FR-RD1) | git history |
| R49 | ✓ Done | **Ravilo TV: Top 10 tab + dedicated detail + request/status** — gated app-bar tab; ranked rows (rank numeral + country weeks-on-chart / global views + trend); rich status indicators (`requested`/`queued`/`downloading`%+`stalled`/`importing`/`available`) that update live; a **separate** DiscoverDetailScreen (no playback/seasons) with a status-driven Request/Watch-Now button (trailers out of scope for now) (FR-RD2) | git history |
| R50 | ✓ Done | **Ravilo config editor: per-user Top 10 lists** — a Top 10 section on the `/ravilo` config screen (R28 chrome): master enable, source + country selects, drag-reorder/toggle list selection from `GET /api/discover/lists`, Radarr-gating link to Settings → Download tools; writes `RaviloConfig.discover`, live-pushed via R33 (FR-RD3) | git history |
| R51 | ✓ Done | **Global-default config + optional per-user override** — replace the per-user-only default (R16): a single **Global layout** every user gets, plus an optional **custom layout** that **fully replaces** it for one user (binary, no piecemeal inheritance). Config screen gains a Global/user **scope switcher** with a create/remove-override flow; store keyed global + full per-user records (FR-RV-G1) | git history |
| R52 | ✓ Done | **Per-page hero carousels + hero naming** — Home keeps its hero; **each channel/collection page** can have its **own optional hero** (opt-in, one per page) built with the shared R32 hero-item builder (items + height + auto-advance); Ravilo TV renders a channel hero. UI says **hero carousel / hero item** everywhere ("featured" removed; facet = In hero / Not in hero). **Hero height clamp now 40–100%** (was R27's 30–100). `HeroConfig` becomes per-placement (FR-RV-H1) | git history |
| R53 | ✓ Done | **Channel editor as a dedicated page + channel-button padding** — promote the Channel/Collection editor from a popup (R36) to a full **in-app page** (sidebar intact, "‹ Back to layout"), so the hero-item picker layers as a normal popup; rows + Library filters stay popups. Channel button gains per-display **padding** (T/R/B/L 0–40px, separate logo/text), live-previewed, collapsed behind an icon that auto-opens when set (FR-RV-C1) | git history |
| R54 | ✓ Done | **Rows config simplification: workbench-only custom rows** — system rows (Continue, Newly Added) fixed at top with enable/disable only; all other rows are workbench-filter rows (no kind picker); `+ Add row` opens workbench directly; default config trimmed to 3 system rows (genre presets removed); legacy GENRE rows migrate to CUSTOM kind on first edit | git history |
| R55 | ✓ Done | **Subtitle rendering surface + embedded text subs** — subtitles never *drew* (bare `TextureView`, no `SubtitleView`/`onCues`) and only external sidecar subs were offered; wire a Media3 `SubtitleView` fed by `onCues` and drop the `isExternal` filter so embedded SRT/ASS/SSA sideload as Jellyfin VTT. Android-only render fix; web already uses `<track>`. Follow-up to R41/R46 (FR-RV-S1) | git history |
| R56 | ✓ Done | **Image-based subtitle parity (PGS/VobSub/DVDSub)** — render image subs like the official Jellyfin client: native in-container where Media3 can, server **burn-in transcode** where it can't, via real Jellyfin `PlaybackInfo`/`DeviceProfile` negotiation (replaces the R14 direct-play `TODO`); subtitle selection re-requests a transcoded stream when burn-in is needed. Follow-up to R55 (FR-RV-S2) | git history |
| R57 | ✓ Done | **Config: Pair-a-TV action + sticky action navbar** — a **Pair a TV** button in the config top bar (beside Save, not under the scope switcher) opens a popup to pick the Jellyfin user + enter the TV's 6-char code (admin side of R03/R15 pairing; device-scoped, orthogonal to R51). The top action bar (Save · Pair · Open Ravilo) is **sticky** so Save stays reachable on the long page (FR-RV-P1) | git history |
| R58 | ✓ Done | **Single global hero height + auto-advance** — drop the **per-channel** hero height **and** auto-advance controls (R52); the **Home hero** settings (height 40–100%, auto-advance) are the one source applied to **every** hero, Home and channel pages alike. Channel Page-hero keeps only its items; `heroHeightPct` + `autoAdvanceSeconds` leave the per-channel `HeroConfig` (FR-RV-H2) | git history |
| R59 | ✓ Done | **Per-channel content rows** — channel editor gains a **Same as Home / Custom** switch (default inherit): a channel can override the Home content rows with its **own ordered set**, built with the shared row workbench (popup over the channel page). System rows still appear unless removed; `ChannelConfig` gains `rows {mode, items}` reusing `RowConfig` (FR-RV-R1) | git history |
| R60 | ✓ Done | **Android phone (mobile) target** — ship Ravilo on a Pixel 9 phone (portrait, touch) from the **same shared Compose codebase**; the only TV lock-in is the thin `:ravilo-android` entry (manifest + Activity), so a new thin `:ravilo-phone` module (or `tv`/`phone` flavor) is all that's needed — no shared-UI fork. Design = **portrait/touch reflow** of the existing system (top tabs, swipe hero, scroll rows, slide-in detail, ranked Top 10, native keyboard); no bottom-nav for v1; responsive `Dimens` over new mockups. TV stays primary (FR-RV-M1) | git history |
| R61 | ✓ Done | **Home rows: reduce system defaults to Continue + Newly Added** — replace the three system rows (Continue, Movies — Newly Added, Series — Newly Added) with two (Continue + Newly Added, all media). `DEFAULT_ROWS` (backend) + `SYSTEM_ROW_DEFAULTS` (editor) trimmed; existing configs gain ✕ delete buttons on the formerly-system split rows via `normalizedRows()` auto-insert of `newly-all` (FR-RV-RD1) | git history |
| R62 | ✓ Done | **Brand recolor: Jellyfin palette + "D · Lit mark" asset regen** — recolored launcher icons (adaptive bg/fg/monochrome + all density `ic_launcher`/`ic_launcher_round`), splash logo and leanback banner copied from the regenerated design asset pack into **both** `ravilo-android` and `ravilo-phone` res; splash `windowSplashScreenBackground` → `#000B25` in both `themes.xml`. Mark shape unchanged; in-app Aurora accent untouched; both modules `assembleDebug` clean (FR-RV-B1) | git history |
| R63 | ✓ Done | **TV performance: right-sized images + tuned image cache + viewport prefetch** — the bottleneck is the image pipeline, not focus/scroll. Backend serves **full-res** images (no `&fillHeight`/`&maxWidth`/`&quality` on any image URL though `PlaybackService` already does it for video); `RemoteImage`/Coil is unconfigured (no placeholder/crossfade, no mem/disk cache, **no SingletonImageLoader on web**); hero re-decodes a full-res backdrop every 7s. Fix: size image URLs per kind at the backend, configure Coil on every target, add placeholder+crossfade (reuse the Tile hash-gradient), right-size the hero. `LazyLayoutCacheWindow` prefetch deferred (needs CMP 1.9). Focus/Tile draw-phase work (R42/43/47) untouched (FR-RV-PF1) | git history |
| R64 | ✓ Done | **Rename "Channels & Collections" → "Channels"** — label-only standardization across the TV app + admin + design mockups. There is no `Collection` model (display-only), so it's a safe string rename, no migration: `section.channels` i18n (en/da/fo), the admin section header (`RaviloConfig.kt:896`), and the `design/` mockups + `ravilo-i18n.js`. Model identifiers (`ChannelConfig`, `channels`, `data-kind="channel"`, ids/CSS) stay unchanged (FR-RV-RN1) | git history |
| R65 | ✓ Done | **Navigate-up: reveal the row title + clear the app bar** — D-pad up to the top content row scrolls the focused tile to `y=0`, hiding the row title ("Action"/"Comedy") and colliding with the 60dp app-bar overlay, because `rememberEdgeBringIntoViewSpec` reveals top-clipped targets at offset 0 with **no top inset** (Home can't use top `contentPadding` — the hero sits under the bar). Fix: add a `topInsetDp` (~94dp = bar + title + headroom) to the vertical spec, tokenize `appBarHeight` in `Dimens`, apply to Home + Channel screens (Discover already compensates via contentPadding) (FR-RV-UP1) | git history |
| R66 | ✓ Done | **Shared channel-button spec + accurate admin preview** — the button is defined twice (Compose `ChannelCard.kt` vs three rough admin approximations) and the admin preview disagrees on size/radius/logo-fit/watermark/gradient-direction/padding. `:shared` is on both consumers' classpath, so a pure-data `ChannelButtonSpec` constants object becomes the single source: TV reads it as `.dp`/`.sp`, admin reads it as CSS px (same aspect ratio + radius ratio, `object-fit:contain` not cover, forced-diagonal gradient, watermark+sheen, applied padding). Rendering can't be shared (Compose≠DOM) but the definition can (FR-RV-CB1) | git history |
| R67 | ✓ Done | **Stop focus jumping to "Home" + stop needless genre reloads** — two bugs: (1) entering Movies/Series snaps focus to Home because `BrowseScreen` requests `navBarFR` which `AppBar` hardwires to nav item 0, ignoring the computed `activeNav`; (2) picking a genre runs `filterByGenre → load()` which goes through `BrowseState.Loading` (un-mounts the focused chip → focus falls back to Home) and refetches `getFacets()` (genres never change per kind). Plus a latent bug: `browse()` has no `genre` param so the filter is a server-side no-op. Fix: entry-focus the active tab, filter genres in place (no Loading, cached facets), and actually send the genre (FR-RV-NF1) | git history |
| R68 | ✓ Done | **Strip ASS/SSA override tags from subtitles** — selected subs show raw `{\an1}`/`{\pos}`/`\N`/`\h` text. Cause: text subs come as Jellyfin VTT (`Stream.vtt`) and Jellyfin's ASS→VTT extractor leaves `{\…}` blocks as literal cue text; Ravilo renders cues verbatim (Android `onCues` passthrough, web native `<track>`), with **zero** cue cleaning (the R17 JASSUB path is unreachable — backend never emits a `.ass` URL). Fix: a shared `cleanCueText` (regex `\{\\[^}]*\}` strip + `\N`/`\h` normalize) applied in Android `onCues` (`cue.buildUpon().setText`) and the web cue path (FR-RV-S3) | git history |
| R69 | ✓ Done | **Player chrome: more compact + modern play/pause** — the woken transport is too big (all sizes are hardcoded literals in `PlayerScreen.kt`): control `Column` `vertical=36dp`, seek `Canvas height(48dp)` for a 12dp bar, button heights 60/54·46·44, `420dp` bottom scrim. And the "yellow" play/pause = a white disc that's the **default-focused** control wearing NOIR's gold focus ring/glow, plus `▶`/`⏸` Unicode glyphs rendering as colored emoji. Fix: shrink the literals (optionally tokenize), use vector icons, restyle play/pause neutral/accent-consistent across all skins (FR-RV-PC1) | git history |
| R70 | ✓ Done | **Real hero artwork in the config schematic preview** — the R28 schematic (`RaviloConfig.kt renderPreview`) draws the hero as a raw `itemId` over a flat gradient, even though `HeroConfig` already stores `displayBackdrop` (TMDB path) + `displayTitle` and the other **three** hero preview surfaces already render the real backdrop. Fix: point the schematic's hero block at `image.tmdb.org/t/p/w300${displayBackdrop}` with the title overlay (fallback to the per-item gradient). No backend change (FR-RV-CP1) | git history |
| R71 | ✓ Done | **"Merge newly added" controls ONE vs TWO Newly-Added rows** — the switch is correctly wired to `mergeNewlyAdded` but behaves like enable/disable. Root cause: R61 made the default a single `newly-all` (`mediaKind=null`) row, but `HomeFeedService.buildRows` merge logic was written to collapse two stored typed rows — so with one row there's nothing to split and both flag states yield one row (flag only greys the editor row). Fix (recommended): keep the single system row but split it at feed-build — `false` → emit "Newly Added Movies" + "Newly Added Series", `true` → one combined; update the editor preview/greying. Not a DB error; no migration (FR-RV-MA1) | git history |
| R72 | ✓ Done | **Detail-screen scroll breathing room + Play/Resume snaps to top** — on Movie/Series detail, focusables scroll flush to the screen edge (the detail screens install the **bare** parameterless `EdgeBringIntoViewSpec` — no peek/inset, unlike Home's `peekDp=80`), and the R45 Play/Resume `animateScrollTo(0)` loses a `MutatorMutex` race with bring-into-view (returning up reveals Play flush at y=0, not the full hero). Fix: give the detail spec a small top inset + bottom peek for all focusables (reuse R65's `topInsetDp`), and make the actions-row snap authoritative (UserInput mutate priority) so focusing Play reframes the full backdrop (FR-RV-DS1) | git history |
| R73 | ✓ Done | **Per-channel item-match count in the Channels list** — `/#/ravilo` Channels shows only a *condition-count* badge (`"2 condition(s) · ALL"`), not how many items match. The R32 workbench live count already does this (`Workbench.kt wbRefreshPreview` → `MediaApi.list(...).total`, where `MediaStore.list` sets `total` = full filtered count). Fix: extract a reusable `countMatching` helper and render a per-channel async count badge in `renderChannels` (using `wbCondsFrom`/`legacyToConds` + the channel's match). No backend change; ANY counts show `≈` until R74 makes them exact (FR-RV-CC2) | git history |
| R74 | ✓ Done | **"Match ANY" must OR conditions (count + Library)** — premise-corrected: ANY already ORs on the **TV feed** (`ConditionEvaluator.matches` used by `HomeFeedService` channels/CUSTOM rows), but it's dropped in the workbench **live count** (`wbRefreshPreview` flattens to per-facet `MediaApi.list`, never sends `wbMatch`) and the **Library** (hardcoded `initialMatch="ALL"`, `onApply` drops it) because `/api/media`→`MediaStore.list` has no `match`/`conditions` param and AND's facets. Fix: route the count + Library through the same `ConditionEvaluator` — add `conditions`+`match` to `MediaStore.list`/`/api/media`/`MediaApi.list`, send them from the workbench + Library; bonus makes `is_none_of`/`not_contains` exact (FR-RV-MM1) | git history |
| R75 | ✓ Done | **Detail hero: audio-track language flags** — show which languages a title can be heard in via a compact `AudioFlagStrip` composable below the meta row in the hero lower-third (both `MovieDetailScreen` + `SeriesDetailScreen`). 20 flag SVGs from lipis/flag-icons bundled as `:ravilo-ui` compose resources (`composeResources/drawable/flag_<cc>.svg`). Backend: `audioLanguages: List<String>` added to `MovieDetail`/`SeriesDetail` DTOs; `DetailService` populates from Jellyfin `MediaStreams` (movie) or first-episode scanned tracks (series). Up to 5 flags + `+N` pill; hidden when none map. On-device test pending (FR-RV-AF1) | git history |
| R77 | ✓ Done | **Player: automatic aspect-ratio correction (letterbox / pillarbox)** — video currently stretches to `fillMaxSize()` regardless of source geometry, distorting every non-16:9 title (scope 2.39:1, flat 1.85:1, SD 4:3, anamorphic). Fix: Android: register a dedicated `Player.Listener.onVideoSizeChanged`, store `VideoSize` in a public `StateFlow`, collect in `PlayerVideoSurface` and apply `Modifier.aspectRatio(dar)` inside a centering `Box` (the existing `Color.Black` PlayerScreen root forms the bars). DAR = `width × pixelWidthHeightRatio / height` (SAR handles anamorphic; rotation auto-applied by ExoPlayer on the TextureView path, no swap). Web: add `object-fit:contain` to the `<video>` element CSS — one-line fix. No user picker. No seam change required (FR-RV-AR1) | git history |
| R76 | ✓ Done | **Backend TV API: eliminate extra Jellyfin round-trips + parallelize fetches** — `tvToken()` called `isTokenValid()` (a live Jellyfin GET) on every route handler; `allItems()` was called twice per detail request; `getResumeItems + getNextUp` ran serially in the Continue row. Fix: (1) cache token validity 5 min in a `HashMap` + `Mutex` — warm cache costs ~μs instead of a Jellyfin RTT; (2) single `allItems()` call per request, result passed to `relatedItems()`; (3) `jellyfinIdIndex` lazy in-memory cache in `MediaStore` for O(1) jellyfinId lookup; (4) `coroutineScope { async {} }` to race `allItems + tvToken` in `DetailService`, `HomeFeedService`, `BrowseService`; (5) `getResumeItems ‖ getNextUp` in `buildContinueRow` (FR-RV-PF2) | git history |
| R78 | ✓ Done | **Detail hero: subtitle-track language flags** — add a second `SUBTITLES: {flags}` row directly under the R75 `AUDIO: {flags}` row on both detail screens, reusing the same component/map/dedup/cap/styling (parameterize `AudioFlagStrip` with a `label`, don't fork). Backend: add `@SerialName("subtitle_languages")` to `MovieDetail`/`SeriesDetail`; populate in `DetailService` from `jfDetail.mediaStreams` filtered `Type="Subtitle"` (movie) or first-episode tracks filtered `TrackKind.SUBTITLE` (series) — no new Jellyfin round-trip. `.distinct()` one flag per language; hide when none map (FR-RV-SF1) | git history |
| R79 | ✓ Done | **Top navbar on Movie/Series detail screens** — the shared `AppBar` (Home·Movies·Series·Top 10·My List + search/avatar) is absent on detail pages; only Back gets you out. Fix: thread `displayName`/`onNavSelect`/`onProfile`/`onSearch`/`discoverAvailable` from `RaviloApp`'s detail branches (Browse reset semantics — `resetTo(Dest.*)` not push — Tab 0→Home, 1→Movies, 2→Series, 3→Discover/MY_LIST, 4→MY_LIST); overlay `AppBar` as last child of the loaded-state `Box` (with inner `Box` wrapping scroll Column), `activeNav=-1`, `scrolled` from detail scrollState. TV focus: `navBarFR` in each detail loaded composable, `onDown→playFR`, `onKeyEvent` UP on hero actions row → `navBarFR`; entry focus stays on Play; `topInsetDp` bumped to `appBarHeight+24`. Back stays single-stage (FR-RV-NB1) | git history |
| R80 | ✓ Done | **Ravilo web: browser Back button + URL navigation** — in the browser build, Back exits the SPA (browser fires `hashchange`, not `Key.Back`) and the URL never changes (nav is in-memory `Dest` stack only). Fix (web target only): new `expect`/`actual` seam in `RaviloRoot.kt` (`pushRoute`/`replaceRoute`/`installHashListener` — no-ops on android); `Dest.toRoute()` maps each destination to a hash path; `push` calls `pushRoute`, `pop` calls `replaceRoute`, `resetTo`/`replaceTop` centralise all formerly-scattered `stack=listOf`/`dropLast+` rewrites; `hashchange` listener drives `pop()` via a `fromHistory` flag guard; initial URL written at boot via `replaceRoute`. Stack stays source of truth; on-device `Key.Back` unchanged (FR-RV-UN1) | git history |
| R81 | ✓ Done | **Detail screen: populate Cast & Crew (from jellystructure data)** — the "Cast & Crew" row (`CastCircle`, `detail.cast` label, rows on Movie/Series/Discover detail) never showed because `DetailService` hard-coded `cast = emptyList()`. Fix: map `MediaItem.cast + MediaItem.crew` (already scanned from TMDB, Phase 76/80) → TV `Person` DTO via a `castFrom(item)` helper — dedupe by `tmdbId`, cast-billing-order first then crew, role = character?:job?:role?:department?:type, image = `image.tmdb.org/t/p/w185{profilePath}`, cap 20. **Zero Jellyfin round-trips** (item already in memory; images from TMDB CDN) — honours "Ravilo off Jellyfin, streaming only". No DTO/UI change needed (FR-RV-CC3) | git history |
| R82 | Done | **Store static playback data at scan time** — persist the static per-file facts the detail path re-fetches from Jellyfin on every open: add `MediaItem.runtime`, `Episode.runtime`, `Episode.jellyfinId` (the playable id — series detail currently calls Jellyfin *only* to map season,ep→id) + `seasonNames`; populate in the scanner (full scan + both re-pulls); serve **movie** audio/sub languages from local `MediaItem.tracks` (parity with series R75/R78). Additive nullable fields, no migration; unblocks R83. Storage only — does not yet drop the Jellyfin calls (FR-RV-DC1) | git history |
| R83 | Done | **Catalog-only detail + bulk playstate endpoint** — make `/api/tv/movie\|series` render from local `MediaStore` with **zero Jellyfin round-trips** (drop `getItemDetail`/`getSeriesEpisodes`), and move detail per-user state to a separate `GET /api/tv/playstate?ids=…` → `{id→{resumeMs,played,playedPct}}` backed by **one** bulk `/Users/{id}/Items?Ids=…&Fields=UserData` (chunked, Semaphore-bounded). **`/api/tv/home` left as-is** — the Continue row stays inline (one R76-parallelized RTT); decoupling it was rejected (would pop in above the catalog and reflow the screen = flicker). Jellyfin stays the **owner** of per-user state, read-only (FR-RV-DC2) | git history |
| R84 | Done | **App two-phase load on the detail screens: instant catalog, late playstate hydration** — render catalog immediately (reuse R33/R40 silent-refresh, no Loading flash) then fold in `/api/tv/playstate` overlays (episode resume bars, watched ✓, movie Play/Resume label — cosmetic since seek is server-side). **Absolute no-flicker rule:** late hydration only fills content inside already-reserved, fixed bounds — nothing appears/resizes/reorders (reserved text lines + fixed-width Play button). Series Play must not target the wrong episode (resume pointer phase-1 or neutral until it lands). Fix the `SeriesDetailScreen` `remember(detail)` season-reset. **Home untouched** (FR-RV-DC3) | git history |
| R85 | Done | **Serve all artwork from jellystructure (cached image proxy)** — replace the direct-Jellyfin image URLs (`JellyfinImageUrl` → `/Items/{id}/Images/…?api_key=…`) with a jellystructure `GET /api/tv/image/{id}/{type}` (public route), serve-from-disk-cache → bounded fetch-on-miss (generalize the proven `Semaphore`-bounded `/api/people/{id}/image` pattern; **FD-ceiling mandatory**); DTO image fields go relative + the app centralizes the `startsWith("/")` prepend in `RemoteImage` (TMDB Discover URLs pass through). **Independent of R82–R84.** Open decisions: image TTL policy (generous+invalidate recommended over short), logo source, image upstream (FR-RV-DC4) | git history |
| R86 | ✓ Done | **Smooth sofa: atomic home-feed cache, cheap filters, token cache** — `HomeFeedService` 5-min feed cache keyed on `(libraryVersion, configHash)`: whole feed (catalog+Continue) returned atomically, stale/expired → rebuild sync — no row popping in after paint. `RaviloDeviceService` 5-min token-validation cache + debounced `updateLastSeen` (at most once/min), invalidated on `unpair()`. `ConditionEvaluator` precomputes `ItemFacets` (lowercased `Set`s) per item per `matches()` call — allocation-free `Set.contains` instead of per-condition lowercasing. `heroIds` hoisted out of the CUSTOM-row loop. `DetailService.getMovieDetail/getSeriesDetail` use `resolveByJellyfinId()` O(1) instead of `allItems().firstOrNull`. Materialised filter→ids table deferred (FR-RV-PF1) | git history |
| R164 | ✓ Done | **Show the IMDb rating + vote count on the detail page** — new `ImdbChip` component renders a compact **IMDb ★ <rating> · <votes>** chip in the Movie/Series detail hero meta row (after the certification badge, before the R142 ✓ Watched chip on Movie), gated on `detail.imdbRating` (Phase 131) being present — hidden entirely when absent, with an accessible `contentDescription` carrying the full "IMDb X.X out of 10, N votes" text. Rating is one-decimal 0–10 (`8` → `8.0`); votes abbreviated (`24.8K` / `1.2M`, `fmtVotes`-equivalent trim-trailing-zero logic). Series shows the **show**-level rating (no per-episode). **Render-only** — server-pushed via the DTO; Ravilo never calls imdbapi.dev. IMDb shown as a plain "IMDb" text mark (gold `#F5C518` chip) + ★, not the logo artwork. | [spec](specs/ravilo/requirements/phase-R164-imdb-rating.md) |
| R165 | ✓ Done | **Remove the Home hero parallax (scroll-linked backdrop drift) (FR-RV-PX1)** — the Home hero backdrop drifted vertically with the content list's scroll (a parallax added in R91); dated, unnecessary motion — removed so the backdrop stays put under the scrolling rows. Deleted the one effect — `translationY = -scrollOffsetPx() * HERO_PARALLAX_FACTOR` at `HeroCarousel.kt:209` — plus its plumbing: the `scrollOffsetPx` param (`:73-74`), the `HERO_PARALLAX_FACTOR` const (`Motion.kt:54-55`), and the call site (`HomeScreen.kt:209-212`). **Kept** the time-based Ken Burns zoom sharing the same `graphicsLayer` (only the `translationY` line was deleted; `clipToBounds` stays). `ChannelScreen` already passed no `scrollOffsetPx` (unaffected); focus-scale/lift (R42/R43/R47) is NOT parallax and is untouched. | [spec](specs/ravilo/requirements/phase-R165-remove-hero-parallax.md) |
| R166 | ✓ Done | **Upcoming: episode-level "Already available" + localized library titles (FR-RV-UP1)** — two backend-only R160 correctness fixes, both from the same shortcut (a matched item borrows only the *fact* of a catalogue match, none of its data). **(a)** an upcoming episode is flagged "Already available" whenever we hold the *series*, not that specific S/E — fix checks `matched.episodes.any { season+episode match }` (the episode list is already decoded on the matched `MediaItem`), decoupling the episode-level `held` pill from the series-level `itemId` routing (holding the series still opens the real detail). **(b)** titles show Sonarr's English string ("Chore Captain (DK)") instead of our library's localized `MediaItem.title` ("Mesterholdet", resolved per the constitution's audio-track language algorithm) — prefer `matched.title`/`overview`/`genre` when matched, fall back to the *arr string only when not held. No app rebuild (the feed carries corrected `status`/`title`). | [spec](specs/ravilo/requirements/phase-R166-upcoming-episode-availability-and-localized-titles.md) |
| R167 | ✓ Done | **Upcoming: openable metadata detail + poster art for not-held items (FR-RV-UP2)** — items we don't hold (a movie, or a series with zero episodes) get a flat gradient (no poster) and open a bare, near-empty detail — "it just fails" for the viewer. Deliver the Discover-parity treatment the user asked for: **posters/backdrops loaded by the client directly from the external CDN** (the *arr calendar's currently-dropped `images[].remoteUrl`, forwarded on `UpcomingItem` — **no jellystructure image proxy**, so R160's deferred FD-surface concern doesn't apply), and a **metadata-rich not-held detail** (backdrop, synopsis, genres, runtime, cast — via a live TMDB enrichment like `DiscoverDetail`) that never renders a blank/error screen. Held items keep their on-disk `posterUrl` (R133) + the real detail; routing (R160 §G) unchanged; no request/acquire action (R160 fence). Picks up R160 dev-review addendum #3. | [spec](specs/ravilo/requirements/phase-R167-upcoming-notheld-detail-and-posters.md) |
| R168 | ✓ Done | **Upcoming: honor Radarr "Minimum Availability" for the "Missing" section (FR-RV-UP3)** — "Missing from your library" over-reports because it flags a movie missing whenever *any* past release date (incl. a **theatrical** date) has passed, ignoring Radarr's own availability judgment. A film in cinemas months ago but with no physical/digital release yet, whose min-availability is "Released" (so Radarr won't grab it), still shows as Missing. Fix: parse `minimumAvailability` + use the already-declared-but-dead `isAvailable` from the Radarr calendar (both returned by default — `ignoreUnknownKeys` currently drops them), flag Missing only when Radarr itself deems it grabbable, stop equating a cinema-only past date with physical/digital in `pickMovieRelease`, and add a small air-date grace window for Sonarr episodes. Per-movie from the API, no config; backend-only. | [spec](specs/ravilo/requirements/phase-R168-upcoming-minimum-availability.md) |
| R169 | ✓ Done | **Ravilo web player: keep the video visible while the controls are shown (FR-RV-WP2)** — web only: showing the transport overlay turns the whole video area **black**; hiding it restores the picture. Root cause (R157's `opaque=false` premise is now outdated — this CMP version's `CanvasBasedWindow` has no such param): the DOM `<video>` is z-index-swapped around an **opaque** canvas by chrome visibility (`RaviloPlayerWasm.setChromeVisible`), so "show controls" sinks the *entire* frame behind the opaque black canvas instead of dimming only where the scrims are. Fix: real video-under-Compose compositing via a **transparent canvas** (re-evaluate `ComposeViewport`/skiko transparency in CMP 1.9.3 — the blocker was `CanvasBasedWindow`-specific; a migration TODO already flags it) so the scrims composite over the live video exactly as on Android; fallback = DOM-overlay chrome above an always-promoted video (R163 precedent). Also kills the initial-open black flash. Android/TV (in-scene TextureView, `setChromeVisible` no-op) untouched. | [spec](specs/ravilo/requirements/phase-R169-web-player-video-behind-chrome.md) |
| R170 | ✓ Done | **Profile-hub nav + unified Discover (Coming Soon / Request)** — trimmed the TV AppBar section tabs to **Home · Movies · Series · Discover** (one optional slot, was two); moved personal destinations (**My List · Settings · Switch profile · Unpair**) under a new avatar dropdown (`ProfileMenu.kt`, D-pad navigable, reuses `SettingsScreen`'s `UnpairConfirmOverlay` + a new shared `unpairAllSessions()`). `UpcomingScreen`/`DiscoverScreen` are kept as the two **segments** of one merged `Dest.Discover(displayName, segment)` destination — each screen still owns its full AppBar+content (no chrome-sharing rewrite) but gained a small `DiscoverSegmentPill` "↔ switch" affordance, shown only when the *other* segment is also available; switching uses `replaceTop` (same `Dest` class ⇒ no slide transition). Discover shows when Coming Soon (Sonarr/Radarr) **or** Request (Seerr, via `configStore.current.seerr?.enabled` — `TvRoutes.kt`'s `/tv/discover`) is available. **Scope note:** the Request segment's actual grid is still chart-shaped and empty (Phase 136 retired its data source); it now shows a "nothing to request yet" placeholder instead of a bare empty header — real Seerr-backed rows are R171's job, not re-done here. | [spec](specs/ravilo/requirements/phase-R170-profile-hub-discover-merge.md) |
| R171 | ✓ Done | **Seerr-powered Request tab (browse · request · search)** — inside Discover (R170), the **Request** tab is a full **Jellyseerr/Overseerr** surface: one horizontal rail **per configured Seerr discover feed** (Phase 137) of **plain poster tiles** (poster · status badge · title · `year·genre·rating` — **no** rank / weeks / trend / "why it's trending" chart framing), a request lifecycle from server state (**Request** → requested → queued/in-progress → **✓ In Library** / not-requested, per-user permission), and a **Search Seerr** pill opening a dedicated on-screen-keyboard search **scoped to the Seerr catalogue only**. Render-only + request-intent-only; Jellystructure proxies Seerr via a new `SeerrDiscoverService` (client never calls Seerr directly) — no persisted acquisition store or background reconciler; every fetch derives each tile's status live from Seerr's own embedded `mediaInfo`. Replaces the retired **R48–R50** chart request flow (`ChartEntry`/`ChartListSpec`/`Chart.kt` deleted; `RequestEntry` replaces `ChartEntry`). Backend: `SeerrClient` gained discover/search/movie-detail/tv-detail/create-request methods against Seerr's documented API; `/tv/discover`, `/tv/discover/item/{mediaType}/{tmdbId}`, `/tv/discover/request`, and new `/tv/search/seerr` all repointed at Seerr. App: `DiscoverScreen`'s `RankTile` → plain `Tile` (which gained an optional badge-color param) + status badge; `DiscoverDetailScreen` dropped "Why it's trending"; new `SeerrSearchScreen`/`SeerrSearchStore` (a deliberate parallel to `SearchScreen`, not a generalization of it). | [spec](specs/ravilo/requirements/phase-R171-seerr-request-tab.md) |
| R172 | ✓ Done | **Request-language picker + change-later in the Request flow (FR-RV-RL1)** — TV half of request-language steering (backend = Phase 139). Instead of a bare **Request** press, a new `RequestLanguagePicker` popup (reusing the player **TrackPicker** visual style, with normal D-pad focus traversal via `dpadFocusable` rather than the player's manual index — this screen doesn't own exclusive input — and `AudioFlagStrip`'s `LANG_CC` flag lookup, made `internal` for reuse) offers **Original** vs **Dansk/Nordic**, with the viewer's **default pre-selected/pre-focused** so Select is usually one confirm; 0–1 catalog languages ⇒ no popup at all (`DiscoverDetail.hasLanguageChoice()`). Per-viewer **default request language** in the config editor (`RaviloConfig.kt`'s behaviour section, cloning R162's `uiLanguage` override pattern exactly — new `beh-u-reqlang` field, options loaded once from `/api/config`); kids default lives in the Settings ▸ Request languages card (`kids_default`, global). Status chip on `DiscoverDetailScreen` carries the **chosen flag** through requested→downloading→available; a **strict, still-waiting** request shows "Waiting for a Dansk release" + a **Change language** action → `POST …/request/{kind}/{tmdb}/language`. A new **In progress** rail (`DiscoverStore.myRequests`) on the Request tab lists the viewer's not-yet-available requests, dropping off once AVAILABLE, so a strict pick from days ago is easy to find and switch. en/da/fo strings added. Verified via real `:ravilo-ui:compileKotlinWasmJs` **and** `:ravilo-android:compileReleaseKotlin` (the actual TV release target) — both clean. Built, AOT-compiled (`compile -m speed`), and deployed (release-only, no debug leftover, never launched) to the stue TV. | [spec](specs/ravilo/requirements/phase-R172-request-language-picker.md) |
| R173 | ✓ Done | **HDR/HLG tone-map negotiation (bug fix, FR-RV-HDR1)** — a viewer reported an HDR10+ movie ("Overcast", confirmed via `ffprobe`: HEVC 10-bit, `smpte2084`/`bt2020`) played very dark on Ravilo but perfectly through Jellyfin's own client. Root cause, verified live against the real Jellyfin server: jellystructure's `DeviceProfile` sent to `PlaybackInfo` (R56) had **no `CodecProfiles` entry**, so nothing ever disqualified an HDR stream from direct play (`PlaybackInfo` confirmed `SupportsDirectPlay: true` for the raw HDR10Plus file); `PlaybackService.startPlayback()`'s `capabilities` parameter was received but discarded (`@Suppress("UNUSED_PARAMETER")`), and `ClientCapabilities` had no HDR field to carry such a signal anyway. Fix: `ClientCapabilities` gains `supportsHdr10`/`supportsHlg` (default `false`, conservative); `JellyfinClient.kt`'s device profile is now built per-request with a `CodecProfiles`/`VideoRangeType` condition — verified live to correctly flip Jellyfin to a tone-mapped SDR transcode (`TranscodeReasons=VideoRangeTypeNotSupported`, `h264-rangetype=SDR`) when unsupported, and to preserve direct play when the client declares real support; `PlaybackService` now passes `capabilities` through (also fixed a latent positional-arg break this surfaced in the burn-in `restream()` call site). Ravilo Android gains a `detectHdrSupport()` seam querying the real `Display.HdrCapabilities`; wasmJs conservatively reports no HDR support (no browser detection path exists). No Dolby Vision (no DOVI pipeline exists) and no client-side tone-mapping fallback — out of scope. Verified via `compileKotlinLinuxX64` + admin `compileKotlinWasmJs` + `:ravilo-ui:compileDebugKotlinAndroid` + `:ravilo-web:compileKotlinWasmJs` + `linuxX64Test` (14/14); **not yet on-device verified** (needs a backend relaunch + Ravilo APK rebuild, deliberately not done this session). | [spec](specs/ravilo/requirements/phase-R173-hdr-tonemap-negotiation.md) |
| R174 | ✓ Done | **Configurable poster-grid columns (landscape + portrait, FR-RV-PT2)** — tiles-per-row on the all-movies/series Browse grid and both Search grids (catalog + Request/Seerr) become operator-set instead of three hardcoded constants (was `GRID_COLS=6`, `GRID_COLS_SEARCH`/`_SEERR_SEARCH=5`, the latter also driving edge-exit focus math). New `RaviloConfig.grid_columns` (landscape, default 6, clamped 2–10) + `PortraitConfig.grid_columns` (portrait, default 2, clamped 1–4; `null` = the built-in 2 — a phone wants fewer columns than the TV, unlike R159 hero height whose null inherits the landscape value). Delivered on the existing `/api/tv/config` payload (these screens read neither the feed nor `tileShape`), applied via two new `LocalGridColumns`/`LocalPortraitGridColumns` composition locals selected by R159's `LocalPortrait` — rotation flips it live; presentation-selection, not derived state (same class as `uiDensity`). Config editor (`RaviloConfig.kt`): a landscape "Items per row" slider in the global Behaviour card + a portrait one in the R159 Portrait section (not gated by its hero toggle; stored `null` at the default 2 so the portrait block only persists when it carries a real override). Verified via `compileKotlinLinuxX64` + `:ravilo-web:compileKotlinWasmJs` + admin `compileKotlinWasmJs` — all clean; **not on-device verified** (needs a Ravilo APK rebuild + deploy, deliberately not done). Extends R159 as that phase intended ("the home for future portrait-only options"). | [spec](specs/ravilo/requirements/phase-R174-grid-columns-config.md) |
| R175 | ✓ Done | **Ravilo username/password login screen — replaces code pairing (FR-RV-AUTH1)** — the client half of Phase 141: the TV/phone stops showing a pairing code and instead shows a username + password **LoginScreen** — dev-review correction: no reusable on-screen keyboard existed (Search uses the native IME), so this uses `BasicTextField` + `PasswordVisualTransformation` (native IME), matching Search's own pattern — that posts to the new `POST /api/tv/login` proxy via a new `TvApiClient.login()`; on success it stores the device token + appends **and switches to** a `LocalSession`. A new per-device `DeviceIdStore` (wasm localStorage / Android SharedPreferences) mints a stable id once, sent on every login — the identity every profile on a shared TV needs to avoid Phase 141's per-DeviceId pruning fix. Wires into the existing multi-user plumbing (`MultiTokenStore`/`ProfilePicker`/`ProfileMenu`) — a new **`ProfileMenu → Add user`** row + `ProfilePicker`'s existing tile both re-open the same screen; each login is another profile, switched to immediately. Restricted users render whatever the server serves (no client-side ACL — filtering is Phase 142). Replaces `PairingScreen`/`PairingStore`/`startPairing`/`pollPairing`/`PairingChallenge`. Verified: `compileKotlinLinuxX64`, admin `compileKotlinWasmJs`, `:ravilo-ui:compileKotlinWasmJs`, `:ravilo-android:compileReleaseKotlin`, `:ravilo-web:compileKotlinWasmJs`, `:ravilo-phone:compileReleaseKotlin` all pass; on-device sign-in not yet tested (no TV deploy this session). | [spec](specs/ravilo/requirements/phase-R175-login-screen.md) |
| R176 | ✓ Done | **Home-feed playstate: cache, concurrent fetch, cross-device live sync (bug fix)** — a profile switch consistently took 4.5–5.8s, measured live (Pixel 9 + bedroom TV), not decreasing on repeat visits. Root cause: `getHomeFeed()` paid a live Jellyfin playstate round trip on **every** call (hit or miss on the structural feed cache — its key deliberately excludes playstate), and on a structural-cache miss paid a **second**, sequential live round trip first (`buildHomeFeed`'s own Continue-row fetch) — worst case ≈8.5s end to end. Fix: new short-TTL (20s) `playstateCache` scoped to the user's **entire visible catalog** (not just one feed's rows — this removes playstate's data dependency on the built feed, which is what unlocked the next part) in `HomeFeedService.kt`; the structural-feed build and the playstate fetch now run **concurrently** (`async`/`await`) in both `getHomeFeed()` and `getChannelFeed()` instead of sequentially; `PlaystateHydrator.fetchPlaystate()`'s chunk loop is now `async`-fanned-out instead of one-at-a-time (benefits every caller — feed/browse/search/related, not just Home). Third piece: whenever a **live** fetch actually happens (never on a cache hit), the result is broadcast to every OTHER device signed in as that user via a new `TvEventBus.notifyPlaystateChanged` → `playstate_changed` WS event (shared `PlaystateChangedEnvelope`) → `TvApiClient.connectEvents`'s new `onPlaystateChanged` callback → `RaviloApp.kt` republishes it on the existing R147 `WatchedBus`, so an already-open Home/Browse/Search screen elsewhere patches its tiles instantly instead of independently paying its own round trip. Verified via `compileKotlinLinuxX64`, `linuxX64Test`, admin `compileKotlinWasmJs`, `:ravilo-ui:compileKotlinWasmJs` + `:ravilo-ui:compileReleaseKotlinAndroid` — all clean. | [spec](specs/ravilo/requirements/phase-R176-home-feed-playstate-cache-live-sync.md) |
| R178 | ✓ Done | **Player: D-pad Select must never re-trigger a hidden control (FR-RV-SEL1)** — reported live: pick a subtitle track, watch for a while, chrome auto-hides as designed, but the hand-rolled `focus: PlFocus` state (`PlayerScreen.kt` doesn't use real Compose focus nodes — only the root `Box` does; every button just tracks a plain enum var) was never reset when chrome hides. Pressing Select/OK later re-dispatched on that stale value (e.g. reopened Audio & Subs) instead of the expected play/pause. Fix: a `hideChrome()` helper (`chromeVisible = false; focus = PlFocus.PLAY`) used at all three chrome-hide sites (auto-hide timer, `onBack`'s hide-chrome branch, `onTap`'s toggle-chrome branch), plus a belt-and-suspenders guard in `onSelect` — `chromeVisible` captured *before* `wake()` (which unconditionally reveals it), so if chrome was hidden when Select fired, `togglePlay()` runs directly, never the stale `focus`-based `when`. Verified live on the Pixel 9: focused Audio & Subs, let chrome auto-hide (~4.5s), pressed Select — now shows the play/pause flash + reveals chrome instead of reopening the picker. | [spec](specs/ravilo/requirements/phase-R178-player-select-key-stale-focus.md) |
| R177 | ✓ Done | **Ravilo: watch Live TV (guide · Now/Next · zapping · player) (FR-RV-LTV1)** — the TV side of Phase 147's Live TV. `TvApiClient` gained `getLiveTvChannels`/`getLiveTvGuide`/`tuneLiveTv`/`liveTvHeartbeat`/`stopLiveTv`; `HomeFeed` gained a `liveTvHome: LiveTvHomePlacement?` field (threaded through `HomeFeedService.buildHomeFeed`) so the client knows placement without a second round-trip. **Home "On now" row**: `HomeStore` polls `getLiveTvChannels()` every 60s only while `feed.liveTvHome.showOnNowRow` is true (own poll loop, independent of the coarser R33-pushed structural feed — program info rolls over at each boundary, same rationale as Continue Watching's own live fetch); `HomeScreen` interleaves it into `feed.rows` at `onNowRowPosition` using the existing `StaticContentRow`/`Tile` components (`Tile.progressPct` reused as-is for live elapsed-time), with an "Open TV Guide" `seeAll` action. **Live player** (`LiveTvPlayerStore`/`LiveTvPlayerScreen`, new `Dest.LiveTv`) is a deliberate SIBLING to `PlayerStore`/`PlayerScreen`, not a branch of it — confirmed via research that the 1700-line VOD player is saturated with finite-item assumptions (next-episode, episode rail, scrub/seek, restream, mark-played) that don't apply to an infinite, no-resume live stream; only the byte-streaming engine (`RaviloPlayer`/`PlayerVideoSurface`, already HLS-capable) is reused. Implements: LIVE badge, an auto-hiding channel bar (logo/number/name/category/current+next program/live progress bar), ←/→ zap with a channel banner (computed from the already-loaded lineup, not a round-trip), 0–9 number-entry tuning (auto-commits after a pause), and ↑ for a Now/Next mini-guide overlay (D-pad list, Select tunes). **EPG guide** (`LiveTvGuideStore`/`LiveTvGuideScreen`, new `Dest.LiveTvGuide`): channel-level category filter chips (dev-review addendum B — Jellyfin has no per-program category data), one horizontally-scrollable program row per channel sized proportionally to duration, current program highlighted with its live progress. **Scope cuts, called out explicitly (not silently dropped):** (1) the guide is a per-channel scrollable-row grid, not a pixel-aligned sticky-time-header/single-shared-timeline grid (a real but modest follow-up, no existing 2D-grid focus utility to build on — flagged by research); (2) per-user Live TV channel access (spec §E/addendum D — channels aren't MediaItems, Phase 142's `visibleTo` doesn't apply) is **not enforced** — every device currently sees the same globally-shown lineup; a kid-safe allow-list is a documented follow-up, mirroring Phase 143's own AllowedTags/BlockedTags enforcement deferral; (3) timeshift/pause-live (§D4, already flagged optional/best-effort in the Phase 147 addendum) is not implemented — live is watch-only, jump-to-live doesn't apply since there's no buffer to rewind. Verified via `compileKotlinLinuxX64`, admin `compileKotlinWasmJs`, `:ravilo-ui`/`:ravilo-web:compileKotlinWasmJs`, `:ravilo-android`/`:ravilo-phone:compileDebugKotlin`; not on-device/live-Jellyfin verified this session (no dev-server restart, per standing preference, and no tuner hardware in this pass). | [spec](specs/ravilo/requirements/phase-R177-livetv-viewing.md) |
| R180 | ✓ Done | **Flag-forward Audio & Subtitles picker (FR-RV-ASP1)** — the in-player track picker redesigned so no technical knowledge is needed to change soundtrack/subtitles: every row is a country flag + plain native endonym + jargon-free badges only (`Default` · `Surround 5.1`/`Stereo` · `Signs only` · `Sound described` · `Describes action` · `Commentary` · `Dubbed`), no codec/delivery-method/latency text anywhere (`PlayerScreen.kt` `PickerOption`/`TrackPicker`, endonym map + title-parsed badges, scroll-follow, per-tab selected-track flag). Landed across 6 commits (`1b72d17`, `be71b08`, `81ceea2` + the shared R181 stages). | [spec](specs/ravilo/requirements/phase-R180-audio-subtitle-picker-overhaul.md) |
| R179 | ✓ Done | **Multi-episode file as one combined card (FR-RV-MEF1)** — the Ravilo side of Phase 149. Series detail groups a multi-episode file into one combined triptych card on both TV and phone (`episodeGroups()` in `SeriesDetailScreen.kt`, mirrored in `ravilo-phone`). | [spec](specs/ravilo/requirements/phase-R179-multi-episode-file-card.md) |
| R181 | ✓ Done | **Default & remembered audio/subtitle tracks (FR-RV-TRK1)** — layered resolution (per-series remembered choice → learned global language → source default → first track), persisted **client-local only** (never sent to jellystructure) via a new `PlaybackPrefsStore` (Android SharedPreferences / Wasm localStorage), keyed per profile. `seriesId` + `originalLanguage` threaded end-to-end for the "Dubbed" badge and per-series memory. Landed across 4 commits (`bb25bad`, `fecfa1f`, `1b72d17`, `634c662`). | [spec](specs/ravilo/requirements/phase-R181-default-and-remembered-tracks.md) |
| R182 | ✓ Done | **Skip Intro / Skip Credits (FR-RV-SKIP1)** — a visibility-timed **Skip Intro** pill (`positionMs` inside `[introStartMs, introEndMs)`; a brief grace countdown governs its own tuck-away, then rides the same show/hide seam as chrome, reappearing on wake() until the intro ends) and a **stinger-aware credits card** retargeted from the old fixed end-of-file heuristic to the real `creditsStartMs` (falling back to the pre-R182 heuristic when unscanned). Card priority: **Skip to scene** (a TMDB stinger — never auto-skipped past) → **Next Episode** (unchanged 8s-countdown auto-advance) → **Skip credits** (movie/last-episode, which previously exited the player silently with no card at all at natural end); always plus **Watch credits**, never more than two buttons, movies never show Next Episode. Behaviour (`skipIntro`/`skipCredits`: Off/Prompt/Auto; `skipSecs`: 4/6/8, replacing the old hardcoded `COUNTDOWN_SECS=8`) is configured **jellystructure-side only** (admin Ravilo-config **Behaviour & preferences**, global + per-user, mirroring the existing `autoplayNext` field exactly — no in-app settings screen), reached via a new `PlayerStore.getConfig()` → `TvApiClient.getConfig()`. Fixes a latent bug in the process: `autoplayNext` was persisted but never actually consulted by the countdown's auto-advance. | [spec](specs/ravilo/requirements/phase-R182-skip-intro-credits.md) |
| R195 | ✓ Done | **Telling apart subtitle tracks that share a language (FR-RV picker)** — design-authored 2026-08-09, dev-reviewed + built 2026-08-10. R180's flag-forward picker assumed one row per language = one choice; live library data says otherwise (46.5%/47.5% of movies/series have 2+ same-language subtitle tracks in a file, 251 file-instances fully indistinguishable, worst case 32 in one file). Picker is now two levels (`pickerLevel`/`pickerVersionIdx` alongside the existing `pickerIdx`, `PlayerScreen.kt`): language rows (arrow + count only when >1 version, no arrow = OK selects outright) → a version list with the flag shown once in the header, each version carrying a plain-language sentence instead of a raw title. `REGION_TABLE` (flags; no `flag_br`/`flag_tw` asset yet — text-only fallback, never a wrong flag), conservative `PROVENANCE_RE` collapse (only when a member has a provenance marker AND all have title text — never blind-merges untitled tracks), widened `SDH_RE`. `RememberedChoice` gained `audioVariant`/`subtitleVariant` (an opaque `kind|region|ordinal` signature) so "always pick SDH" survives to the next episode — `resolveTrackSelection()` tries the signature first, falls back to the old language-only match. **Real gap found + fixed mid-build**: a null-language cluster (untagged tracks) had no language to name its row after (`pickerName(null,"")` → blank) — new `groupDisplayName()` falls back to the cluster's dominant kind word, never raw title text. Every row also gained real tap/touch handling for the first time — the shipped R180 flat picker had none at all, D-pad-only; a tap now runs the exact same `pickerSelect()`/`pickerBack()` the D-pad's Select/Back call, so TV and phone can't diverge. Bazarr `hi`-flag plumbing (untitled-track SDH) intentionally deferred as its own follow-up — confirmed genuinely new backend work, not blocking this phase. Verified via all 5 Ravilo compile targets + `compileKotlinLinuxX64`/`linuxX64Test`. Not yet live-tested on-device. | [spec](specs/ravilo/requirements/phase-R195-same-language-subtitle-picker.md) |
| R194 | ✓ Done | **Media-session artwork uses the season poster, not an episode still (FR-RV-SESS3)** — direct follow-up correcting R193's artwork priority: series playback now prefers the current episode's SEASON poster over an episode still, falling back to the series' own poster (never an episode still) when the season has none. New backend route `GET /api/tv/image/{itemId}/season/{season}/poster` (`RaviloArtworkService.serveSeasonPoster`, `ArtworkDownloader.seasonPosterPath` widened to `internal`); `Season.posterUrl` (new, null — not a 404-prone URL — when the season has no poster on disk, since a native `MediaMetadata.setArtworkUri` has no fallback of its own); `DetailService` gained an `ArtworkDownloader` param to check season-poster existence. Client: `PlayerEpisodeEntry.seasonPosterUrl` + `EpisodePlayContext.seriesPosterUrl` (threaded into the existing R193 `Dest.Player.posterUrl`, carried through auto-advance for the whole binge); `PlayerScreen`'s artwork resolution is now `seasonPosterUrl ?: posterUrl` with no `stillUrls` in the chain; both `PlayerScreen`/`LiveTvPlayerScreen` gained a `resolveImageUrl()` to prefix relative `/api/tv/image/...` paths against the server base URL (the OS media session bypasses Coil's own resolution). Verified via `compileKotlinLinuxX64`, `linuxX64Test`, and all 5 Ravilo compile targets — all pass. Not on-device verified (backend restart + deploy are user-initiated). | [spec](specs/ravilo/requirements/phase-R194-mediasession-season-artwork.md) |
| R193 | ✓ Done | **Rich media-session metadata (title/episode/artwork), scoped to TV only (FR-RV-SESS2)** — direct follow-up to R192: once the TV's session card only appears while genuinely watching, enrich it (title, "S1 · E3"-style episode kicker as subtitle — Media3 has no numeric season/episode fields — poster/still artwork) so a household member's phone shows what's actually playing instead of generic "Ravilo"/"Stue TV". Also closes a real scoping question: `RaviloAppContext.isTelevision` (new, runtime `UiModeManager`/`Configuration.uiMode` check, not a build flavor) gates `RaviloPlayerAndroid.ensureMediaSession()` to TV hardware only — the shared `androidMain` code between `:ravilo-android` and `:ravilo-phone` means the phone app would otherwise inherit the exact same cross-device-advertising capability; decided (user's explicit call) that the phone never creates a `MediaSession` at all, trading away Bluetooth/headset transport-button support there for a stronger "never visible to other devices" guarantee, since Media3 has no public API for a session that's locally-active-but-not-cross-device-visible. `RaviloPlayer.load(...)` gained `title`/`subtitle`/`artworkUrl` (web actual accepts but ignores them — no cross-device concern there, out of scope). Verified via `:ravilo-ui:compileDebugKotlinAndroid`, `:ravilo-ui:compileKotlinWasmJs`, `:ravilo-web:compileKotlinWasmJs`, `:ravilo-android:compileDebugKotlin`, `:ravilo-phone:compileDebugKotlin` — all pass. Not on-device verified (deploy is user-initiated). | [spec](specs/ravilo/requirements/phase-R193-mediasession-rich-metadata-tv-only.md) |
| R192 | ✓ Done | **Release the OS MediaSession when the player backgrounds, not just on screen exit (FR-RV-SESS1)** — reported live: minutes after watching on the "stue" TV, with the TV powered off, the viewer's phone still showed a system Cast/media-control card mirroring it (Android's built-in cross-device session surfacing, same Google account — no custom Cast code exists in this repo). Root cause: `RaviloPlayerAndroid`'s `MediaSession` (R44, for hardware transport keys) was only released via `PlayerScreen.kt`'s `DisposableEffect(Unit) { onDispose { player.release() } }`, which fires only when the Compose player screen leaves composition — never on `ON_STOP` (TV sleep/HDMI-CEC power-off/backgrounding), the far more common way a TV session actually ends; `exo.pause()` updated the reported playback state but left the session itself "active" and advertised indefinitely. Media3's `MediaSession` has no public `isActive` setter, so the fix is release-and-recreate-on-demand: `mediaSessionLazy: Lazy<MediaSession>` → a nullable `mediaSessionRef` + `ensureMediaSession()`; new `RaviloPlayer.setSessionActive(Boolean)` (Android: release/recreate the session only, never the still-alive `exo` player; web: no-op) wired into `PlayerLifecycleEffect`'s existing `ON_STOP`/`ON_START` branches, alongside the pre-existing backend session stop/re-arm calls. Verified via `:ravilo-ui:compileDebugKotlinAndroid`, `:ravilo-ui:compileKotlinWasmJs`, `:ravilo-web:compileKotlinWasmJs`, `:ravilo-android:compileDebugKotlin`, `:ravilo-phone:compileDebugKotlin` — all pass. Not on-device verified (deploy is user-initiated). | [spec](specs/ravilo/requirements/phase-R192-mediasession-background-cleanup.md) |
| R183 | ✓ Done | **Dolby Vision playback + a decodable transcode fallback (FR-RV-DV1)** — reported live: "Contact Week" (2160p DV profile 8 / HDR10+ hybrid, 26.6 GB) never produced a frame in Ravilo while Jellyfin's own TV client plays it fine. Two independent defects in one negotiation, both in `deviceProfile()` (`JellyfinClient.kt`), both root-caused and re-verified against the live server (Jellyfin 10.11.11). **(1)** R173's `VideoRangeType` allow-list was `SDR|HDR10|HDR10Plus|HLG` and **no Dolby Vision range type was ever in it** (R173's explicit non-goal), so all 30 DV titles in the library were permanently forced to transcode however capable the client was — even though DV **profile 8** is single-layer, its base layer *is* a conformant HDR10/HDR10+/HLG/SDR stream, and the DV metadata rides in RPU NALs every decoder ignores. New `allowedVideoRangeTypes()` mirrors Jellyfin's own Android TV client rule-for-rule: always `SDR` + `DOVIWithSDR`; `HDR10`/`HDR10Plus` + `DOVIWithHDR10`/`DOVIWithHDR10Plus` with `supportsHdr10`; `HLG` + `DOVIWithHLG` with `supportsHlg`; `DOVI` (profile 5, no HDR10 base layer) only behind a real DV decoder; `DOVIWithEL`/`DOVIWithELHDR10Plus` (profile 7, dual-layer) only behind DV + multi-instance HEVC; `DOVIInvalid` never. **(2)** the fallback transcode was itself undecodable — with **nothing** declared about the H.264 target, Jellyfin advertised `CODECS="avc1.424029"` (Baseline **level 4.1**) at the source's full `3840x1606`, so ExoPlayer dropped the only HLS variant and preparation failed before the first frame (this hit *every* transcode, not just DV — an HDR10+ title on ravilo-web had the same broken fallback). New `h264TargetConditions()` declares `VideoProfile` + `VideoLevel` + `Width`/`Height` from the client's real decoder ceiling (`IsRequired=false`), turning it into `avc1.640033` (High/L5.1); unreported limits fall back to a fleet-safe 1080p High/L5.1. Deliberately **no `VideoBitrate` condition — verified live that one also disqualifies direct play of any source above it. Client side: `detectHdrSupport()` now also reports `dolbyVision` (a `video/dolby-vision` decoder) and `dolbyVisionEl` (`dvhe.07` **and** `maxSupportedInstances ≥ 2`), a new `detectAvcDecoderLimits()` seam reports the real AVC width/height/level (wasmJs → `UNKNOWN`, so ravilo-web gets the safe target), and both `PlayerStore`/`LiveTvPlayerStore` thread the 5 new `ClientCapabilities` fields. Live matrix re-verified with the exact emitted JSON: profile-8 hevc **and** AV1 DV → direct play, profile 7 → decodable transcode, HDR10+/SDR/h264 titles unchanged, SDR-only client still tone-mapped (now decodable). New `DeviceProfileTest` (8 tests). **Not on-device verified** (deploy is user-initiated) — note the DV half already works with the *installed* APK after a backend restart, since profile 8 only needs the `supportsHdr10` signal the app already sends. Known adjacent issue left out of scope and documented in the spec: eager text-subtitle sideloading made Jellyfin spend 4m37s extracting this file's 4 `subrip` tracks at play time. | [spec](specs/ravilo/requirements/phase-R183-dolby-vision-playback.md) |
| R163 | ✓ Done | **Play-Trailer button + fullscreen YouTube/Vimeo embed** — the Movie/Series detail hero's **▷ Trailer** button is now **conditional** on `detail.trailer` (Phase 130's DTO field; there was no button at all before this phase, contrary to the spec's "stub" framing) and opens `TrailerOverlay` — a fullscreen embed (`youtube-nocookie.com/embed` autoplay+`controls=0`, or `player.vimeo.com`) with a top chrome bar (Trailer kicker · title · trailer name · provider pill · focusable Close). TMDB trailers have no direct stream, so `TrailerEmbed` (`ravilo-ui/.../seams/`) is an **embedded provider player, not ExoPlayer**, expect/actual per platform: Android hosts a `WebView` (`mediaPlaybackRequiresUserGesture=false`, torn down via `AndroidView.onRelease`) composing normally behind the Compose chrome (both visible/interactive — no platform quirk); **web hosts a DOM `<iframe>` fixed above the Compose canvas** (`z-index:2`, `pointer-events:none`, mirroring `RaviloPlayerWasm`'s proven `<video>` z-order technique), which as a **known, documented trade-off** means the video wins the visual stack on web (the Close chrome is logically focused and Back/Select-driven exit works via Compose's key routing, but isn't visually drawn on top of the iframe there) — Android gets the full persistent-chrome experience the design intends. Close (or Back, always) exits, tears down the embed, and restores focus to the Trailer button. "Trailer" was already untranslated in all 3 locales (`action.trailer` pre-existed in `Strings.kt`). Out of scope per spec: NFO writing was Phase 130's call, not this one. | [spec](specs/ravilo/requirements/phase-R163-trailer-playback.md) |
| R162 | ✓ Done | **Behaviour preferences as a field-level per-user overlay (decoupled from the layout override)** — split the Ravilo config into **two independent per-user layers**: the R51 **layout override** (`ravilo_config` table, hero/channels/rows/Top 10, *full-replace*, untouched) and a new **behaviour & preferences overlay** (new `ravilo_behaviour` table/`BehaviourOverlay` model — `ui_language`, `skin`, `tile_shape`, `show_continue_progress`, `autoplay_next`; sparse, *field-level*). **Fixed the confirmed-live bug**: `applyViewerSettings` (the `PUT /api/tv/settings` handler) used to `getConfig→save`, snapshotting the whole resolved global layout into a personal per-user record on a viewer's first setting change; it now writes **only** a `"viewer"`-tagged overlay entry, touching `ravilo_config` not at all. Resolution is **viewer entry → admin entry → global default** (`RaviloConfigService.resolveBehaviour`); `getConfig()` always overwrites the 5 fields with the resolved overlay after loading the layout record, so overriding a preference never creates/needs a custom layout and removing one (`removeCustomConfig`) never loses preferences (separate table). Setting a value equal to the global default clears back to "follow global" (sticky). New `GET`/`PUT`/`DELETE /tv/admin/behaviour` routes (admin set/reset — refuses to overwrite a `"viewer"`-tagged entry, matching the design's guardrail) plus a one-time idempotent boot migration (`migrateAllLegacyBehaviourFields`) lifting existing per-user records' divergent fields into the overlay. Admin editor (`RaviloConfig.kt`): the Behaviour section is now **always editable in user scope** (independent of the "Create custom layout" lock), each field shows Following-global/Overridden/✱-Set-by-viewer state + Reset, and applies **immediately** (not gated behind the big Save button) — global scope is unchanged (edits the shared defaults via the normal Save flow). `ViewerSettingsRequest`/`ResolvedBehaviour` gained `ui_language` (R161 will be the first UI to send it). | [spec](specs/ravilo/requirements/phase-R162-behaviour-preference-overlay.md) |
| R161 | ✓ Done | **In-app Settings page: theme · language · unpair** — Added the missing language picker and an "Unpair this TV" danger action. | git history |
| R160 | ✓ Done | **Upcoming: a dedicated air/release calendar (Sonarr + Radarr) with detail, "already available" & "missing" states** — A new tab showing upcoming episodes/movies with live availability status and a "missing from your library" section. | git history |
| R159 | ✓ Done | **Portrait screen config: per-orientation overrides, first = hero height 20–100 %** — A phone/portrait viewport can now use a different hero height than landscape/TV. | git history |
| R158 | ✓ Done | **TV player: drop the opaque white fill on focused transport buttons** — Replaced a one-off white-fill focus style with the app's standard accent-ring chrome. | git history |
| R157 | ✓ Done | **Ravilo web: a working media player — visible video + clickable controls** — Fixed the video being hidden behind an opaque Compose canvas and the transport controls having no real click handlers. | git history |
| R156 | ✓ Done | **Ravilo web: kill the fullscreen "Critical dependency" dev overlay** — Suppressed a harmless Kotlin/Wasm build warning that was fullscreening the webpack dev-server overlay. | git history |
| R155 | ✓ Done | **Ravilo handles remote commands: play / playstate / home** — The client side of the Phase 111 remote-control API and the Phase 110 dashboard bridge. | git history |
| R154 | ✓ Done | **Validate the Top 10 / Discover config + warn in the Ravilo config editor** — Flags un-covered chart selections in the editor instead of letting them silently show nothing on the TV. | git history |
| R153 | ✓ Done | **Age-rating certification badge on the detail hero** — A colour-coded region+code chip on the hero, matching the admin badge's tier palette. | git history |
| R152 | ✓ Done | **Receive & display Jellyfin server messages (DisplayMessage) as a top-right toast** — The client side of the Phase 110 dashboard-message bridge — a stacking, auto-dismissing toast. | git history |
| R151 | ✓ Done | **Season picker: per-season watched indicator + remove "Mark all watched"** — Each season pill now shows a live watched-count badge/progress sliver instead of just a binary checkmark. | git history |
| R150 | ✓ Done | **Season picker: watched-state ✓ + auto-select active season** — The picker auto-selects the first incomplete season on open and scrolls to the first unwatched episode. | git history |
| R149 | ✓ Done | **Series detail: Sonarr next-airing for ongoing series** — Shows the next episode's air date in the hero for series that haven't ended. | git history |
| R148 | ✓ Done | **Series detail: episode air date in the picker** — Each episode now shows its scanned TMDB first-air date under its title. | git history |
| R147 | ✓ Done | **Instant cross-screen watched-state propagation** — marking a movie/episode played on detail (or a ≥90% player finish) broadcasts the server-returned `CardPlayState` on a `WatchedBus`; the retained Home/Browse/Search/Channel stores patch their visible tiles in place (✓ appears instantly, no re-fetch/flicker). Server-authoritative (the `setPlayed` response), same shape as R49 acquisition patching. | — |
| R146 | ✓ Done | **Chunk play-state requests so long series show watched state** — a 261-episode series asked `/api/tv/playstate` for all ids in one ~8.6 KB URL → Ktor CIO HTTP 400 "Line exceeds limit of 8192 characters" → every episode looked unwatched. `TvApiClient.getPlaystate` now chunks ids (100/req) and merges; a failed chunk is skipped. Watched state was already fully Jellyfin-controlled (verified end-to-end) — purely a transport limit. | — |
| R145 | ✓ Done | **Responsive mobile layout** — the phone target reused the TV screens, inheriting 48dp gutters + a fixed 60%-width detail hero column (wasted/cut content on a ~411dp screen). New `LocalCompact` (window < 600dp) + `raviloHPad` (20dp phone / 48dp TV) at every content-margin site; movie+series detail hero fills full width on phone. TV unchanged. | — |
| R144 | ✓ Done | **Multi-provider Discover + Search navbar padding fix** (was mislabeled R142 in commit `a111e0f`) — backend `/api/tv/discover` was spec-looking up only `d.source` (single provider), silently dropping lists from other providers; fixed to scan all registered providers' `availableLists(d.region)`. Same fix for the detail endpoint (sourceLabel/attribution now derived from the actual owning provider). Discover header "via Tudum" hardcode removed — subtitle now "Trending now · {region}" since row titles carry provider context. SearchScreen `padding(top = 48.dp)` corrected to `appBarHeight + 24.dp` (12dp of content was hidden behind the navbar). | — | backend `/api/tv/discover` was spec-looking up only `d.source` (single provider), silently dropping lists from other providers; fixed to scan all registered providers' `availableLists(d.region)`. Same fix for the detail endpoint (sourceLabel/attribution now derived from the actual owning provider). Discover header "via Tudum" hardcode removed — subtitle now "Trending now · {region}" since row titles carry provider context. SearchScreen `padding(top = 48.dp)` corrected to `appBarHeight + 24.dp` (12dp of content was hidden behind the navbar). | — |
| R143 | ✓ Done | **Per-channel system rows** — Continue Watching and Newly Added become configurable per Custom channel (show/scope/merge) instead of only global. | git history |
| R142 | ✓ Done | **Watched state everywhere** — Jellyfin played/in-progress state (✓ badge, progress sliver, mark-played) now appears on every poster surface across Home, Browse, Search, detail, and the player. | git history |
| R141 | ✓ Done | **Resilient live config propagation: never need an app restart again** — Added a poll fallback and reconnect hardening so config changes reach the TV even if the single live WebSocket silently stops working. | git history |
| R140 | ✓ Done | **Comfortable vertical framing for content rows** — A focused row now settles toward the middle of the screen on down-navigation instead of the bottom. | git history |
| R139 | ✓ Done | **Stable back-focus: return to the exact originating tile** — Completes R137 — Back now restores focus to the exact tile that was selected, not just the scroll position. | git history |
| R138 | ✓ Done | **Series detail: don't skip the season picker, smoother scroll, keep hero scroll-to-top** — Fixed a focus race that could skip the season picker on DOWN, and replaced an instant inter-section scroll with an animated one. | git history |
| R137 | ✓ Done (partial) | **Back restores scroll to where you were** — Home/Channel/Browse retain scroll position across navigate→back; exact-tile focus restore was completed separately by R139. | git history |
| R136 | ✓ Done | **Channel page: top-navbar parity** — The Channel page now shows the full section-nav app bar instead of a stripped-down back-only bar. | git history |
| R135 | ✓ Done | **Focusable, expandable description (detail)** — The synopsis is now D-pad focusable and expands inline on select instead of being permanently truncated. | git history |
| R134 | ✓ Done | **Merge audio + subtitle flags into one line (detail)** — Combined two stacked flag rows into one horizontal line. | git history |
| R133 | ✓ Done | **Serve Ravilo artwork from jellystructure on-disk files (retire the Jellyfin image proxy)** — Ravilo images are now served directly from jellystructure's own resized on-disk artwork, fixing blank-poster races caused by the old Jellyfin proxy. | git history |
| R131 | ✓ Done | **Episode-still screen-grabber (lowest-priority placeholder, auto-upgraded by TMDB)** — Auto-extracts a video frame for episodes with no TMDB still, auto-upgraded once a real TMDB still becomes available. | git history |
| R130 | ✓ Done | **Title-logo with readable text fallback (hero + detail)** — Hero/detail titles fall back to text instead of blank when a clearlogo is missing or fails to load. | git history |
| R127 | ✓ Done | **Workbench facet-value counts (+ channel-scoped narrowing)** — Facet chips now show item counts, narrowed to a channel's own filter when the workbench is opened from inside one. | git history |
| R87 | ✓ Done | **Channel row-coverage gap via a workbench `Content row` facet** — a title can match a channel filter yet no Custom content row, so it's invisible while browsing that channel. Made "in channel, in none of its rows" an ordinary workbench filter, not a bespoke report: (R87a) shared `Condition.rows` + a `content_row` membership facet in `ConditionEvaluator` (recursively runs each referenced row's filter; is_any_of/is_none_of) — gap = `channel conds AND content_row is_none_of <rows>`, evaluated everywhere (list/batch-count); (R87b) the shared Workbench authors/edits the contextual `content_row` facet (offered only with a rows context; row-chip value picker; round-trips `Condition.rows`); (R87c) the channel editor shows an inline "Not shown by any row" gap panel (orphaned-title tiles + N-of-M badge, green at 0, live) + a "＋ Add catch-all row" (no-condition row closes the gap), System rows excluded; (R87d) "Open these N in Library ↗" hands the filter to the Library — channel conds as per-facet params + the content_row condition as a `coverage` URL param — rendered as a normal removable/editable active chip (⚙ opens the workbench with the rows context), no special Library mode (FR-RV-C5) | git history |
