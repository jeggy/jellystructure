package dev.jellystructure.shared.tv

/** R293 (FR-R293-7) — a browser's WebSocket API cannot set a handshake header, so the query form stays
 *  here and only here. */
internal actual val WS_TOKEN_IN_QUERY: Boolean = true
