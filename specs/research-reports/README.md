# specs/research-reports/

Standalone **research & investigation reports** — deep dives that inform a decision or a future
phase but are not themselves specs. Each report is a point-in-time analysis (audit, feasibility
study, decoupling plan, perf investigation, etc.), often produced before the work is scoped into
`requirements/`.

**How this differs from the rest of `specs/`:**
- `constitution.md` / `plan.md` / `requirements/` describe what the system **is and must be** —
  they are kept current with the code.
- Reports here are **research, dated at the top**, and may go stale. They capture *why* a
  direction was chosen and the findings behind it. When a report's plan is adopted, the durable
  parts graduate into `requirements/` (a phase spec) and/or `constitution.md`; the report stays as
  the record of the analysis.

**Convention:** one file per report, kebab-case, with a `**Date:**` line near the top. Add a row
to the index below.

| Report | Date | Summary |
|--------|------|---------|
| [`ravilo-jellyfin-decoupling-investigation.md`](ravilo-jellyfin-decoupling-investigation.md) | 2026-06-26 | How much Ravilo depends on Jellyfin (direct + via jellystructure) and a plan to serve catalog, artwork, and per-user-state reads from jellystructure so the app loads faster — streaming stays on Jellyfin. |
| [`backend-performance-investigation.md`](backend-performance-investigation.md) | 2026-06-26 | Why the Library page, text search, and the Ravilo sofa feel slow: every read decodes the whole 18.7 MB / 307-item JSON-blob library (no SQL pushdown, no decode cache, no gzip). Prioritised workstreams (decode cache, projection, caching, transport, engine, filter caching) → specced as phases 88–90 + R86. Includes the no-flicker rule for Ravilo. |
| [`session-user-device-management-2026-07-07.md`](session-user-device-management-2026-07-07.md) | 2026-07-07 | Why every Ravilo device binds to the admin user (the "Sign in as" pick is never sent; `/tv/pair/approve` copies the admin cookie identity), why that stale admin token spams the 401/403 session-bridge logs, and that restricted Jellyfin users see the whole catalog (no per-user ACL). Decision: retire code pairing for a username/password login proxied through jellystructure, filter the catalog by each user's Jellyfin library access, add a Users & Devices overview → specced as phases 141–143 + R175. |
| [`ravilo-cover-art-video-track-2026-07-08.md`](ravilo-cover-art-video-track-2026-07-08.md) | 2026-07-08 | Why *Server Farm* (TURG release) wouldn't start on the TV: the MKV muxes a `cover.png` as a non-attached-picture video stream (`video/x-unknown`), and `RaviloRenderers`' `EXTENSION_RENDERER_MODE_PREFER` preferred the FFmpeg *video* renderer, leaving the real h264 track with no decoder. Client fix shipped (hardware-only video, `482a336`). Proposes future jellystructure **management support**: scan-time detection of a `cover_as_video` triage type + a one-click "fix cover track" repair reusing the track-edit infrastructure. Also notes the SurfaceView/screencap diagnostic caveat. |
| [`credits-and-intro-detection-2026-07-13.md`](credits-and-intro-detection-2026-07-13.md) | 2026-07-13 | How to give Ravilo's "Next Episode" popup (and a new "Skip Intro") a Netflix-level experience instead of today's fixed "20s before file end" heuristic. Surveys existing chapter-scan infra (Phase 149) and the `PipelineStepOps` extension point as reuse points; external research on Jellyfin's Intro Skipper (Chromaprint cross-episode fingerprinting for intros, ffmpeg black/silence heuristics for credits — two different techniques), Plex, and Amazon's (not Netflix's) published ML approach. Decision: port/adapt Intro Skipper's own approach directly into jellystructure's backend with GPL-3.0 attribution (both projects share that license) rather than depend on the Jellyfin-side plugin or its native `MediaSegments` API (confirmed plugin-gated even for writes). Proposes a `SegmentMarkers` data model (no migration needed), a phased `detect_segments` pipeline step, player trigger changes with graceful fallback for unscanned content, and a new jellystructure admin scrubber UI for reviewing/overriding detected timestamps. |
| [`stue-tv-4k-playback-stutter-2026-08-28.md`](stue-tv-4k-playback-stutter-2026-08-28.md) | 2026-08-28 | Why 4K titles stutter on stue TV, checked against the real files/client/server after a third-party briefing blamed Dolby Vision demuxing, lossless-audio overload and missing 24p matching. **All three disproved for the reported title:** the file is DV Profile 8.1 with **E-AC3 768 kbps** (no lossless track at all), Media3 parses `dvcC` once at init and the TV has native `dvhe.st` hardware DV, and the panel exposes a **single 60 Hz mode** with `mRefreshRateChangeable: false` so app-side refresh matching has nothing to switch to. What the evidence does show: both TV decoders declare a **60 Mbps** ceiling the server never asks about (Spindlelegs runs 93 Mbps average for two hours), an **18.6 % Wi-Fi retry ratio** during the reported viewing hour plus a 2.4 GHz association ejected by `DISASSOC_LOW_ACK`, and `sda` read latency going **8.17 → 73.43 ms** under concurrent writes on the spindle that holds the library, the download target and the cross-seed hardlinks. Also catalogues five gaps in our own code (hardcoded `deviceProfile()`, `audioCodecs` populated but never read, no link telemetry, no QoE capture, playback-blind background work) → specced as phases **177**, **178** + **R216**. Includes what was *not* established. |
| [`ravilo-web-pwa-player-cast-2026-09-18.md`](ravilo-web-pwa-player-cast-2026-09-18.md) | 2026-09-18 | The web client as the phone client instead of a native iOS app: install (PWA) on iOS and Android, a proper player, and Chromecast. Measured: the production bundle **boots on WebKit** (Playwright 1.61, Safari 26.5-era) and at phone width in Chromium with zero errors; the live site ships **15.3 MB of wasm uncompressed**, one-hour cache, **no CSP** on `ravilo.example.net`, and `manifest.json` answers with `index.html`. The player is a `<video>` behind an opaque canvas with a z-index swap and a second DOM transport bar, constant `isBuffering=false`, no track switching, CDN-loaded hls.js/JASSUB, and it sends the TV's `mkv/hevc/eac3` list to the server — a black screen on an iPhone (the receiver's `hls_only` bug, one level finer). Facts verified: WasmGC needs **iOS 18.2+**; iOS 26 opens every Home Screen site as a web app; **no `requestFullscreen` on iPhone** (the installed app *is* the fullscreen); Cast Web Sender is Chrome-only and **no Cast on any iOS browser**. Proposes 235 (serving), R263 (installs), R264 (honest capabilities), R265 (the web player = R244's phone player, hinging on one spike: a transparent `ComposeViewport` canvas over the video), R266 (Cast from Chrome), R267 (Play on a Ravilo TV), and a gated 236/R268 **backend Cast launcher** (Cast v2 over TLS from Native) that would make casting a server feature for every client. Seven owner questions. |
| [`ravilo-mobile-player-chromecast-ios-2026-09-16.md`](ravilo-mobile-player-chromecast-ios-2026-09-16.md) | 2026-09-16 | The phone app measured against a mobile video player's conventions, plus what "proper Chromecast" and an iOS target would take. Findings: the phone renders the **TV player chrome verbatim** (48 dp gutters, forced landscape, session ended on `ON_STOP`, no `MediaSession` on phone by R193's decision, no PiP/background/lock-screen/gestures), there is **no phone player design at all**, and a phone on cellular gets **no bitrate cap** because `detectLinkState()` only knows ethernet/Wi-Fi. Chromecast: three architectures — Jellyfin's own receiver (bypasses every jellystructure invariant; spike only), a **Ravilo CAF web receiver** that is just another Ravilo device (recommended; `shared` already compiles to JS for Tizen), and "Play on a Ravilo TV" via Phase 111 (nearly free) — with Cast Connect as the end state. Every cast session is a Jellyfin remux/encode (Cast never direct-plays MKV). iOS: no target exists, `commonMain` is clean, 30 `expect`s need actuals, and the engine choice is AVPlayer + `hlsOnly` (AirPlay/PiP free, MKV/DTS never direct) vs VLCKit. Proposes phases 216/R243–R252 and eight owner questions. |
| [`ios-build-host-macbook-setup-2026-09-16.md`](ios-build-host-macbook-setup-2026-09-16.md) | 2026-09-16 | Guide for using the owner's MacBook as the iOS build host while development stays on Debian: Apple targets only build on macOS (Debian skips them), so the model is edit on Debian, build/run/upload on the Mac over SSH. One-time setup (Xcode, JDK 21, CocoaPods, Android cmdline-tools because `:ravilo-ui` applies AGP, kdoctor, automatic signing, App Store Connect API key, keep-awake + Remote Login), day-to-day commands (push/pull or rsync, `xcodebuild`, `simctl`, `devicectl`, screenshots and logs back to Debian), a self-hosted macOS GitHub runner for tag-driven TestFlight uploads that skips rather than fails when the laptop is closed, and the project changes the bring-up phase needs (`iosArm64`/`iosSimulatorArm64`, framework export, `:ravilo-ios`, VLCKit + Cast SDK pods). Steps marked *verify* are to be confirmed on first run. |
| [`claude-code-remote-agent-management-2026-08-10.md`](claude-code-remote-agent-management-2026-08-10.md) | 2026-08-10 | **Standalone — unrelated to jellystructure.** Implementation-ready design brief for a personal, self-hosted Claude Code control-plane website: spin up/resume sessions across multiple project directories, control them from a machine other than the one holding the checkouts, auto-resume after usage-quota exhaustion. Doc-verified against the Agent SDK reference. Key findings: Claude Code's own **Remote Control** is a **closed dead end** for this (claude.ai-UI-only, not a public API — a custom site can never attach to a `remote-control` session), so the design builds on the **Agent SDK**, which is Anthropic's supported path for embedding Claude Code in your own app and loses none of Remote Control's properties; a **`rate_limit_event`** message carries `status` + `resetsAt` + `rateLimitType` (`five_hour`/`seven_day`/…), which is the exact auto-resume primitive (never hardcode 5h — there's a weekly bucket too); **`SessionStore`** is a first-class adapter interface making cross-host resume supported rather than bespoke; and `canUseTool` is an async callback with an `AbortSignal`, so remote phone-tap approval is directly supported. Specifies a three-tier architecture (website → per-host runner daemon → CLI subprocesses), the **copy-paste runner enrollment/pairing flow** (short-lived enrollment token → long-lived credential, npx / installer / Docker distribution, and the "Claude Code isn't signed in on that host" onboarding state your web tool cannot resolve for the user), the full data model, the **REST + WebSocket API contract the UI is built against**, the auto-resume state machine, a per-workspace permission-profile design (incl. the "auto-approved tools never reach `canUseTool`" trap), an 11-screen UI inventory, and a risk-retiring build order. Open risks flagged: `canUseTool`'s signature differs across SDK versions, `rate_limit_event` is undocumented in the official reference (pin the parser to a captured real payload), and unattended OAuth-token longevity for subscription auth on a headless server. |
| [`stue-tv-test-sweep-2026-09-16.md`](stue-tv-test-sweep-2026-09-16.md) | 2026-09-16 | Live stue-TV sweep of the ten most recently implemented phases. Found the running backend is a phase behind the TV client (`main` @ 210; 211–215 undeployed), which is itself why phase **211**'s symptom is live — a series' own progress/next-up never updates while Continue Watching does. **R241 is still reproducible**: a remembered per-series subtitle is never applied to the next episode (three repros), and the `dan`/`da` split it blames turns out to be a perfect proxy for embedded-vs-external-sidecar; what gets selected is exactly the source-default fallback, pointing at `persistChoice`'s `seriesKey = currentSeriesId ?: currentItemId` rather than at the language comparison. **R242** works on-device and its open question closes: the scrim is too weak to read the panel over, and the panel's text runs to x=1920 with no TV safe-area margin. Plus unmapped languages rendering as `EL`/`HBS-SRP`/`HBS-HRV` (the same map `sameLanguage()` depends on), a "Soon" badge painting over the resume badge, fast chrome auto-hide making D-pad sequences land on "Next", and a prod `focus_detail_delay_ms = 3200` that makes J invisible in practice. |
