# Tasks

Step-by-step TODO for the **active** phase. Keep this short and current — when a phase finishes,
clear its checklist and seed the next phase from its spec in [`requirements/`](requirements/).

See [`STATUS.md`](STATUS.md) for the overall snapshot.

---

## Active: none — the admin app is feature-complete (phases 0–47 done)

No admin phase is in flight. Active development is on the **Ravilo** side; see
[`ravilo/STATUS.md`](ravilo/STATUS.md) and
[`ravilo/requirements/README.md`](ravilo/requirements/README.md). When a new admin phase starts, seed
its checklist here from its spec in [`requirements/`](requirements/).

## Loose ends (track in STATUS.md)
- [ ] Resolve the port free-check `TODO` in `Main.kt` (`checkPortFree` "doesn't work"; catch the bind
  exception instead).
