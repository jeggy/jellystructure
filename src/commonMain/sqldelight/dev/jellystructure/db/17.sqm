-- Phase 142 — a restricted user's allowed Jellyfin library ids, normalized (no dashes, lowercased)
-- and comma-joined; NULL = unrestricted (admin or EnableAllFolders) (v17 -> v18).
ALTER TABLE ravilo_device ADD COLUMN allowed_libraries TEXT;
