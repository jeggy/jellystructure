-- Phase 143 — admin web sessions previously tracked only expiry; add created/last-used so the
-- Users & Devices overview can show them (v18 -> v19).
ALTER TABLE session ADD COLUMN created_at INTEGER NOT NULL DEFAULT 0;
ALTER TABLE session ADD COLUMN last_used_at INTEGER NOT NULL DEFAULT 0;
